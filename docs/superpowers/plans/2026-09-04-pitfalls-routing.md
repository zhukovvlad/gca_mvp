# План: §11 «Грабли» — корпус по областям, раздел маршрутизатором

**Спека:** `docs/superpowers/specs/2026-09-04-pitfalls-routing-design.md`
**Ветка:** `docs/pitfalls-routing`

**Коммит-основание:** `b40435b` (мерж PR #42). Все числа плана сняты на нём;
там же берётся текст корпуса для сверки.

## Global Constraints

- **Инварианты §3–§8 и §10 по существу не меняются.** В §10 только добавляется
  пункт DoD; ни одно действующее правило не переформулировано.
- **Текст пунктов §11 не переписывается** — кроме четырёх замен отсылок,
  перечисленных поимённо в Task 2. Всё остальное переносится посимвольно.
- **Миграции не редактируются** (§9.3). Из 58 ссылок на «§11» в исходниках
  часть лежит в `alembic/versions/`; адрес «§11» поэтому обязан остаться
  действующим, и ни одна ссылка в коде не правится этой фичей.
- **Плану запрещены тела тестов и тела реализации** (§9.1, v6.16). Ниже —
  утверждения, подписи и типы. Это первый план проекта под этим правилом.
- **Порог размера:** `AGENTS.md` ≤ 72 000 символов после нормализации к LF.
- **Кириллица в stdout:** любой скрипт, печатающий по-русски, обязан нести
  `sys.stdout.reconfigure(encoding="utf-8")` — консоль cp1252, и
  документированная команда должна работать без `PYTHONIOENCODING`.
- **`backend/scripts/` покрыт ruff** (`line-length = 120`, select
  `E,F,I,B,UP,SIM`). Прогон — `uv run ruff check .` из `backend/`, без
  конвейера: код возврата после `| tail` принадлежит `tail`.
- **Разовые скрипты в репозиторий не кладутся** — у них нет предмета после
  мержа (решение v6.17, повторено в спеке §2.3).

## Структура файлов

```
AGENTS.md                                  Edit: §11 → маршрутизатор; преамбула;
                                                 §9.2, §9.3, §10; врезка v6.18
docs/AGENTS-revisions.md                   Edit: + запись ## v6.17
docs/pitfalls/frontend.md                  Create: 18 пунктов
docs/pitfalls/runs.md                      Create: 6
docs/pitfalls/backend.md                   Create: 7
docs/pitfalls/db.md                        Create: 6
docs/pitfalls/parser.md                    Create: 5
docs/pitfalls/surfaces.md                  Create: 2
docs/pitfalls/process.md                   Create: 2
backend/scripts/check_agents_index.py      Edit: + 5 проверок (12–16)
justfile                                   Edit: + рецепт check-agents-index,
                                                 вызов из ci
.github/workflows/backend-tests.yml        Edit: + шаг стража
docs/devlog/2026-09-04-pitfalls-routing.md Create: финал фичи
```

Вне репозитория (разовое, по Global Constraints): `$TEMP/verify_pitfalls_move.py`
(сверщик корпуса) и `$TEMP/mutate_pitfalls.py` (обвязка пяти снятий).

## Решения плана, которых нет в спеке

1. **Порядок задач: сверщик корпуса пишется ПЕРВЫМ, до переезда.** Спека
   требует его существования (DoD 4), но не говорит когда. Первым — потому что
   до переезда он обязан быть КРАСНЫМ («46 пунктов базы, 0 найдено в
   `docs/pitfalls/`»), и это единственный момент, когда его собственная
   работоспособность проверяема: после успешного переезда зелёный сверщик и
   сверщик, который ничего не сравнивает, выглядят одинаково. Тот же приём, что
   у стража: сначала красный вход, потом зелёный.
2. **Ревизия v6.18 делается ОДНОЙ задачей вместе с переносом v6.17 в архив.**
   Разделить нельзя, и это следствие действующего стража, а не вкус: перенести
   v6.17 в архив, оставив заголовок `v6.17`, красит проверку 6 («действующая
   отсутствует в архиве»); поднять заголовок до v6.18, не перенеся v6.17,
   красит проверку 5 («у каждой исторической строки есть запись в архиве»).
   Зелёное состояние существует только когда оба сделаны.
3. **Шапка файла области — ровно одна строка, начинающаяся с `**Когда
   читать:**`.** Спека требует шапку, но не её форму; форму надо закрепить,
   иначе проверка 15 стережёт вкус исполнителя. Литерал вынесен в константу
   скрипта.
4. **Пункт файла области распознаётся как строка, начинающаяся с `- `, в
   начале строки.** Тот же формат, в котором пункты живут в §11 сейчас;
   продолжения — строки с отступом. Распознавание НЕ зависит от шапки
   (требование спеки §2.3).
5. **Порядок пунктов внутри файла — как в §11**, по возрастанию исходного
   номера. Сортировка по смыслу не делается: она была бы вторым суждением
   поверх классификации и не проверяема.
6. **Классификация 46 пунктов зафиксирована в Task 2 таблицей поимённо.** Без
   этого исполнитель изобретал бы её заново, а спека называет её суждением с
   тремя спорными пунктами.

## Задачи

### Task 1: Разовый сверщик корпуса, предъявленный красным

**Files**
- Create: `$TEMP/verify_pitfalls_move.py` (в репозиторий НЕ входит; `$TEMP` —
  каталог временных файлов, как у обвязок снятий прошлых фич)

**Interfaces**
- Потребляет: `git show b40435b:AGENTS.md` (существует), каталог
  `docs/pitfalls/` (на момент задачи ещё пуст).
- Производит:

```python
BASE: str                      # "b40435b", хэш коммита-основания
HEADER: str                    # литерал шапки области, см. решение 3
ALLOWED_EDITS: dict[int, str]  # номер пункта -> причина замены отсылки, 4 записи

def base_items(base: str) -> dict[int, str]
    # номер пункта -> нормализованный текст, из §11 коммита-основания

def moved_items(root: Path) -> dict[str, list[str]]
    # файл области -> нормализованные тексты пунктов

def normalize(item: str) -> str
    # склейка строк, свёртка пробелов; регистр и пунктуация сохраняются

def report() -> int
    # печатает базу, «N → M», отсутствующие, лишние, разрешённые замены;
    # код возврата 1 при любом расхождении
```

**Утверждения**
- `len(base_items(BASE))` равно **46** — числу пунктов §11 на основании;
- **до переезда сверщик КРАСНЫЙ:** каталог `docs/pitfalls/` пуст либо
  отсутствует, отчёт печатает «46 → 0» и перечисляет все 46 как отсутствующие,
  код возврата 1. Это и есть доказательство, что сверщик сравнивает, а не
  печатает зелёное;
- нормализация идемпотентна: `normalize(normalize(x)) == normalize(x)`;
- нормализация склеивает продолжения пункта в одну строку и сворачивает
  подряд идущие пробелы, но **не** меняет регистр и не удаляет пунктуацию —
  иначе посимвольная сверка перестаёт быть посимвольной;
- пункт, чей текст отличается от базового и чей номер НЕ в `ALLOWED_EDITS`,
  попадает в отчёт как расхождение;
- пункт, чей номер в `ALLOWED_EDITS`, сверяется до места замены и после него:
  меняется только названная отсылка, остальной текст совпадает посимвольно;
- `git`-обёртка идёт с `check=True`: молчаливый ноль на отсутствующем коммите
  дал бы правдоподобное «расхождений нет».

**Имена**
- Заводятся этой задачей: `base_items`, `moved_items`, `normalize`, `report`,
  `BASE`, `HEADER`, `ALLOWED_EDITS`.
- Существуют, проверено `grep`-ом: `sys.stdout.reconfigure` — приём уже
  применён в `backend/scripts/count_plan_edits.py:53` и
  `check_agents_index.py`.

**Проверка**
- `python "$TEMP/verify_pitfalls_move.py"` — **КРАСНЫЙ**, «46 → 0»,
  46 отсутствующих, код возврата 1. Вывод сохранить: он идёт в devlog как
  доказательство работоспособности сверщика.

---

### Task 2: Корпус в семь файлов, §11 — маршрутизатор

**Files**
- Create: `docs/pitfalls/frontend.md`, `runs.md`, `backend.md`, `db.md`,
  `parser.md`, `surfaces.md`, `process.md`
- Edit: `AGENTS.md` (§11 целиком: строки 633–1005 на основании)

**Interfaces**
- Потребляет: `verify_pitfalls_move.py` из Task 1, `HEADER` оттуда же.
- Производит: семь файлов и раздел-маршрутизатор; их читает Task 4.

**Классификация 46 пунктов — фиксированная, не пересматривается**

Номер — порядковый номер пункта в §11 коммита-основания, сверху вниз.

Номера даны с опознавателем — началом пункта. Опознаватель несущий, а не
украшение: по одному номеру сбой в счёте сдвинул бы всю классификацию, и
исполнитель обязан сверить оба.

**`frontend.md` — 18**

| № | Опознаватель |
|---|---|
| 11 | Печатную раскладку А4 проверяет только замер в браузере |
| 12 | Наименование работы надо зажимать по высоте всюду |
| 15 | Имена CSS-переменных из макета НЕ совпадают с токенами приложения |
| 16 | Совпадение подписей со столбцами у диаграммы |
| 17 | Меряя тесноту в браузере, меряй ВСЕ элементы ряда |
| 18 | Выравнивание СТРОКИ диаграммы двигает СТЕК колонки целиком |
| 19 | Новый корень страницы обязан нести `container-page` |
| 20 | «Прокрутка живёт внутри контейнера» — НЕ критерий читаемости |
| 21 | Утверждение о том, что делает МАКЕТ, проверяется командой |
| 22 | Выравнивание не сходится, пока у ячеек строки РАЗНАЯ КОРОБКА |
| 23 | Согласованный макет сверяется ОДНИМ ПРОХОДОМ И МАШИНОЙ |
| 27 | Утилиты `dark:` у примитивов shadcn перебивают токен-классы |
| 28 | Прогон на стенде заканчивается снимком ЭКРАНА |
| 29 | `activeBar` у recharts не возвращает прямоугольник в неактивный слой |
| 30 | Снимок нужен и для СОСТОЯНИЙ ВЗАИМОДЕЙСТВИЯ |
| 43 | `max-width` у `td` в АВТОРАЗМЕТКЕ НЕ ОБЯЗЫВАЕТ |
| 45 | Класс-утилита потребителя не побеждает вариантный класс базового |
| 46 | Тема переключается АТРИБУТОМ, а не системной настройкой |

**`runs.md` — 6**

| № | Опознаватель |
|---|---|
| 13 | `npx tsc --noEmit` во фронтенде проверяет НОЛЬ файлов |
| 14 | `npx vitest` из КОРНЯ репозитория поднимает другой vitest |
| 25 | Случайный `npx <инструмент>` из каталога БЕЗ этого инструмента |
| 40 | Перезапись файла целиком из Python переводит его в LF |
| 41 | Дата внутри ZIP имеет гранулярность 2 секунды |
| 42 | Два прогона pytest против одной тестовой БД одновременно портят оба |

**`backend.md` — 7**

| № | Опознаватель |
|---|---|
| 8 | Деньги: Decimal end-to-end; в JSON — строки |
| 9 | Pydantic ключует `field_validator`-ы по имени метода |
| 10 | Один uvicorn worker — не «дефолт, который можно поменять» |
| 24 | `Decimal.//` усекает частное К НУЛЮ, а не вниз |
| 33 | Минус, приклеенный к строке, — не вычитание |
| 34 | У величины, чей ЗНАК участвует в решении, тест обязан покрыть ОБА знака |
| 36 | Булево на направление — это два состояния там, где достижимо три |

**`db.md` — 6**

| № | Опознаватель |
|---|---|
| 1 | COALESCE-уникальные, частичные и EXCLUDE-индексы — только raw SQL |
| 4 | Инкремент `norm_version` — это не «поменял константу» |
| 5 | `UNIQUE NULLS NOT DISTINCT` — PG16-синтаксис |
| 6 | Откат приложения ниже миграции 0003 не поддерживается |
| 7 | btree не индексирует значения длиннее 2704 байт |
| 44 | `with_for_update(key_share=True)` — это `FOR NO KEY UPDATE` |

**`parser.md` — 5**

| № | Опознаватель |
|---|---|
| 2 | Парсер опирается на объединённые ячейки (`build_merged_shape_map`) |
| 3 | `normalize_job_title_with_lemmatization` тянет NLP-зависимости |
| 26 | Точность денег в xlsx ограничена форматом, а не кодом |
| 38 | `Worksheet.cell(..., value=None)` в openpyxl НИЧЕГО НЕ ПИШЕТ |
| 39 | Чтение НЕ-якорной ячейки объединённого диапазона даёт `None` |

**`surfaces.md` — 2**

| № | Опознаватель |
|---|---|
| 31 | Пустое поле — не факт, а ОТСУТСТВИЕ факта |
| 32 | Подпись, объявляющая НАПРАВЛЕНИЕ, обязана считать его по величинам |

**`process.md` — 2**

| № | Опознаватель |
|---|---|
| 35 | Дефект рядом с правкой не помечать «дофичевым» по памяти |
| 37 | «Не входило в список задачи» — не ответ на расхождение с макетом |

Суммы: 18 + 6 + 7 + 6 + 5 + 2 + 2 = **46**. Каждый номер встречается ровно
один раз; спорные три (31, 32 — поверхности, а не фронт; 40 — прогоны, а не
парсер) названы в спеке §1.2 и здесь не пересматриваются.

**Четыре разрешённые замены отсылок** — единственные правки текста; всё
остальное переносится посимвольно:

| Пункт | Было | Станет |
|---|---|---|
| 16 | «Правило то же, что для печатной раскладки **ниже**» | «…что для печатной раскладки **А4** (пункт про `@media print` в jsdom)» |
| 30 | «Правило **выше** про «снимок всего экрана в обеих темах»» | «Правило про **снимок ЭКРАНА в обеих темах**» |
| 39 | «Замерено **там же**» | «Замерено **на фиче «колонки по заголовкам»**» |
| 43 | «грабли, о которых **выше** уже есть запись про зажим и перенос» | «грабли, о которых **уже есть записи про зажим по высоте и про перенос**» |

Замена 16 несёт вторую цену, и её надо знать: отсылка «ниже» сегодня
двусмысленна — подходят и пункт про печатную раскладку А4, и пункт про
прокрутку и перенос (спека §1.3). Замена выбирает первый и тем закрывает
существующую двусмысленность; это правка по существу, и она объявлена здесь,
а не сделана молча.

**Утверждения**
- каждый из семи файлов открывается строкой `HEADER` — ровно одной;
- сумма пунктов по семи файлам равна **46**, и множество их нормализованных
  текстов совпадает с множеством из `base_items` с точностью до четырёх
  разрешённых замен;
- ни один пункт не встречается в двух файлах;
- §11 в `AGENTS.md` содержит **семь строк маршрутизации** — по одной на файл,
  каждая с областью, фразой «когда читать» и ссылкой; восьмой строки нет;
- §11 содержит правило пополнения: новая грабля заводится в файл своей
  области, а не в `AGENTS.md`;
- объём §11 после правки — **не более 1 500 символов** (ожидание ~1 200);
- ни одна из 284 строк с «§11» вне `AGENTS.md` не правится: адрес «§11»
  остался действующим;
- в перенесённых пунктах **нет ни одного литерала `v6.N`** (проверено на
  основании — их там нет), поэтому проверка 8 стража остаётся зелёной;
- четыре пути в бэктиках вида `docs/…` внутри пунктов оставлены как есть:
  это проза, а не ссылки, и путь от корня верен в любом файле (решение v6.17);
- markdown-ссылок внутри пунктов ноль, поэтому переписывать относительные
  цели не требуется — в отличие от переноса врезок в v6.17.

**Имена**
- Заводятся этой задачей: семь файлов `docs/pitfalls/*.md`.
- Существуют, проверено `grep`-ом: `docs/insights/` — соседний каталог того же
  класса памяти.

**Проверка**
- `python "$TEMP/verify_pitfalls_move.py"` — **ЗЕЛЁНЫЙ**, «46 → 46»,
  отсутствующих нет, лишних нет, четыре замены названы. Вывод — в devlog;
- `cd backend && uv run python scripts/check_agents_index.py` — зелёный по
  одиннадцати прежним проверкам (новых ещё нет);
- `python -c "import pathlib;print(len(pathlib.Path('AGENTS.md').read_text(encoding='utf-8')))"`
  — ожидание около 70 600; порог 72 000 сверяется в Task 3, после врезки.

---

### Task 3: Ревизия v6.18 — атомарно с переносом v6.17 в архив

**Files**
- Edit: `AGENTS.md` (заголовок; преамбула; §9.2; §9.3; §10)
- Edit: `docs/AGENTS-revisions.md`

**Interfaces**
- Потребляет: `check_agents_index.py` (существует, одиннадцать проверок),
  формат записи архива `## v6.N` (существует).
- Производит: заголовок `v6.18`, врезку v6.18, историческую строку v6.17,
  запись `## v6.17` в архиве.

**Почему одной задачей.** Разделение даёт только красные состояния: перенос
v6.17 в архив при заголовке `v6.17` красит проверку 6 (действующая обязана
отсутствовать в архиве); подъём заголовка без переноса красит проверку 5 (у
каждой исторической строки есть запись в архиве). Зелёное состояние
существует только когда сделано и то и другое.

**Утверждения**
- заголовок `AGENTS.md` объявляет **v6.18**;
- врезка v6.18 стоит полным текстом и называет четыре тронутых раздела —
  §9.2, §9.3, §10, §11 — и правку формы преамбулы;
- полный текст врезки v6.17 (**1 962 символа** на основании) присутствует в
  `docs/AGENTS-revisions.md` под заголовком `## v6.17` и **отсутствует** в
  `AGENTS.md`;
- при переносе цели markdown-ссылок врезки v6.17 переписаны от каталога
  `docs/`: `docs/superpowers/…` → `superpowers/…`, `docs/devlog/…` →
  `devlog/…`. Ловушка та же, что в v6.17, и проверка 11 стража её стережёт;
- в преамбуле стоит историческая строка v6.17: версия, дата `2026-09-03`,
  перечень разделов (§9.2, §12 и форма преамбулы), ссылки на спеку, devlog и
  якорь `docs/AGENTS-revisions.md#v617`;
- §9.2 несёт строку класса `docs/pitfalls/` — «известная грабля стека или
  домена по области: чем кусает, как обойти, чем замерено»; строка стоит рядом
  со строкой `docs/insights/`;
- §9.3 **не содержит** фразы «ни одна проверка их не касается» и называет
  команды буквально: docs-only и `AGENTS.md` — `just check-agents-index`;
  прочие правки — `just ci`;
- §10 несёт пункт DoD: фича называет в devlog тронутые области и прочитанные
  по ним файлы граблей;
- **инварианты §3–§8 не тронуты ни одним символом**, и это проверяется дифом
  по областям, а не чтением: `git diff` показывает изменения только в
  преамбуле, §9.2, §9.3, §10, §11.

**Имена**
- Заводятся: врезка `v6.18`, запись архива `## v6.17`, рецептное имя
  `just check-agents-index` (упоминается в §9.3 до своего создания в Task 5 —
  порядок намеренный: §9.3 объявляет команду, Task 5 её заводит).
- Существуют, проверено `grep`-ом: `## v6.16` в архиве — образец формы записи;
  строка `| Прошедшие ревизии | \`docs/AGENTS-revisions.md\` |` в §9.2 —
  образец формы строки таксономии.

**Проверка**
- `cd backend && uv run python scripts/check_agents_index.py` — зелёный по
  одиннадцати проверкам; проверки 4, 5, 6, 7, 9 и 11 подтверждают перенос;
- `python -c "import pathlib;print(len(pathlib.Path('AGENTS.md').read_text(encoding='utf-8')))"`
  — **не больше 72 000**;
- `git diff --stat b40435b -- AGENTS.md` и просмотр областей дифа: §3–§8 не
  затронуты.

---

### Task 4: Пять новых проверок стража

**Files**
- Edit: `backend/scripts/check_agents_index.py`

**Interfaces**
- Потребляет: `read_lines`, `canonical`, `HEADING`, `LINK`, `preamble_of`
  (существуют в скрипте), `docs/pitfalls/*.md` из Task 2, §11-маршрутизатор
  из Task 2.
- Производит:

```python
PITFALLS_REL: str          # "docs/pitfalls"
PITFALL_HEADER: re.Pattern # шапка «Когда читать:», литерал из решения 3
PITFALL_ITEM: re.Pattern   # начало пункта: r"^- "

def router_lines(lines: list[str]) -> list[str]
    # строки маршрутизации §11, в порядке появления

def router_targets(lines: list[str]) -> list[str]
    # цели ссылок строк маршрутизации, с кратностью

def pitfall_items(path: Path) -> list[str]
    # нормализованные пункты файла области; НЕ зависит от наличия шапки

def check_12(lines, files) -> tuple[bool, list[str]]   # множества
def check_13(lines) -> tuple[bool, list[str]]          # кратность строк
def check_14(files) -> tuple[bool, list[str]]          # дубли пунктов
def check_15(files) -> tuple[bool, list[str]]          # шапка
def check_16(files) -> tuple[bool, list[str]]          # хотя бы один пункт
```

**Утверждения**
- **12:** множество целей строк маршрутизации равно множеству файлов
  `docs/pitfalls/*.md` — в обе стороны. Файл без строки и строка в никуда
  ловятся одной проверкой намеренно (спека §2.3);
- **13:** число строк маршрутизации равно числу уникальных целей. Множества
  этого не ловят: вторая строка на тот же файл оставляет их равными;
- **14:** нормализованные ПОЛНЫЕ тексты пунктов по всем семи файлам не
  повторяются. Сравнение по первой строке отвергнуто спекой: оно запретило бы
  разные пункты с одинаковым началом и проверяло часть текста;
- **15:** каждый файл области несёт шапку `PITFALL_HEADER`;
- **16:** каждый файл области несёт хотя бы один пункт по `PITFALL_ITEM`;
- **распознавание пункта не зависит от шапки, и наоборот** — требование
  спеки §2.3. Без него снятие шапки красит и 15, и 16, и изолированного входа
  для 16 не существует. `pitfall_items` не ищет шапку и не пропускает строки
  до неё;
- у каждой проверки своё диагностическое сообщение, называющее файл;
- скрипт остаётся **read-only**: ни одна из пяти проверок не пишет на диск;
- итоговая строка печатает «16 из 16».

**Имена**
- Заводятся: `PITFALLS_REL`, `PITFALL_HEADER`, `PITFALL_ITEM`,
  `router_lines`, `router_targets`, `pitfall_items`, `check_12`…`check_16`.
- Существуют, проверено `grep`-ом: `read_lines`, `canonical`, `HEADING`,
  `LINK`, `text_tokens`, `main` в том же файле.

**Проверка**
- `cd backend && uv run ruff check .` — зелёный, без конвейера;
- `cd backend && uv run python scripts/check_agents_index.py` — «16 из 16»;
- **пять снятий, каждое красит ровно одну проверку** (входы — таблица спеки
  §2.3): контрольный прогон базы до снятий; восстановление только в `finally`;
  строка `FILE_RESTORED` в выводе; `git diff` после. Обвязка обязана требовать
  ровно 16 распознанных строк проверок и сравнивать исход мутанта с исходом
  БАЗЫ, а не код возврата с нулём: на прошлой фиче разбор вывода не сматчился
  и все мутанты отрапортовались бы убитыми.

| Проверка | Вход снятия |
|---|---|
| 12 | удалить строку маршрутизатора, оставив файл |
| 13 | продублировать строку маршрутизатора, ничего не удаляя |
| 14 | ЗАМЕНИТЬ пункт копией соседнего (не добавить) |
| 15 | снять шапку у одного файла |
| 16 | удалить все пункты одного файла, оставив шапку |

---

### Task 5: Рецепт `just`, вызов из `ci`, шаг в CI

**Files**
- Edit: `justfile`
- Edit: `.github/workflows/backend-tests.yml`

**Interfaces**
- Потребляет: `check_agents_index.py` с шестнадцатью проверками (Task 4),
  рецепт `ci` (существует, `justfile:180`), цепочку `ci-backend`
  (существует, `justfile:175`).
- Производит: рецепт `check-agents-index`; шаг workflow с той же командой.

**Утверждения**
- рецепт `check-agents-index` запускает страж из `backend/` и возвращает его
  код; тела рецепта — одна команда;
- рецепт вызывается из `ci` так, что **ненулевой код стража роняет `just
  ci`**. Цепочки `ci` идут в подоболочке с `pipefail`, иначе код возврата взял
  бы `sed` — приём уже применён, не изобретать заново;
- шаг workflow **зеркалит тело рецепта прямой командой**, а не зовёт `just`:
  решение спеки §2.5, причина — `just` не упоминается ни в одном workflow, все
  четыре шага бэкенда зовут инструменты напрямую, и установка `just` дала бы
  новый способ упасть до запуска стража;
- шаг стоит **до** тестов: он идёт секунды, а набор — семь минут, и дрейф
  документации незачем узнавать после;
- шаг не требует Postgres и не зависит от сервиса контейнера;
- **красный прогон в CI предъявлен:** PR с нарушенной проверкой роняет
  workflow, после восстановления workflow зелёный. Зелёный CI сам по себе не
  доказывает, что шаг исполняется, — это и проверяется;
- зелёный `just ci` целиком: правка трогает `justfile` и CI, поэтому
  освобождение docs-only на эту фичу не распространяется (§9.3).

**Имена**
- Заводится: рецепт `check-agents-index`.
- Существуют, проверено `grep`-ом: `ci`, `ci-backend`, `ci-frontend`,
  `lint-backend` в `justfile`; `astral-sh/setup-uv` с закреплённым SHA и
  `version: "0.11.29"` в workflow.

**Проверка**
- `just check-agents-index` — зелёный;
- умышленно сломать одну проверку, `just ci` — **красный**, восстановить;
- push в PR со сломанной проверкой — workflow **красный**; восстановить —
  зелёный;
- `just ci` — зелёный целиком.

---

### Task 6: Devlog и финал фичи

**Files**
- Create: `docs/devlog/2026-09-04-pitfalls-routing.md`

**Утверждения**
- devlog несёт замеры: размер до и после, объём §11 до и после, объёмы семи
  файлов, вывод сверщика корпуса **красный** (до переезда) и **зелёный**
  (после), пять снятий с указанием красной проверки на каждом;
- devlog называет **тронутые области и прочитанные файлы граблей** — новый
  пункт DoD §10, введённый этой же фичей, применяется к ней самой;
- devlog называет четыре замены отсылок и вторую цену замены в пункте 16:
  выбор одного из двух возможных адресатов;
- devlog называет отступления от плана, если они были, и границы: что страж
  не проверяет (чтение файла граблей — не механизируется);
- новый инсайт §12 **не заводится**, если фича не выстрадает правила (§12).

**Проверка**
- `cd backend && uv run python scripts/check_agents_index.py` — «16 из 16»
  (devlog добавил файл в `docs/`, проверка 8 сверяет упоминания версий);
- `just ci` — зелёный.

## Команды проверки

- По задаче: указаны в самой задаче.
- По фиче целиком: `just ci` (§9.3) — обязателен, фича трогает `justfile` и
  CI; `just check-agents-index` — «16 из 16»; размер `AGENTS.md` ≤ 72 000.
- Стенда фича не касается: экранов не трогает ни одного.
