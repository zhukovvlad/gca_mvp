# План: <тема фичи>

**Спека:** `docs/superpowers/specs/ДАТА-тема-design.md`
**Ветка:** `feat/тема`

> Шаблон плана. **Правила — в `AGENTS.md` §9.1 и §9.2**; здесь только форма и
> место разделов, чтобы их не приходилось вспоминать. Если форма разойдётся с
> §9.1, права §9.1 — этот файл ничего не устанавливает.
>
> Эту врезку из своего плана удалите.

## Global Constraints

Что нельзя нарушать ни в одной задаче: инварианты, которых касается фича
(со ссылкой на §), правило денег, права ролей, обратная совместимость
контракта. Одним списком, коротко.

## Структура файлов

Что создаётся, что правится, что удаляется — деревом или списком. Здесь видно
скоуп фичи целиком, до разбиения на задачи.

## Решения плана, которых нет в спеке

На утверждение гейта 3. Всё, что план решил сам: порядок задач, имена, границы
между слоями, выбор точки расширения. Если раздел пуст — так и написать
«пусто», а не убирать: пустота здесь тоже утверждение.

## Задачи

### Task 1: <что делает задача>

**Files**
- Create: `путь`
- Edit: `путь`
- Test: `путь`

**Interfaces**
- Потребляет: `имя` (существует), `имя` (существует).
- Производит:

```python
# ТОЛЬКО подписи и типы. Тел нет.
@dataclass(frozen=True)
class Что-то:
    поле: тип

def имя(аргумент: тип) -> тип
```

**Утверждения**
- <что должно быть верно, с числами, которые утверждают>;
- <негативный случай: вход отличается ровно одним ограничением>;
- <свойство, если оно есть: на каком домене и что именно исчерпывается>.

**Имена**
- Заводятся этой задачей: `имя`, `имя`.
- Существуют, проверено `grep`-ом: `имя`, `имя`.

**Проверка**
- `команда` — зелёная; сколько тестов она выбирает ДО и ПОСЛЕ этой задачи
  (новые имена задачи обязаны попадать в выбор — §9.1).

### Task 2: …

## Команды проверки

- По задаче: указаны в самой задаче.
- По фиче целиком: `just ci` (§9.3), плюс прогон на стенде, если фича трогает
  экран.
