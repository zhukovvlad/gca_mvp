# План: дорожная карта под гит

**Спека:** `docs/superpowers/specs/2026-09-17-roadmap-under-git-design.md`
**Ветка:** `docs/roadmap-under-git`, от `main` `2135dbe`

## Global Constraints

- **Факт живёт в одном месте** (§9.2). Правило пересмотра карты существует
  ровно в одном экземпляре — в §9.2; в карте остаётся указатель без пересказа.
  **Состояния «правила нет нигде» не существует ни на одном коммите:** снятие
  правила из карты и заведение его в §9.2 — один коммит (задача 2).
- **Форма таблицы §9.2 не меняется:** клетка «Где» — одиночный путь в
  бэктиках у всех строк, включая новую. Маршрут для стража живёт в прозе.
- **Продуктовые пункты карты не меняются.** Горизонты А–Г, восемнадцать
  пунктов, строка «Состояние на 09.09.2026» — не тронуты. Служебный раздел
  меняется ровно одним блоком (правило 3).
- **`/tasks` остаётся в `.gitignore` целиком**, негатор не заводится.
- **Разовый сверщик переезда в репозиторий не попадает** — он живёт в
  `$SCRATCH`, существует до мержа и умирает вместе с фичей. Это действующая
  практика, записанная в докстринге `check_agents_index.py`: «разовый сверщик
  переезда фичи (`$SCRATCH/verify_move.py`), который существует до первой
  правки и в репозиторий не попадает (§9.1)».
- **Страж красен до задачи 4.** План не обещает зелёного `check-agents-index`
  ни на одной задаче раньше той, что заводит ревизию: проверка «каждое
  упоминание версии разрешается» краснеет на спеке и плане, называющих
  `v6.23` (`docs/pitfalls/process.md`).
- **Команду, чей код возврата и есть предмет, запускать без конвейера**
  (`docs/pitfalls/runs.md`).
- **Кириллица:** `read_text(encoding="utf-8")` при чтении, `PYTHONIOENCODING=utf-8`
  при печати из дочернего python. Существующие файлы править точечно, а не
  перезаписью целиком: перезапись переводит файл в LF и делает `git status`
  грязным при пустом `git diff`.
- **Ни один тест не пишет в рабочий репозиторий.** Входы собираются списками
  строк, отсутствие файла подменяется через `monkeypatch` модульной константы
  `ROOT` — образец `backend/tests/unit/test_check_agents_index.py`.
- **В `main` не коммитить; перед каждым пушем — `just ci` целиком** (§9.3):
  фича правит `.py`, освобождение «правки только в `docs/`» к ней не относится.
  Пушей у фичи **два** (задача 5), и `just ci` идёт перед каждым.

## Структура файлов

```
docs/product-roadmap.md                       Create  задача 1, правится задачей 2
AGENTS.md                                     Edit    задача 2 (§9.2), задача 4 (заголовок, преамбула)
backend/scripts/check_agents_index.py         Edit    задача 3
backend/tests/unit/test_check_agents_index.py Edit    задача 3
docs/AGENTS-revisions.md                      Edit    задача 4
docs/devlog/2026-09-18-roadmap-under-git.md   Create  задача 5
tasks/ROADMAP.md                              Delete  задача 5, вне гита, в ОСНОВНОМ дереве

$SCRATCH/verify_roadmap_move.py               Create  задача 1, ВНЕ репозитория
$SCRATCH/test_verify_roadmap_move.py          Create  задача 1, ВНЕ репозитория
$SCRATCH/roadmap-source.sha256                Create  задача 1, ВНЕ репозитория
```

## Решения плана, которых нет в спеке

1. **Порядок задач.** Перенос → правило пересмотра (карта и §9.2 одним
   коммитом) → страж → ревизия → финал. Порядок вынужден дважды: `check_18`
   краснеет, пока §9.2 не несёт маршрут (значит задача 2 раньше задачи 3), а
   тест зелени `check_18` требует существующего файла карты (значит задача 1
   раньше задачи 3).
2. **Снятие правила из карты и заведение его в §9.2 — ОДНА задача.** Порознь
   между ними существует коммит, на котором правила нет ни в карте, ни в
   `AGENTS.md`: карта уже ссылается на §9.2, а §9.2 ещё молчит. Обратный
   порядок дал бы окно с двумя экземплярами — тоже нарушение, но менее опасное;
   атомарный коммит не даёт ни того, ни другого.
3. **Оракул переноса и его тесты живут в `$SCRATCH`, а не в `backend/`.**
   Постоянной функции у них нет: после задачи 5 источника не существует, и
   сверять больше нечего. Семь проверок при этом остаются — меняется место, а
   не объём доказательства.
4. **Полный digest источника сохраняется файлом** `$SCRATCH/roadmap-source.sha256`
   на задаче 1 и сверяется на задаче 5 ПЕРЕД удалением. `tasks/ROADMAP.md`
   игнорируется, и его правка между задачами не видна ни `git status`, ни
   ревью; удалять файл, не убедившись, что он тот же самый, — потеря данных.
5. **Имя константы `ROADMAP_REL`** — по образцу `IMPL_REL`, `SCHEMA_REL`,
   `AXES_REL`.
6. **Тесты `check_18` живут в существующем `test_check_agents_index.py`**, не в
   новом файле: `-k check_agents_index` отбирает по имени модуля, и новые
   тесты попадают в тот же выбор, что старые.
7. **Шапку «Когда читать:» карта уже несёт** — задача 1 её не заводит и не
   правит; `check_18` проверяет, что она ровно одна.
8. **Якорь `#92-таксономия-документов`** — правило слага существующих ссылок на
   подразделы (`docs/reference/screens.md#1-договорыобъекты`): нижний регистр,
   точки и слэши удалены, пробелы в дефисы.
9. **Devlog датируется 2026-09-18** — днём реализации, а не днём спеки.

## Задачи

### Task 1: Перенос карты и разовый оракул переноса

**Files**
- Create: `docs/product-roadmap.md`
- Create: `$SCRATCH/verify_roadmap_move.py` — вне репозитория
- Create: `$SCRATCH/test_verify_roadmap_move.py` — вне репозитория
- Create: `$SCRATCH/roadmap-source.sha256` — вне репозитория

**Interfaces**
- Потребляет: `tasks/ROADMAP.md` **основного дерева пользователя**
  (`C:\Users\zhukov_v\Projects\GCA_MVP\tasks\ROADMAP.md`) — только на чтение; в
  worktree этого файла нет по построению.
- Производит (внутри `$SCRATCH`, ни одно имя не переживает фичу):

```python
LINK_MOVES: tuple[tuple[str, str], ...]   # десять пар «было → стало», данные скрипта
LINK: re.Pattern[str]                     # markdown-ссылка, группа 1 — ЦЕЛЬ

def strip_link_targets(text: str) -> str
def digest(text: str) -> str              # sha256 текста с LF-нормализацией
def link_targets(text: str) -> list[str]
def verify(source_text: str, moved_text: str, docs_root: Path) -> tuple[bool, list[str]]
def main(argv: list[str]) -> int          # 0 — сошлось, 1 — расхождение, 2 — нет источника
```

**Утверждения**
- у перенесённой карты **ноль** целей, начинающихся с `../docs/`, и десять
  локальных markdown-целей всего;
- список пар «было → стало» совпадает с `LINK_MOVES` поимённо И по кратности:
  `../docs/TECH_DEBT.md` → `TECH_DEBT.md` четырежды,
  `../docs/proposals/2026-08-18-amendments-delta-model.md` →
  `proposals/2026-08-18-amendments-delta-model.md` дважды, и по одному разу для
  четырёх остальных целей;
- каждая из десяти новых целей разрешается в существующий файл относительно
  `docs/`;
- `digest(strip_link_targets(источник))` равен
  `digest(strip_link_targets(перенесённая))` — кроме целей ссылок не изменился
  ни один байт;
- **негативный: изменённый байт ВНЕ целей ссылок краснит оракул**, даже когда
  все десять пар на месте;
- **негативный: потерянная ссылка краснит оракул, хотя остаток сходится** —
  вход, на котором одна сумма без перечня целей молчала бы;
- **негативный: лишняя ссылка краснит оракул при сошедшемся остатке** — вторая
  половина того же: сходимость не доказывает разбиения, и обе стороны
  предъявляются раздельно;
- **негативный: замена не по карте** (`../docs/TECH_DEBT.md` → `../TECH_DEBT.md`)
  краснит оракул: такой пары нет в `LINK_MOVES`;
- **негативный: новая цель не разрешается** в существующий файл — краснит,
  даже если пара из `LINK_MOVES`;
- **отсутствие файла-источника даёт код 2 и названную причину**, а не молчаливый
  ноль;
- карта переносится копированием БАЙТОВ, а не перезаписью из Python;
- **полный sha256 источника записан в `$SCRATCH/roadmap-source.sha256`** — на
  17.09.2026 он равен
  `7ff5e21b944f4aa40f5a9b67fd30550c48f684ebd350af726b89f6388a17dac5`, и задача
  фиксирует ФАКТИЧЕСКИЙ на момент копирования, а не переписывает это число;
- новый файл отслеживается (`git ls-files docs/product-roadmap.md` печатает
  путь), а `/tasks` остаётся игнорируемым целиком: `git check-ignore -q tasks`
  возвращает 0 и `git ls-files tasks/` пуст — негатор не заведён.

**Имена**
- Заводятся этой задачей: `docs/product-roadmap.md`; в `$SCRATCH` —
  `LINK_MOVES`, `strip_link_targets`, `digest`, `link_targets`, `verify`,
  `main`.
- Существуют, проверено `grep`-ом: `tasks/ROADMAP.md` в основном дереве,
  `docs/TECH_DEBT.md`, `docs/proposals/2026-08-18-amendments-delta-model.md`,
  `docs/proposals/2026-08-25-tenders-model.md`,
  `docs/superpowers/specs/2026-08-27-stage-summary-design.md`,
  `docs/superpowers/specs/2026-08-24-passport-volumes-design.md`,
  `docs/superpowers/specs/2026-08-17-contract-comparison-design.md` — шесть
  целей, в которые обязаны разрешиться десять ссылок.

**Проверка**
- `PYTHONIOENCODING=utf-8 python -m pytest $SCRATCH/test_verify_roadmap_move.py -q`
  — зелёная. Выбор ДО задачи: **0** (модуля нет). ПОСЛЕ: **7** — зелень и шесть
  негативных утверждений выше.
- `PYTHONIOENCODING=utf-8 python $SCRATCH/verify_roadmap_move.py <источник>
  docs/product-roadmap.md` — код **0**, без конвейера.
- `just test-unit-k check_agents_index` — **9** и до, и после: репозиторных
  тестов задача не заводит, совпадение здесь ожидаемо.
- `just check-agents-index` — **16 из 17**, код 1. Зелёного стража задача не
  обещает: красна проверка 5 и только на спеке и плане.

---

### Task 2: Правило пересмотра — указатель в карте и нормативный экземпляр в §9.2

Одна задача и **один коммит**: порознь существует состояние, в котором правила
нет нигде (Global Constraints, решение плана 2).

**Files**
- Edit: `docs/product-roadmap.md` — раздел «Что это за документ и как он
  живёт», пункт 3.
- Edit: `AGENTS.md` — подраздел `### 9.2. Таксономия документов`: таблица и
  абзац под ней.

**Interfaces**
- Потребляет: `docs/product-roadmap.md` (задача 1).
- Производит: строку таблицы и абзац-маршрут, на которые опирается `check_18`
  задачи 3; указатель в карте, на который опирается DoD 5 спеки.

**Утверждения**
- пункт 3 карты — **markdown-ссылка**, а не адрес в бэктиках: цель
  `../AGENTS.md`, якорь `#92-таксономия-документов`;
- цель разрешается: `AGENTS.md` существует, и в нём есть заголовок
  `### 9.2. Таксономия документов`, из которого якорь построен;
- в карте **ноль** вхождений формулировки «в той же ветке» и ноль пересказов
  правила: в самой карте экземпляра не осталось;
- **в том же коммите** §9.2 несёт правило пересмотра — то есть экземпляр ровно
  один на КАЖДОМ коммите ветки, а не только в конце;
- правила 1 и 2 карты не изменены ни на символ;
- продуктовые пункты карты не изменены: восемнадцать пунктов горизонтов А–Г на
  месте, ни одна их строка не входит в diff;
- diff по КАРТЕ трогает ровно один блок текста; `AGENTS.md` того же коммита под
  это утверждение не подпадает;
- таблица §9.2 несёт **тринадцать** строк данных вместо двенадцати;
- клетка «Где» новой строки — **одиночный путь в бэктиках**, и markdown-ссылок
  во всех тринадцати клетках «Где» по-прежнему **ноль**;
- в прозе §9.2 есть markdown-ссылка, чья ЦЕЛЬ равна ровно
  `docs/product-roadmap.md`;
- ссылка стоит ВНУТРИ подраздела `### 9.2.`, а не в §9.1 или §9.3 — граница та
  же, которую `subsection_of` проводит по ближайшему `### ` либо `## `;
- абзац несёт границу класса против предложения (кончаются пункты, а не карта;
  граница — достаточность проработки, не происхождение замера) и против
  программы (у карты нет метрики и конца);
- ни одна из двенадцати прежних строк таблицы не изменена;
- новая строка стоит после строки «Предложение».

**Имена**
- Заводятся этой задачей: пункт-указатель в карте, строка «Дорожная карта» в
  таблице §9.2, абзац-маршрут.
- Существуют, проверено `grep`-ом: `### 9.2. Таксономия документов`,
  `docs/product-roadmap.md`, строка «Предложение» таблицы, `subsection_of`.

**Проверка**
- `just check-agents-index` — **16 из 17**, код 1, красна та же проверка 5.
- pytest по этой задаче не выбирает ничего: тестов она не заводит, и это
  заявлено явно. Негативные утверждения предъявляются снятием в фазе
  предъявления (`docs/process/implementation.md`).

---

### Task 3: Восемнадцатая проверка стража

**Files**
- Edit: `backend/scripts/check_agents_index.py` — константа, функция,
  регистрация в `main()`.
- Test: `backend/tests/unit/test_check_agents_index.py` — тесты `check_18`
  дописываются в существующий модуль.

**Interfaces**
- Потребляет (существует, проверено `grep`-ом): `ROOT`, `read_lines`,
  `subsection_of`, `LINK`, `PITFALL_HEADER`, `main`.
- Производит:

```python
ROADMAP_REL = "docs/product-roadmap.md"

def check_18(lines: list[str]) -> tuple[bool, list[str]]
```

Регистрация — в список `results` функции `main()`, последней строкой, с именем
«§9.2 ↔ docs/product-roadmap.md (маршрут, шапка)».

**Утверждения**
- **зелень на реальном дереве:** подраздел `### 9.2.` есть, ссылка на карту
  есть, файл существует, шапка «Когда читать:» ровно одна — предикат не
  тождественно красный;
- **ветвь 1** (подраздела `### 9.2.` нет) — один вход; ранний возврат, и
  отчёт не красит заодно ветвь 2: из отсутствующего раздела вторую ветвь не из
  чего построить;
- **ветвь 2, вход (а):** в §9.2 нет ни одной локальной ссылки;
- **ветвь 2, вход (б):** путь стоит голой подстрокой в бэктиках, без
  markdown-разметки — свидетельство, что маршрут сверяется по ЦЕЛИ ссылки, а не
  вхождением подстроки;
- **ветвь 2, вход (в):** §9.2 ссылается на ДРУГОЙ существующий локальный файл
  — свидетельство, что предикат сравнивает саму цель с `ROADMAP_REL`, а не
  непустоту множества целей: ослабленный `if not targets:` держал бы этот вход
  зелёным;
- **ветвь 2, вход (г):** ссылка на карту стоит в соседнем подразделе (§9.1 либо
  §9.3) — граница подраздела предъявлена, а не предположена;
- **ветвь 3** (файла нет на диске) — один вход, через подмену `ROOT`, а не
  правку дерева;
- **ветвь 4** (шапок «Когда читать:» не ровно одна) — кратность, границы **0**
  и **2**, два входа;
- **`check_18` участвует в прогоне `main()`**, а не только существует в коде:
  снятие регистрации роняет прогон, `main()` возвращает 1;
- ни один вход не роняет две ветви разом — каждая красна по своей;
- ни один тест не пишет в рабочий репозиторий.

**Имена**
- Заводятся этой задачей: `ROADMAP_REL`, `check_18`, десять тестов
  `test_check_18_*`.
- Существуют, проверено `grep`-ом: `ROOT`, `read_lines`, `subsection_of`,
  `LINK`, `PITFALL_HEADER`, `main`, `IMPL_REL` (образец константы),
  `check_17` (образец формы и докстринга).

**Проверка**
- `just test-unit-k check_agents_index` — зелёная. Выбор ДО задачи: **9**
  (замерено 18.09.2026: `9/1112 tests collected`). ПОСЛЕ: **19** — десять новых
  тестов попадают в тот же выбор, потому что отбор идёт по имени модуля.
  Совпадение чисел до и после означало бы, что команда новых тестов не видит.
- `just check-agents-index` — **17 из 18**, код 1: проверок стало восемнадцать,
  красна по-прежнему одна, пятая.

---

### Task 4: Ревизия v6.23

**Files**
- Edit: `AGENTS.md` — заголовок документа и преамбула.
- Edit: `docs/AGENTS-revisions.md` — врезка v6.22 полным текстом.

**Interfaces**
- Потребляет: §9.2 задачи 2, `check_18` задачи 3.
- Производит: объявление действующей версии, от которого зависит проверка 5.

**Утверждения**
- заголовок `AGENTS.md` объявляет **v6.23**;
- в преамбуле ровно **одно** объявление v6.23 — кратность считается по списку,
  а не по множеству;
- v6.23 **не** лежит в архиве;
- врезка v6.22 стоит в `docs/AGENTS-revisions.md` полным текстом и в преамбуле
  её больше нет;
- каждое упоминание `v6.N` в репозитории разрешается архивом либо действующей
  версией: спека и план перестают быть неразрешёнными;
- врезка называет «**18 из 18**» и объявляет проверки 4, 5 и 6 выполненными тем
  же переносом врезки, что и в прошлых ревизиях;
- локальные ссылки архива разрешаются в существующие файлы;
- врезка называет, что инварианты домена не тронуты: §3, §4, §5, §6, §7, §8 и
  §10 не изменены ни одним символом.

**Имена**
- Заводятся этой задачей: врезка v6.23, запись `## v6.22` в архиве.
- Существуют, проверено `grep`-ом: `TITLE_VERSION`, `PREAMBLE_DECL`,
  `ARCHIVE_DECL` (предикаты стража), `docs/AGENTS-revisions.md`.

**Проверка**
- `just check-agents-index` — **18 из 18**, код возврата **0**, запуск без
  конвейера. **Это первая задача плана, которой разрешено обещать зелёного
  стража**: раньше неё зелень недостижима по построению.
- `just test-unit-k check_agents_index` — **19** и до, и после: задача тестов
  не заводит, и совпадение здесь ожидаемо, а не подозрительно.

---

### Task 5: Финал — два круга проверки, артефакт, удаление источника, devlog

Задача идёт **пятью шагами в этом порядке**, и порядок несущий: факт нельзя
записать в devlog раньше, чем он случился, а случиться он может только после
того, как файл виден в удалённой ветке.

1. `just ci` целиком → **первый push**.
2. Подтвердить файл в удалённой ветке:
   `git ls-tree origin/docs/roadmap-under-git -- docs/product-roadmap.md`.
3. Перепубликовать артефакт; сверить sha256 источника и удалить его.
4. Записать свершившиеся факты в devlog, закоммитить.
5. `just ci` целиком → **второй push**.

**Files**
- Create: `docs/devlog/2026-09-18-roadmap-under-git.md`
- Delete: `tasks/ROADMAP.md` — **в основном дереве пользователя**, вне гита.
- Read: `$SCRATCH/roadmap-source.sha256` (задача 1).

**Interfaces**
- Потребляет внешний вход, которого нет в репозитории: **артефакт
  `https://claude.ai/code/artifact/7bd9313a-ee8b-4a41-ba47-81f083d800cf`**.
- **Исполнитель публикации называется здесь, а не подразумевается:**
  перепубликацию делает агент инструментом `Artifact` (действие `publish` с
  `url` этого артефакта) **после явного разрешения пользователя** — правка
  внешнего носителя необратима и наружу. Если пользователь публикует сам, шаг 4
  ждёт его подтверждения: факт пишется в devlog только после ответа, а не
  после отправки просьбы.
- Производит: devlog фичи (§9.1 шаг 5).

**Утверждения**
- `just ci` целиком зелёный **перед каждым** из двух пушей;
- новый файл существует в удалённой ветке — подтверждено `git ls-tree`, а не
  предположено по успеху `git push`;
- **перед удалением источника его sha256 сверен с сохранённым на задаче 1**:
  файл игнорируется, его правка между задачами не видна ни `git status`, ни
  ревью, и удаление разошедшегося файла — потеря данных. **При расхождении
  задача останавливается**, перенос повторяется с нового источника, и только
  потом удаление;
- артефакт перепубликован одной строкой-указателем; ссылка — **permalink на
  конкретный commit SHA** вида `blob/<sha>/docs/product-roadmap.md`, и SHA
  берётся у коммита, на котором одновременно существуют перенесённая карта,
  указатель в ней и нормативное правило в §9.2, — то есть не раньше задачи 2;
  практически это HEAD первого пуша (шаг 1), где есть и ревизия;
- **удаление старой копии — единственное намеренное касание файловой системы
  основного дерева**: ветка там не переключается, `git status` не меняется,
  незакоммиченный план ветки выгрузки не трогается;
- devlog называет оба внешних факта — публикацию и удаление — **свершившимися**,
  и записывается после них, а не до;
- devlog называет прирост `AGENTS.md` числом: до фичи **49 053** знака, цель
  программы сжатия ~44 000 превышена на **5 053** ещё до неё;
- devlog называет тронутые области и прочитанные файлы граблей: процесс и
  приёмка (`docs/pitfalls/process.md`), прогоны и инструменты
  (`docs/pitfalls/runs.md`);
- devlog называет, что после мержа ветка `feat/tender-changes-export`
  ребейзится и предъявляет гейт 3 повторно — фича prerequisite, и это её
  последствие.

**Имена**
- Заводятся этой задачей: `docs/devlog/2026-09-18-roadmap-under-git.md`.
- Существуют, проверено `grep`-ом: `docs/devlog/` (каталог),
  `docs/pitfalls/process.md`, `docs/pitfalls/runs.md`,
  `$SCRATCH/roadmap-source.sha256` (задача 1).

**Проверка**
- `just ci` целиком — зелёный, дважды, без конвейера.
- `just check-agents-index` — **18 из 18**, код 0.
- `just test-unit-k check_agents_index` — **19**.
- `PYTHONIOENCODING=utf-8 python $SCRATCH/verify_roadmap_move.py <источник>
  docs/product-roadmap.md` после шага 3 обязан вернуть **2** с названной
  причиной «нет источника»: источник удалён, и молчаливый ноль здесь был бы
  ложью.

## Команды проверки

- **По задаче:** указаны в самой задаче.
- **По фиче целиком:** `just ci` (§9.3), дважды — перед каждым пушем. Экранов
  фича не трогает, прогон на стенде не нужен; Postgres нужен только цепочке
  `just ci`, самой фиче — нет.
- **Оракул переноса** живёт в `$SCRATCH` и в репозиторий не попадает; после
  мержа он не сохраняется, и воспроизводимость доказательства обеспечивает
  devlog, называющий его вывод.
