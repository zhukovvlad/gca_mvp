# Devlog: пересчёт сумм сметы по изменённой ставке НДС

**Ветка:** `feat/vat-rate-recalculation`
**Спека:** [2026-08-13-vat-rate-recalculation-design.md](../superpowers/specs/2026-08-13-vat-rate-recalculation-design.md)
**План:** [2026-08-13-vat-rate-recalculation.md](../superpowers/plans/2026-08-13-vat-rate-recalculation.md)
**Рамка фазы:** [phase7-frame.md](../phase7-frame.md)

Реальные суммы и реквизиты в devlog не переносятся (DoD рамки фазы): ниже —
структурные факты, дельты, времена и счётчики.

---

## Замеры нулевой задачи (гейт)

Спека §1.6 назвала четыре замера и условие: любой разошедшийся со спекой
останавливает работу и возвращает на гейт 2. **Ни один не разошёлся.** Ниже —
числа, а не вердикты.

Стенд: PostgreSQL 16, `127.0.0.1:5459`, база `gca_dev`, миграции на `0011`
(совпадает со спекой §1.1).

### База тестов до фичи

Снята **до** любой правки, `--collect-only`, а не арифметикой (Global
Constraints плана):

| Что | Число |
|---|---|
| backend, `pytest --collect-only -q` | 1504 собранных |
| frontend, `vitest run` | 23 файла / 329 тестов, зелёные |

### Замер А — лоты, предложения и ставки корпуса

| estimate_id | лотов | предложений | различных ставок | ставок NULL |
|---|---|---|---|---|
| 1 | 1 | 1 | 1 | 0 |
| 2 | 1 | 1 | 1 | 0 |
| 3 | 1 | 1 | 0 | 1 |
| 5 | 1 | 1 | 1 | 0 |

Ни одной сметы с `distinct_rates > 1`. **Граница §5.1 остаётся ограничением, а
не дефектом.**

**Чего замер не устанавливает — и это важнее его вердикта.** У каждой сметы
корпуса **ровно одно предложение и один лот**, поэтому расхождение заявленных
ставок по лотам на этом корпусе структурно **невозможно**. Замер подтверждает,
что дефекта нет сегодня; он **не** подтверждает, что «одна ручная база на смету»
проверена на входе, способном её нарушить. Первая же смета с несколькими лотами
переводит §5.1 из «проверено» в «не проверено», и это надо пересмотреть тогда, а
не считать закрытым сейчас.

### Замер Б — механизм агрегации: **выбрана группировка**

Артефакт: [`backend/scripts/measure_vat_aggregation.py`](../../backend/scripts/measure_vat_aggregation.py).

**Отступление от плана, назначенное на исполнении.** Скрипт плана читает
`v_position_deviations`, а на стенде этот VIEW **пуст** — вся очередь Review не
разобрана, у всех 3133 каталожных строк `kind = 'TO_REVIEW'`, а VIEW фильтрует
`kind = 'POSITION'`. Прогон «как есть» сравнил бы два пустых результата и
напечатал «результаты совпадают построчно»: ложно-зелёный замер ровно того
класса, о котором [false-test-premises.md](../insights/false-test-premises.md).
Поэтому весь прогон — промоушен каталога, `ANALYZE`, обе выборки, оба `EXPLAIN`
— обёрнут в **одну транзакцию, откатываемую в `finally`**. `ANALYZE` внутри
обязателен: без него планировщик работает по статистике, где `kind='POSITION'`
не встречается ни разу.

Стенд после прогона проверен отдельной сессией: `catalog_positions` с
`kind <> 'TO_REVIEW'` — **0**, `v_position_deviations` — **0 строк**, миграция —
`0011`. Стенд не изменён.

Объёмы внутри транзакции: 5712 строк VIEW, 3 предложения со ставкой, **2921
группа у обоих кандидатов**. Построчная сверка: расхождений **0** — кандидаты
делают одну работу, планы сопоставимы.

| Прогон | Кандидат 1 (группировка): Planning / Execution, мс | Кандидат 2 (join на VALUES): Planning / Execution, мс |
|---|---|---|
| 1 | 4.372 / 11.231 | 4.738 / 16.341 |
| 2 | 4.527 / 13.051 | 4.110 / 16.714 |
| 3 | 4.302 / 11.973 | 4.685 / 18.102 |

Внутрикандидатный шум ~1.8 мс у обоих; разница между кандидатами 3.66–6.13 мс,
воспроизводится во всех трёх прогонах.

Верхний узел одинаков (`GroupAggregate` над `Sort`); различие уровнем ниже:
кандидат 1 — `Hash Join`, `shared hit=970`; кандидат 2 — `Nested Loop` с
`Index Scan` на каждую из 3882 строк, `shared hit=21498` (**в 22 раза больше**).
Причина — занижение кардинальности VALUES-джойна планировщиком (`rows=35`
против фактических 3882).

**Вердикт: `grouping`.** Три независимых сигнала: время, буферы, и свойство вне
плана запроса — список `VALUES` строится в Python и растёт линейно с охватом
матрицы. **Шаг 7 задачи 3 исполняется в варианте «группировка», как записан в
плане** — правки плана не требуется.

**Честная граница.** Корпус — 4 сметы, 6 договоров. Разница в миллисекундах
близка по порядку к шуму, и **по одному времени замер механизмы не различил
бы**; решающим стал буферный сигнал, структурный, а не шумовой. Замер не
устанавливает поведение при выборке в сотни договоров: занижение кардинальности,
из-за которого кандидат 2 получил вложенный цикл, при большем объёме может как
исчезнуть, так и усугубиться.

### Замер В — устойчивость точности: **§2.3 в силе**

Проверялась не воспроизводимость (её у `Decimal` нет причин терять), а
устойчивость к самой точности: проектная `ARITHMETIC_PRECISION = 100` против
контрольной `1000`, обе после округления до копеек.

Сетка: 3 масштаба (максимум `total_cost_total` стенда — порядок 10¹⁰ — ×1, ×10,
×100, то есть до порядка 10¹²) × 5 баз × 5 целей = **75 комбинаций**.
**Расхождений: 0 из 75.**

Отдельно — аддитивность на реальном составе позиций (смета 1, предложение 1,
1193 позиции, пересчёт 20 % → 16 %):

```
Σ money_round(restate(позицияᵢ)) − money_round(Σ restate(позицияᵢ)) = 0.18
```

18 копеек на 1193 позициях. Это **не** расхождение точностей, а известный эффект
поштучного округления против округления итога — то есть замеренное подтверждение
довода, по которому §2.2/§2.4 округляют **один раз на границе представления**, и
причина Global Constraint «внутрь агрегации округление не попадает никогда».

### Замер Г — допуск сверки нетто

Сверка `gross × 100 / (100 + vat_rate) − file_net` по предложениям, в `Decimal`.

Исключено одно предложение (`id=3`): у него **одновременно** `vat_rate IS NULL`
и нет строки `total_cost_excluding_vat`. Обе причины названы, а не пропущены
молча.

Дельты по модулю у трёх включённых: `0.02`, `0.035`, `0.02`. **Шум = 0.035.**

**`NET_RECONCILIATION_TOLERANCE = 3.5`** — на два порядка выше шума, как требует
спека §2.10. Оба числа записаны, как она же и требует.

**Чего замер не устанавливает:** три точки, у всех одинаковая заявленная ставка
20 % — поведение шума при других ставках не проверено; природа шума
(0.02–0.035) не диагностирована, замерена только величина.

### Вердикт гейта

Все четыре замера согласуются со спекой. **Расхождений нет, гейт 2 в силе.**

---

## Замер печатной раскладки А4 (задача 11)

`@media print` в jsdom не наблюдаем (`AGENTS.md` §11), поэтому замер сделан в
системном Chrome через `playwright-core` (без установки браузеров), на живом
стенде, на паспорте договора с **действующей поправкой**: заявлено файлом 20 %,
ставка показа 16 %.

**Дерево раскрыто перед замером — это и есть суть замера.** Печатный состав
следует состоянию раскрытия на экране, а по умолчанию дерево свёрнуто (§7.4).
Замер состояния по умолчанию уже пережил в проекте две фичи как дефект
([false-test-premises.md](../insights/false-test-premises.md)): обещание «нет
обрезки по правому краю» дано о полном составе, значит и мерить надо при
максимуме содержимого.

| Что | Значение |
|---|---|
| лист А4 при 96 dpi | 794 × 1123 px |
| строк дерева до раскрытия / после | 23 → **88** (20 кликов по шевронам) |
| ширина содержимого | **794 px**, выход за правый край — **0 px** |
| высота содержимого | 2969 px = **2,64 листа** (печать многостраничная, §7.4) |
| `thead` | 1, `display: table-header-group` → шапка повторяется на каждом листе |
| строк `tbody` с `break-inside ≠ avoid` | **0 из 87** |
| сноска о поправке на бумаге | **есть**, `data-print` не выставлен, вне скрытых предков |
| открытых диалогов во время замера | 0 |
| ошибок консоли | 1: `401 GET /api/auth/me` — проверка сессии ДО входа, не сбой данных |

Текст сноски на бумаге: «Суммы паспорта показаны в ставке НДС 16 %, пересчитано
из заявленной в файле 20 % — ставка НДС этой сметы скорректирована вручную.»

**Дефект самого замера, найденный по ходу и исправленный.** Первая версия
скрипта раскрывала дерево кликом по всем `button[aria-expanded="false"]` — и
попадала по кнопке «Изменить ставку», у которой тот же атрибут, потому что она
триггер модального диалога. Дерево оставалось свёрнутым (23 строки), а лист
мерился с открытой модалкой поверх него. Признак шеврона — **пустой текст**
кнопки (внутри только иконка); после уточнения селектора раскрытие дало 88 строк.
В скрипт добавлена явная проверка `dialogOpen == 0` и счётчик строк до/после:
замер, не изменивший число строк, теперь виден сразу.

## Тринадцать снятий защиты (задача 12)

Протокол [verifying-guards.md](../insights/verifying-guards.md) целиком: контрольный
прогон до снятия, обязательный `assert old in text` с проверкой единственности
вхождения, sha256 до/после, подтверждение что патч **применился**, красный прогон
с именами упавших, восстановление **из побайтовой копии** (не `git checkout --`:
он даёт CRLF и ложную тревогу), сверка sha256 после восстановления и повторный
зелёный прогон.

Контроль до снятий: **1607 passed, 6 skipped**. После всех тринадцати —
те же 1607, дерево чистое.

Работа велась побайтово, а не текстом: `read_text`/`write_text` на Windows
переводят переводы строк и превратили бы однострочное снятие в переписывание
всего файла — восстановление после такого «не совпало» бы по хэшу без всякой
причины.

| № | Что снято | Итог | Упало |
|---|---|---|---|
| 1 | ветка тождества `restate_gross` | RED | 5: три юнита на `exponent` + оба теста тождества §2.4 (паспорт и свод) |
| 2 | запрет цели при неизвестной базе | RED | 3, включая тест гонки |
| 3 | тот же запрет со стороны снятия базы | RED | 1 — ровно `test_clearing_base_with_target_kept_is_rejected` |
| 4 | `FOR UPDATE` | RED | 1 — только тест гонки |
| 5 | право `admin` | RED | 1 — `test_member_is_forbidden` |
| 6 | `vat_from_net` → универсальный множитель | RED | 1 — цель 0 % дала ненулевой налог |
| 7 | независимость `delta_to_file_total` от поправок | RED | 1 |
| 8 | различение кодов причин пустого отклонения | RED | 2 |
| 9 | приоритет «без объёма» в разбиении §2.9 | RED | 1 |
| 10 | пересчёт строки по СВОЕЙ базе | RED | 1 |
| 11 | приоритет статусов свёртки | RED | 1 |
| 12 | исключение несравнимых предложений из `delta` | **GREEN** | 0 — см. ниже |
| 13 | обнуление аудита при снятии обеих ставок | RED | 3, включая пару `RESTRICT` |

### Снятие 12 не воспроизвело дефект — и это записано замером, а не вердиктом

Спека ждёт: «добавление непроверяемого предложения обязано изменить сумму».
Воспроизвести это **не удалось**, и вот почему.

Первая попытка (складывать дельты всех проверок, подставляя ноль вместо `None`)
дала красный прогон — но **не по той причине**: падение было
`AssertionError: assert '0' == '0.00'`, то есть сдвиг экспоненты, а не изменение
суммы. Засчитывать такое снятие нельзя.

Вторая попытка изолировала сам фильтр: `_COMPARABLE` убран, но берутся только
не-`None` дельты. Результат — **105 passed, зелено**.

Вывод: исключение несравнимых предложений из суммы **инертно**, потому что
`check_proposal_net` по построению НИКОГДА не выдаёт дельту для статусов
`unknown_base` и `not_applicable` — она у них `None`. Состояние, от которого
защищает фильтр, недостижимо. Соседний слой защиты (слой 8) находится **выше**:
не в свёртке, а в конструкции самой проверки. Юнит-тест
`test_fold_delta_ignores_non_comparable_proposals` относительно этого фильтра
вакуозен: он добавляет проверки с `delta=None`, которые не могут сдвинуть сумму
ни при каком фильтре.

### Снятие 5 сначала покраснело не по той причине

Первая версия подменяла `require_admin` на `get_current_user` в сигнатуре — но
это имя в роутер не импортировано, модуль не загрузился, и упало 12 тестов
`NameError`-ом. Красный прогон был, а проверяемое свойство не проверялось вовсе.
Исправлено подменой самого имени при импорте
(`from auth import get_current_user as require_admin`): модуль цел, снято ровно
право. Результат — 1 упавший, `member` получил 200.

### Отступление по снятию 10

Спека просит снять базу из `GROUP BY` VIEW `v_category_totals`. Миграция обязана
быть неизменной во времени, а замена VIEW на тестовой базе потребовала бы
DDL-правки со своим восстановлением. Снятие применено на **потребителе**:
`_direct_totals` пересчитывает все строки по базе первой строки вместо своей.
Проверяемый инвариант тот же — «смета с разными базами по предложениям обязана
дать неверную сумму», — и он покраснел.

## Найденное на исполнении: стенд не тот, на котором писалась спека

Замеры попутно установили состояние `gca_dev`, и оно расходится с тем, что
спека считает данностью:

| Факт стенда | Что на него опирается |
|---|---|
| `catalog_positions` = 3133, **все `TO_REVIEW`**, `POSITION` — ноль | §2.12 и §6 DoD называют инвариантом «каталог 1932» |
| `v_position_deviations` = **0 строк** | матрицы, отклонений и отчёта «для банка» на стенде **не существует** |
| `rate_standards` = **0** | §2.12: «нормативы удаляются и заводятся заново» — удалять нечего |
| `v_category_totals` = 387 строк по 4 сметам | паспорт проекта работает; задача 11 (замер печати) исполнима |

Это не расхождение дизайна и не повод к гейту 2: ни одно **решение** спеки на
этих числах не стоит, а интеграционные тесты идут против `gca_test` с фабриками
и стенда не касаются. Расходятся два **числа приёмки** (§2.12, §6) и объём
подготовки стенда к задачам 11 и 13.

