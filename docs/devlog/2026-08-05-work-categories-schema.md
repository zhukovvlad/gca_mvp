# Devlog Ф1: классификатор видов работ в БД

**Дата:** 2026-08-05
**Ветка:** `feat/work-categories-schema`
**Спека:** [2026-08-05-work-categories-schema-design.md](../superpowers/specs/2026-08-05-work-categories-schema-design.md)
**План:** [2026-08-05-work-categories-schema.md](../superpowers/plans/2026-08-05-work-categories-schema.md)
**Рамка фазы:** [phase7-frame.md](../phase7-frame.md)

Первая фича фазы 7. Прикладной код не тронут: миграция, ORM-модель и тесты.

## Что сделано

1. **Таблица и инварианты** (`77f248e`) — миграция 0005 создаёт
   `work_categories`, `models.WorkCategory` зеркалит её выражения, класс
   `TestWorkCategoriesSchema` закрепляет одиннадцать инвариантов. Схемные тесты
   создают свои строки (коды `9xx`, `sort_order` от 999000) и от сида не
   зависят — поэтому задача принята отдельно и красных тестов в коммите не
   осталось.
2. **Сид и дерево** (`ec45163`) — 362 статьи шаблона литералом в миграции,
   `parent_id` выводится из самого кода одним UPDATE, миграция отказывает
   громко на сиротах и на несовпадении числа вставленных строк. Класс
   `TestWorkCategoriesSeed` — семь тестов содержимого.

## Замеры

**Тесты:** 927 passed / 6 skipped до фичи → **946 passed / 6 skipped** после.
Плюс девятнадцать — ровно столько, сколько добавила фича (11 схемных + 8 сида,
последний добавлен по кругу внешнего ревью), ни один существующий тест не тронут
и не ослаблен.

**`just ci`, зелёный путь:** backend **163,00 с (2:42)**, frontend vitest 18
файлов / 175 тестов **26,93 с**, eslint и tsc чистые, `OK: все проверки
прошли`. Заметно быстрее прошлого замера (267 с + 52,7 с в devlog процесса v2) —
машина в этот раз не была занята ничем другим; сравнивать эти числа между
прогонами нельзя, они меряют не только код.

**Круговой рейс** (`alembic downgrade base` → `upgrade head`): пять шагов вниз,
пять вверх, без ошибок. После рейса — ревизия `0005`, 362 статьи, 21 корень,
22 корзины. Требование DoD фазы выполнено.

**`alembic check`:** `No new upgrade operations detected`. В выводе есть
`UserWarning: Computed default on work_categories.is_bucket cannot be modified` —
это ожидаемо и не отказ: autogenerate `Computed`-выражения не сравнивает вовсе
(потому расхождение модели и миграции сторожит
`test_orm_declares_the_same_expressions`, а не детектор дрейфа).

**Негативная проверка отказа на сиротах** (правило трёх слоёв —
[phase6-start.md:348](../phase6-start.md#L348), а **не** `AGENTS.md` §11, как
говорят план Ф1 и devlog процесса v2: в `AGENTS.md` этого правила нет вовсе,
проверено grep-ом; делал оркестратор лично, не исполнитель — здесь легко получить
ложно-зелёный результат). Сначала подтверждено, что оба отказа вообще присутствуют в файле
(`grep -c` по тексту каждого = 1). Затем из литерала изъята статья `11.99`, её
дети оставлены на месте, и миграция перекачена:

```
RuntimeError: у статей нет прямого предка в справочнике: 11.99.1, 11.99.2, 11.99.3, 11.99.4
```

`select to_regclass('work_categories')` после падения вернул `NULL`,
`select version_num from alembic_version` — `0004`: транзакция откатилась
целиком, полусостояния нет. Файл восстановлен побайтово (sha256 до порчи и
после восстановления совпал), миграция накатана, 18 тестов снова зелёные.

**Уточнение факта, найденное этой проверкой.** У корзины `11.99` **четыре**
ребёнка — `11.99.1`–`11.99.4`, — а спека §1 и план Task 2 Step 6 перечисляют в
прозе три (`11.99.2`–`11.99.4`). Это неполное перечисление в тексте, а не дефект:
ни один тест состав детей не утверждает, счёт 22 корзин сходится (`11.99.1`
корзиной не является), ни одно из пяти замеренных решений не затронуто.
Граничный случай спеки — «ребёнок корзины сам корзиной не становится» — от этого
только шире.

**Находка финального ревью: `sort_order` стерёгся наполовину.** Ревью заметило,
что `test_sort_order_follows_the_template` сверял только **мультимножество**
значений (`ORDER BY sort_order` → `[10 … 3620]`), а не привязку `sort_order` к
строкам. Проверено снятием защиты: с формулой `(len(SEED) - index) * 10` код `1`
получает 3620, код `99` — 10, первым по `sort_order` идёт `99` — классификатор в
паспорте печатался бы задом наперёд, — и **все 18 тестов оставались зелёными**.
Спека §4 требует «монотонен в порядке шаблона»; закреплены были только
уникальность и кратность 10.

Тест усилен сверкой краёв шаблона по коду (`1` → 10, `99` → 3620). Та же поломка
теперь даёт `{'99': 10} != {'99': 3620}` и `1 failed` — механизм доказан, а не
совпадение. Полную сверку всех 362 кодов не делаем: она завела бы в репозиторий
второе представление шаблона, от которого спека §3 отказалась осознанно.

## Круг внешнего ревью (Codex): два блокирующих пробела в тестах

Оба подтверждены контрпримером до правки, оба закрыты. Миграция, сид и модель
замечаний не вызвали — правился только тест-файл.

**P1 «ORM-parity допускал лишний CHECK».** `test_orm_declares_the_same_expressions`
проверял три выбранных ключа словаря. Замер: четвёртый CHECK, объявленный
**только в модели**, давал 18 зелёных тестов и `alembic check` без дрейфа — то
есть такое расхождение не сторожил никто. Сверка словарём целиком (`ORM_CHECKS`,
симметрично `DB_CHECKS`) закрывает это: контрпример даёт `1 failed`.

**P1 «`sort_order` не привязан к строкам».** Пробел реальный, но здесь важно
назвать, что именно закрыто, а что закрыть нельзя.

Тест теперь сверяет **полный список кодов в порядке `sort_order`** с порядком
литерала `WORK_CATEGORIES_SEED`, который читается из самой миграции (второй копии
шаблона в репозитории не заводим). Это закрывает целиком расхождение
«порядок литерала → записанный `sort_order`»: контрпример, где присвоение
переставлено (индексы 12↔20), а литерал не тронут, даёт `1 failed`; прежняя сверка
краёв его пропускала.

**Чего эта сверка не ловит — и не может.** Контрпример из самого ревью
(перестановка строк `2` и `3` **внутри литерала**) остаётся зелёным, и это не
недоделка: литерал служит там и эталоном, и предметом проверки, поэтому
перестановка двигает обе стороны сравнения. Свойство «порядок литерала = порядок
шаблона» в репозитории непроверяемо по построению — шаблон в `samples/` не
коммитится, а второе представление спека §3 отвергла осознанно.

Остаток закрыт двумя свойствами, которые проверяются **без шаблона и без
литерала**, по одному содержимому таблицы
(`test_parent_always_precedes_child_and_roots_ascend_by_number`):

| Свойство | Замер |
|---|---|
| родитель всегда раньше ребёнка (спека §1) | 0 нарушений |
| корни (коды без точки, включая `99`) по возрастанию номера | выполняется |
| полная числовая сортировка | **не выполняется**: `10.2.6` перед `10.2.5`, `10.7.5` перед `10.7.4` |

Первые два и ловят контрпример ревью — `2` и `3` корни, их перестановка ломает
возрастание: `1 failed`. Третья строка таблицы — причина, по которой требовать от
порядка полной числовой сортировки нельзя: в шаблоне две законные инверсии, и
перестановку двух сиблингов внутри уровня эти инварианты не заметят. Это явно
принятая граница, как и число 362 (спека §6: снапшот шаблона, а не инвариант).

Заодно синхронизирован документационный хвост того же ревью: спека §1 и план
Step 6 называли трёх детей корзины `11.99`, а их четыре.

## Граница счётчика строк — что он на самом деле стережёт

Записано отдельно, потому что иначе следующий читатель припишет счётчику защиту,
которой у него нет. Проверка `inserted != len(WORK_CATEGORIES_SEED)` **почти ни
от чего не защищает**: `INSERT` без `ON CONFLICT` либо проходит целиком, либо
бросает исключение ещё до `SELECT count(*)`, так что «частично провалившейся
вставки» в этом сценарии не существует. Она остаётся как предписанная спекой
sanity-проверка и сработает в двух случаях: таблица окажется непустой к моменту
вставки, или кто-то позже сделает вставку условной (`ON CONFLICT DO NOTHING`).

Разделение обязанностей такое:

| Что стережём | Чем |
|---|---|
| полнота литерала (362 строки) | `test_whole_template_is_seeded` |
| дерево целиком (341 строка с точкой) | `test_every_dotted_code_has_its_prefix_as_parent` |
| сироты при накатке | отказ в самой миграции, до коммита |
| непустая таблица / условная вставка | счётчик строк |

## Отступления от плана

- **Порядок шагов Task 1 изменён: Step 4 (ORM-модель) выполнен до красного
  прогона Step 2.** Как написано, шаг был недостижим: Step 1 добавляет в тест-файл
  модульный импорт `from models import WorkCategory`, а класс появляется только в
  Step 4 — pytest упал бы на сборе с `ImportError`, а не с ожидаемым
  `relation "work_categories" does not exist`, и красный прогон ничего бы не
  доказал. Ожидания тестов при этом не менялись ни на символ; после перестановки
  красный прогон дал ровно ожидаемый текст.
- **Коммиты делал оркестратор**, исполнители только писали код и гоняли команды —
  так распределены роли на эту фичу (план, раздел об исполнении).
- **Литерал вставлен программно**, а не перепечатыванием: 362 строки русского
  текста при ручном переносе рискуют испортиться незаметно. Скрипт вставки —
  временный, в репозиторий не попал, вместе с `gen_seed.py` и `seed_rows.txt`
  удалён поимённо, каталог снят `rmdir`.
- **Первый заход Task 2 прерван** из-за нечитаемого вывода (см. хвосты). Step 1
  (генератор литерала) успел отработать; его результат проверен независимо
  (362 строки, 362 уникальных кода, 21 корень, 341 код с точкой, 22 корзины,
  сирот ноль, ни одного `?`-мусора) и переиспользован, а не сгенерирован заново.

## Хвосты

- **`alembic check` не входит ни в `just ci`, ни в GitHub workflow** — прогнан
  руками (спека §8). Добавление проверки в `just ci` — отдельная мелкая фича, в
  бэклоге.
- **Кириллица в выводе команд на этой машине бьётся.** Дочерний python с
  перенаправленным stdout пишет в ANSI codepage (windows-1251 при консоли в
  65001), и сообщение миграции приходит мусором. Лечится префиксом
  `PYTHONIOENCODING=utf-8 PYTHONUTF8=1` — с ним текст `RuntimeError` выше читается
  как есть. Данные при этом не страдают: файл сида всё время оставался валидным
  UTF-8, проверено побайтово.
- **`ruff format --check` красный** на затронутых файлах — но он красный и до
  фичи, и на уже принятой миграции 0004: проект использует только `ruff check`
  (`just lint-backend`), форматтер в гейт не входит. Не форматировали, чтобы не
  переписывать чужой код мимо скоупа фичи.
- **Прогон на живом стенде `gca_dev`** — требование DoD фазы, не Ф1: смотреть
  нечего, пока нет экрана. Планируется в Ф6 (рамка фазы).
- Следующий шаг фазы 7 — Ф2 (`feat/parser-smr-article`): guard заголовка
  «Статья СМР» и агрегатная строка допработ.
