# Ф4б: ставка НДС из шапки колонок — devlog

**Ветка:** `feat/vat-rate`. **Спека:** [2026-08-08-vat-rate-design.md](../superpowers/specs/2026-08-08-vat-rate-design.md).
**План:** [2026-08-08-vat-rate.md](../superpowers/plans/2026-08-08-vat-rate.md).
**Рамка фазы:** [phase7-frame.md](../phase7-frame.md), блок «Ф4б».

Ставка НДС заявлена в шапке ценового блока подрядчика — это факт файла, а не
вывод делением. Ф4б доводит её до `proposals.vat_rate`, а деление по блоку
итогов делает перекрёстной проверкой и никогда — источником значения. Контракт
парсера поднят до `3.1.0` (минор: ключ добавлен, ничего не исчезло), схема — до
миграции `0008`.

---

## 1. Что сделано по задачам

| Задача | Коммит | Содержание |
|---|---|---|
| 1 | `e3de07a` | `get_column_keys` поднята на уровень модуля, `money_group_offsets` — единственный источник смещений |
| 2 | `8693f70` | fixture: суффикс ставки в двух групповых шапках + тест формы входа |
| 3 | `c3ca64f` | ядро `parser/vat_rate.py`: распознавание суффикса, согласие двух шапок |
| 4 | `f0a09eb` | сверка с блоком итогов, четыре предупреждения, локальный контекст деления |
| 5 | `308f9e9` | проводка `header_row` вниз, чтение двух ячеек, контракт `3.1.0` |
| 6 | `4b010e4` | миграция 0008, колонка и `CHECK`, защитная конверсия при импорте |
| 7 | `78af374` | E2E: суффикс листа fixture → колонка `proposals.vat_rate` |
| 8 | `61ac044` | проверка некаскадирования переведена на полный трёхстрочный блок |
| 8 | `046e8da`, `1f7f1ca` | devlog и рамка; инсайты, перевод стенда |
| 8 | см. §4a | доработка по финальному ревью ветки: сквозной путь нулевой ставки и нижняя граница диапазона |

## 2. Замеры

### 2.1. Число собранных тестов — замерялось после каждой задачи

| Момент | Собрано | Дельта |
|---|---|---|
| база (до фичи) | **1190** | — |
| после задачи 1 | 1199 | +9 |
| после задачи 2 | 1200 | +1 |
| после задачи 3 | 1219 | +19 |
| после задачи 4 | 1243 | +24 |
| после задачи 5 | 1255 | +12 |
| после задачи 6 | 1261 | +6 |
| после задачи 7 | 1268 | +7 |
| после доработки по ревью (§4a) | **1274** | +6 |

`tests/unit/parser`: 334 → 343 → 362 → 386 → 398 → **400** (доработка §4a).
`tests/integration/test_import_fixture_e2e.py`: 25 → 26 (задача 2) → 33 (задача 7).
Пофайлово: `test_parse_contractor_row` 17 → 26, `test_get_proposals` 21 → 23,
`test_estimate` 50 → 61, `test_estimate_import` 59 → 66,
`test_schema_constraints` 87 → 90. Новый `test_vat_rate` — 44.

`just ci` целиком, шагами и в форме CI — `EXIT=0`: `uv lock --check`, ruff («All
checks passed»), `db-test-check` с `alembic check` («No new upgrade operations
detected»), backend pytest **1268 passed / 6 skipped**, eslint, `tsc -b
--noEmit`, vitest **175** (18 файлов) — фронтенд не менялся, и число совпадает с
базой. Шесть пропусков — те же известные из `test_auth_coverage.py`, других нет;
`1274 − 6 = 1268` сходится. Прогнан дважды: до доработки по финальному ревью
(тогда `1262 passed` из 1268 собранных) и после неё.

Круговой рейс `alembic downgrade base && upgrade head` проверен **на тестовой
базе**; `alembic check` — «No new upgrade operations detected». На стенде рейс
намеренно не гонялся: `AGENTS.md` §11, откат ниже 0003 там упрётся в
`ProgramLimitExceeded`, и это не дефект Ф4б.

### 2.2. Fixture

Правка — две текстовые ячейки. `max_row` 2602 и число объединений 2615 **не
изменились**, `K9:N9` и `O9:R9` целы, длины меток 26/25 → 44/43 — ровно те же,
что у 159-ТУ и 42-ТУ. Числа Ф3 воспроизвелись: `2576 / 746 / 222 / 485 / 39 /
38 / 16` и `EXPECTED_PRICED_ROWS`; числа Ф4a — три записи
`proposal_summary_lines` и тождество сложения по четырём колонкам.

Четыре отношения `vat / excluding × 100`, посчитанные **в тесте, из значений
листа** (не у парсера):

| колонка | отношение |
|---|---|
| материалы | `20.00000000007693` |
| СМР | `20.00000000004486` |
| косвенные | `20.00000000019817` |
| итого | `20.00000000005138` |

Максимум `1.98·10⁻¹⁰` п.п. от двадцати — тот же порядок, что назвала спека
§1.3, и на восемь порядков ниже допуска `0.01`.

### 2.3. Реальные оферты

Разбор продакшен-парсером (`parse_estimate`), не через тесты:

| Оферта | `parser_version` | `vat_rate` | Предупреждений |
|---|---|---|---|
| 159-ТУ | 3.1.0 | `"20"` | 0 |
| 42-ТУ | 3.1.0 | `"20"` | 0 |
| 449-ТУ | 3.1.0 | `null` | **2** |

У 449-ТУ ровно два: «В строке "В том числе НДС" … суммы не указаны» (Ф4a) и
«Ставка НДС не получена из шапки ценового блока …» (Ф4б). **«Сверка не
проведена» среди них нет** — некаскадирование работает. Но доказательством
некаскадирования сам этот замер не служит: у 449-ТУ ставки нет И в блоке итогов
нет ключа `total_cost_excluding_vat`, то есть ветка прикрыта дважды (спека
§2.7). Доказывает синтетический вход — негативная проверка 11.

Два прочих файла `samples/` (шаблон классификатора и сводный анализ) сметами не
являются и отвергаются `EstimateParseError` так же, как до фичи.

### 2.4. Стенд `gca_dev`

Переведён **точечным удалением трёх смет и повторной загрузкой** — это выбранный
способ, а не обход: `replace=true` для неизменённого файла не работает, правило 1
`AGENTS.md` §5 проверяется раньше правила 2 (грабли Ф4a §5.3). Стенд не чистился
и не пересоздавался, схема доведена до `0008` **только `upgrade`** — кругового
рейса на стенде не было (`AGENTS.md` §11).

| Договор | `parser_version` | `vat_rate` | `proposal_summary_lines` | позиций |
|---|---|---|---|---|
| ДГП-159-TU | 3.1.0 | **20** | 3 | 1512 |
| ДГП-42-TU | 3.1.0 | **20** | 3 | 1377 |
| ДГП-449-TU | 3.1.0 | **NULL** | 2 | 2576 |

В сыром JSON — `"20"` / `"20"` / `null`, то есть строка доезжает до `raw_data` и
конвертируется в колонку, а не появляется из воздуха на слое импорта.

Счётчики до удаления и после загрузки **совпали**: договоров 3, объектов 3,
подрядчиков 3, каталожных строк 1932, смет 3, `position_items` 5465
(= 1512 + 1377 + 2576). Каталог не вырос ни на строку — новые импорты нашли
существующие `TO_REVIEW`-строки по той же нормализованной паре. `import_jobs`
6 → 9: старые сохранены, это аудит (`AGENTS.md` §5 правило 3).

Предупреждения на стенде: у 159-ТУ и 42-ТУ о ставке — **ни одного**; у 449-ТУ
ровно два, «суммы НДС не указаны» (Ф4a) и «ставка НДС не получена из шапки»
(Ф4б), **ни одного** «сверка не проведена». Прочие предупреждения этих jobs —
доменные (расхождение шапки с карточкой договора, названия статей, длинные
наименования) и к Ф4б отношения не имеют: они были и до фичи.

449-ТУ прикрыт веткой некаскадирования дважды, поэтому доказательством служит не
стенд, а негативная проверка 11 (§3).

## 3. Негативные проверки: двенадцать снятий

Протокол [verifying-guards.md](../insights/verifying-guards.md) целиком.
**Контрольный прогон до всякого снятия: `tests/unit/parser` — 398 passed, 0
failed** (слой 3: раннер действительно стартовал). По каждому снятию —
`assert old in text` с проверкой единственности вхождения, sha256 до и после
(`changed=True` во всех: ни один патч не «не применился» молча, слой 2), прогон
с записью числа упавших, восстановление **из побайтовой копии** (не
`git checkout --`: он даёт CRLF и ложную тревогу) со сверкой sha256.
`restored_byte_exact=True` во всех двенадцати, итоговый файл побайтово равен
исходному. **Делал оркестратор лично.**

| № | Что снято | Красных |
|---|---|---|
| 1 | якорь `$` в правиле распознавания | 1 |
| 2 | привязка к словам «с учетом ндс» | 1 |
| 1+2 | обе сразу (пара слоя 8) | 2 |
| 3 | проверка диапазона `0..100` | 2 |
| 4 | требование согласия двух шапок | 4 |
| 5 | предупреждение «ставка не получена» | 10 |
| 6 | предупреждение «шапки заявляют разные ставки» | 1 |
| 7 | предупреждение «расхождение с блоком итогов» | 2 |
| 8 | предупреждение «сверка не проведена» | 2 |
| 9 | фильтр годности (`to_decimal`) в сверке | 5 |
| 10 | ветка «база ноль при ненулевом НДС» | 1 |
| 11 | некаскадирование | 2 |

Снятия, не уронившего ничего, нет ни одного — мёртвых защит не найдено.

### 3.0. Ещё пять снятий — по находкам финального ревью ветки (§3a)

Контрольные прогоны до снятия: `tests/unit/parser` 400, `test_estimate_import`
66, `test_schema_constraints` 90 — все зелёные.

| № | Что снято | Красных |
|---|---|---|
| A | `build_vat_rate`: `is None` → truthiness | 2 |
| B | `get_proposals`: `is not None` → truthiness | 1 |
| C | `_vat_rate`, **вход**: `value is None` → truthiness | **0** |
| C2 | `_vat_rate`, **выход**: `return rate` → `return rate or None` | 1 |
| D | `CHECK`: арм `>= 0` → `> 0` (модель и миграция вместе) | 1 |

**Проверка C не уронила ничего, и это не «защиты нет».** По слою 7 такой
результат обязывает спросить, существует ли защита; ответ оказался третьим:
регресс, который проба моделировала, в этом месте **невозможен по типу данных**
— `value` приходит в `_vat_rate` строкой, а `not "0"` ложно. Модель дефекта была
неверна, а не защита мертва. Заменив модель на реалистичную (`return rate or
None` на выходе, где `rate` уже `Decimal`), получили красный. Записано обе
пробы, потому что вывод «здесь нечего ломать» — тоже замер.

Отдельно: проба C с первого раза дала `occurrences=0` и была **пропущена
пробником, а не объявлена успешной**. Причина — файл в рабочем дереве лежит с
CRLF, а шаблон был записан с LF. Это ровно слой 2: не применившийся патч
выглядит как доказанная защита, и спасает только проверка единственности
вхождения.

### 3.1. Слой 8: что ещё стоит на пути дефекта

Спека прямо назвала пункты 1 и 2 кандидатами в пару — снятие якоря могло бы
маскироваться привязкой к словам и наоборот. **Замерено, а не объявлено:**
снятие якоря роняет 1 тест, снятие привязки — 1, снятие обеих — ровно 2. Суммы
сходятся, множества упавших не пересекаются, то есть защиты **независимы**: у
каждой свой дефект, и ни одна не прикрывает снятую. Это отличается от находки
Ф4a §4.1, где две защиты оказались взаимно маскирующими и исходный дефект
воспроизводился только снятием обеих.

Проверка 9 сформулирована честно: снят не `to_decimal` (он общий с Ф4a, и его
снятие проверяло бы чужую защиту), а **ветвление по признаку негодности** внутри
сверки. Результат — негодное значение перестаёт быть **названным** в
предупреждении, уходя в соседнюю ветку «нет значения». Пять красных — это
параметризованный тест по `'n/a'`, `'NaN'`, `'sNaN'`, `'Infinity'` и пара
противоположных бесконечностей.

Проверка 11 — единственная, где «снятие» есть добавление: некаскадирование
ломается не удалением кода, а тем, что при неполученной ставке к
предупреждениям добавляется второе, «сверка не проведена». Это в точности тот
дефект, от которого защищает §2.7, и вход к нему — полный годный трёхстрочный
блок итогов при молчащих шапках, то есть форма fixture **до** правки §2.10.

## 4. Соответствие «требование спеки → тест»

Построено по тексту §2 требование за требованием
([replaying-new-rules.md](../insights/replaying-new-rules.md), слой 3).

| Требование | Исполнитель |
|---|---|
| §2.1 ключ на уровне подрядчика, не внутри `contractor_items` | `test_vat_rate_sits_next_to_width_not_inside_items`, `test_fixture_raw_vat_rate_is_a_decimal_string_on_the_contractor_level` |
| §2.1 ключ присутствует ВСЕГДА, в том числе `None` | `test_vat_rate_key_is_always_present_even_as_none` |
| §2.1 значение — десятичная строка в п.п. | `test_fixture_raw_vat_rate_is_a_decimal_string_on_the_contractor_level` (проверяет `isinstance(..., str)`) |
| §2.1 версия минорная, причина — добавленный ключ | `test_parser_version_is_minor_because_a_key_was_added_and_nothing_disappeared` |
| §2.2 нормализация (NBSP, двойной пробел, регистр) | `test_nbsp_and_double_space_and_other_case_do_not_prevent_recognition` (предпосылка замерена внутри теста) |
| §2.2 головная часть метки не участвует | `test_both_group_labels_with_suffix_are_recognized` (две разные головные части) |
| §2.2 якорь на конец строки | `test_trailing_text_after_percent_is_not_recognized`; снятие №1 |
| §2.2 процент вне конструкции не ставка | `test_arbitrary_percent_outside_the_construction_is_not_recognized`; снятие №2 |
| §2.2 диапазон `0..100` | `test_rate_above_range_is_rejected_and_problem_carries_the_actual_number`; снятие №3 |
| §2.2 запятая и точка равнозначны | `test_comma_and_dot_decimal_separator_give_the_same_value` |
| §2.2 «без НДС» → `null`, а не ноль | `test_without_vat_wording_gives_none_not_zero` |
| §2.2 заявленный `0%` отличим от отсутствия | `test_zero_percent_is_a_declared_zero_not_an_absence` (метка), `test_declared_zero_survives_agreement_and_check` (ядро), `test_declared_zero_rate_reaches_parse_result_as_zero_not_as_absence` (полный путь парсера), `test_declared_zero_rate_is_stored_as_zero_not_as_null` (колонка), `test_declared_zero_is_a_representable_state` (схема); снятия A, B, C2, D |
| §2.3 смещения из единой функции раскладки | `test_money_group_offsets_match_measured_layout`, `test_vat_rate_found_at_measured_offsets_for_every_supported_width`, обратная проверка `test_vat_rate_not_found_when_suffix_uses_wrong_offset_formula` |
| §2.3 подъём `get_column_keys` не меняет разбор | `test_column_lift_keeps_the_positional_layout` (эталон литералами, все четыре ширины) |
| §2.3 неподдерживаемая ширина → `ValueError` | `test_money_group_offsets_rejects_unsupported_colspan` |
| §2.3 таблица исходов согласия двух шапок | `TestResolveDeclaredRate` целиком, включая ДВА отдельных теста на «дала только одна»; снятия №4, №5, №6 |
| §2.4 `vat_rate` получает только заявленное | `test_fixture_database_vat_rate_is_a_decimal_twenty`: в колонке ровно `Decimal("20")`, тогда как деление даёт `20.0000000000769` — будь источником деление, значение было бы другим |
| §2.4 сверка по всем четырём колонкам | `test_fixture_summary_block_vat_ratio_is_within_tolerance` (параметризован по четырём), `test_deviation_beyond_tolerance_names_the_column_and_carries_three_numbers` |
| §2.4 локальный контекст, глобальный не тронут | `test_global_decimal_context_is_left_untouched`, `test_division_overflow_is_unverified_and_does_not_raise` |
| §2.5 допуск с обеих сторон границы | `test_deviation_exactly_at_the_tolerance_boundary_is_not_a_mismatch` + `test_deviation_just_beyond_the_tolerance_boundary_is_a_mismatch` |
| §2.6 нет обоих ключей → сверки нет и предупреждения нет | `test_missing_excluding_vat_key_means_no_check_at_all`, `test_missing_both_keys_means_no_check_at_all`, `test_two_row_summary_block_declares_a_rate_but_gives_no_warning_at_all` |
| §2.6 оба пусты → тишина | `test_both_values_in_a_column_blank_is_silent` |
| §2.6 `0 / 0` → тишина | `test_zero_over_zero_is_silent` |
| §2.6 база `0` при ненулевом НДС → расхождение | `test_zero_base_with_nonzero_vat_is_a_mismatch`; снятие №10 |
| §2.6 часть пары → «не проведена» | `test_only_one_side_of_the_pair_is_filled_is_unverified` |
| §2.6 негодное значение названо и не роняет разбор | `test_unusable_value_is_unverified_with_the_actual_value_and_does_not_raise`, `test_opposite_infinities_in_the_pair_are_unverified_and_do_not_raise`; снятие №9 |
| §2.6 пустая строка — пустота, не негодность | `test_empty_string_is_blank_not_unusable` |
| §2.6 `DecimalException` при делении → «не проведена» | `test_division_overflow_is_unverified_and_does_not_raise` |
| §2.7 четыре предупреждения, каждое своё | снятия №5–№8; `test_mismatch_and_unverified_are_two_separate_warnings` |
| §2.7 предупреждения не каскадируют | `test_rate_not_obtained_short_circuits_the_check_even_with_a_full_valid_summary`; снятие №11 |
| §2.7 все четыре — parser warnings (сессия A) | `test_vat_rate_warning_reaches_parse_result_warnings` |
| §2.7 агрегирование с усечением | **граница**, см. §6 п.1 |
| §2.8 тексты не различаются между лотами | `test_vat_rate_warning_is_not_doubled_on_a_two_lot_sheet` |
| §2.8 номер строки шапки проводится, не ищется второй раз | `test_vat_rate_survives_a_header_row_shift` (константа 9 не зашита) |
| §2.9 колонка и `CHECK` (оба арма выражения) | `test_value_over_the_range_is_rejected_past_the_import` (`101`), `test_value_below_the_range_is_rejected_past_the_import` (`-1`), `test_declared_zero_is_a_representable_state`; снятие D |
| §2.9 диапазон конверсии при импорте с обеих сторон | `test_out_of_range_rate_becomes_null_with_a_value_problem` (`"120"`), `test_negative_rate_becomes_null_with_a_value_problem` (`"-5"`) |
| §2.9 конверсия НЕ `_money` | `test_nan_rate_becomes_null_with_a_value_problem_not_an_integrity_error`, `test_out_of_range_rate_becomes_null_with_a_value_problem` |
| §2.9 payload без ключа → `NULL`, гейта нет | `test_payload_without_the_key_imports_with_null_and_no_gate` |
| §2.10 fixture заявляет ставку в обеих шапках | `test_fixture_column_headers_declare_the_vat_rate` (читает лист напрямую) |
| §2.10 числа Ф3 не поехали | существующие тесты `test_import_fixture_e2e.py`, воспроизведены в том же прогоне |
| §2.11 backfill не делается, стенд переводится удалением | **граница**, см. §6 п.2 и §5 п.3 |
| §2.12 ручной правки ставки нет | **граница**, см. §6 п.3 |

## 4a. Финальное ревью ветки: что оно нашло

Ревью (Fable) вынесло **APPROVE** — дефектов корректности не нашло ни одного, —
но указало на три пробела в силе тестов, и два из них подтвердились замером.
Все закрыты этой же веткой, до внешнего круга.

1. **Заявленный ноль нигде не проходил дальше первого слоя.** Он проверялся
   только в `read_label_rate`; весь путь наверх держался на том, что и ядро, и
   `get_proposals`, и конверсия импорта сверяют `is None`, а не truthiness.
   Регресс вида `if not declared.rate` прошёл бы **весь набор зелёным**, молча
   превратив единственный законный ноль в `NULL`, — то есть неразличение «ноль»
   и «не заявлено», от которого §2.2 отказалась явно. Замерено снятиями A, B,
   C2, D: четыре разных места пути, и каждое теперь стережётся.
2. **Нижняя граница диапазона не проверялась нигде** — ни в `CHECK` (тест бил
   только `101`), ни в конверсии импорта (только `"120"`). Арм `>= 0` можно было
   выкинуть, не уронив ни одного теста; при этом отрицательное значение,
   дошедшее до вставки, роняет `IntegrityError` **всю смету** — тот же класс,
   против которого написан тест `NaN`.
3. **Замечание о недостижимой ветке `rate < VAT_RATE_MIN`** в `read_label_rate`
   — принято и названо границей (§6 п.13), кода не меняли: `\d+` отрицательных
   не даёт, ветка стоит как страховка на случай правки регулярного выражения.

Ещё два замечания приняты и закрыты мелкими правками: тест `Overflow` теперь
утверждает, что текст несёт **фактическое значение** (§2.6 этого требует, а
проверялись только счётчики), а требование §2.8 «номер строки шапки не ищется
второй раз» переведено из строки соответствия в **границу** (§6 п.14) — тест
`test_vat_rate_survives_a_header_row_shift` доказывает, что константа 9 не
зашита, но реализация, ищущая шапку второй раз **правильно**, поведенчески
неотличима и этот тест прошла бы.

Замечание к спеке (информационное, кода не требует): правило §2.2 привязано к
написанию «с учетом», и вариант «с учётом НДС 20%» через «ё» даст штатный `NULL`
с предупреждением. Это согласуется с решением спеки «новый вариант метки сначала
замеряется и лишь потом расширяет правило», но «ё» — самый вероятный первый
реальный вариант; занесено в границы (§6 п.15), чтобы при первом же таком файле
причина была известна заранее.

## 5. Отступления от плана

1. **Фикстуры `test_get_proposals` переведены на поддерживаемые ширины блока** —
   решение оркестратора. План называл шаг 4 задачи 5 «механической правкой 21
   вызова», но замер до кода вскрыл, что все фикстуры этого файла задают
   `colspan` 1 и 3, тогда как `money_group_offsets` знает только 8, 9, 10 и 11.
   После подключения раскладки файл покраснел бы целиком по `ValueError`.
   Проверено фактом, а не рассуждением: `_validate_contractor_blocks` отвергает
   любую другую ширину `EstimateParseError` и вызывается **до**
   `read_lots_and_boundaries`, то есть в продакшене `get_proposals` такой ширины
   не видит никогда. Значит защитная ветка вокруг раскладки была бы мёртвым
   путём ([verifying-guards](../insights/verifying-guards.md), слой 7), а сами
   фикстуры стояли на невозможном входе. Ширины стали 11 и 10, ожидания
   остались конкретными числами (`== 11`, `== 10`), не «любым числом».
   Это ровно тот случай, ради которого заведён слой 2
   [replaying-new-rules](../insights/replaying-new-rules.md): правило судит уже
   существующие входы, и спросить об этом надо было до кода — что и было сделано.
2. **Синтетический лист `test_performance` получил суффикс ставки** —
   коллатеральное следствие того же подключения: лист не заявлял ставку, и его
   утверждение `warnings == []` покраснело. Лист приведён к форме реальных
   файлов — то же решение, что §2.10 приняла для fixture. Ожидание не ослаблено.
3. **Перевод стенда потребовал отдельного разрешения пользователя.** Операции с
   живой базой — `alembic upgrade head` на `gca_dev` и удаление трёх смет — были
   заблокированы политикой доступа среды. Обходить запрет оркестратор не стал:
   работа была доведена до конца во всём остальном, а стенд вынесен вопросом.
   Пользователь разрешил («приложение в разработке, ценных данных нет»), после
   чего шаг выполнен полностью (§2.4). Порядок оказался полезен сам по себе:
   состояние стенда было замерено **до** всяких действий, поэтому сверка «до и
   после» опирается на числа, а не на память.
4. **Два вакуозных теста удалены на ревью задачи 3** — `isinstance(...,
   LabelReading)` и `isinstance(..., DeclaredRate)`. Такой тест не может упасть
   в принципе, а значит относится к тому же классу, что мёртвая защита
   ([false-test-premises](../insights/false-test-premises.md), пункт 4).
5. **Проверка некаскадирования усилена до полного трёхстрочного блока**
   (коммит `61ac044`): сверке хватило бы и двух строк, но смысл требования §2.7
   в том, что сверка не запускается, **хотя могла бы**, и на полной тройке это
   видно прямо во входе. Снятие №11 замерено уже после этой правки.
6. **Число негативных проверок — двенадцать, а не одиннадцать.** Двенадцатая —
   комбинированное снятие 1+2, заведённое требованием слоя 8: без неё вывод
   «защиты независимы» был бы заявлением, а не замером.

## 6. Названные границы

Требование без исполнителя объявляется границей, а не замалчивается.

1. **Усечение перечня в предупреждениях Ф4б недостижимо.** Оба перечня строятся
   по денежным колонкам, их четыре, а `MAX_SUMMARY_WARNING_EXAMPLES` равен пяти
   — то есть `_examples` в Ф4б никогда не усекает, и теста на усечение быть не
   может: у него нет входа. Механизм переиспользован ради единообразия текста и
   на случай, если перечень когда-нибудь станет длиннее.
2. **Отсутствие backfill тестом не стережётся** (спека §5 п.7): отсутствие кода
   тестом не проверяется. Та же граница, что у Ф4a §5.6.
3. **Отсутствие ручной правки ставки тестом не стережётся** — по той же причине
   (спека §2.12, §5 п.7).
4. **Ширины блока 8, 9 и 10 в корпусе не наблюдались** (спека §5 п.3). Раскладка
   для них выводится из существующего `get_column_keys` и проверяется только
   синтетикой. Про существование таких файлов Ф4б не утверждает ничего.
5. **Формы «шапки заявляют разное», «значение вне диапазона», «база ноль при
   ненулевом НДС», «приписка после процента» в корпусе отсутствуют** и
   проверяются только синтетикой (спека §5 п.2). Корпус умеет соглашаться с
   неверным правилом.
6. **Ставка над `unit_cost` проверяема только косвенно** (спека §5 п.1):
   итоговых строк по `unit_cost` в файле нет, поэтому лгущий суффикс над этой
   группой поймался бы лишь расхождением с соседней шапкой.
7. **Дедупликация предупреждений идёт по точному тексту** (граница 9 Ф4a в
   силе): на многоподрядчиковом листе два подрядчика с одинаковой проблемой
   дадут одно сообщение. Вход вне корпуса смет ГП и косметический.
8. **Дефект `_money` с `NaN`/`Infinity` не чинится** (граница 7 Ф4a в силе).
   Ф4б его не расширяет — ставка конвертируется своей проверкой, — но и
   остальные денежные поля не лечит.
9. **Граница 8 Ф4a остаётся:** пустая строка внутри блока итогов обрывает обход
   молча. Семантику терминатора Ф4б не трогала.
10. **Guard `contractor_col_start is not None` в `get_proposals` продакшеном не
    достижим:** `read_contractors` ставит `column_start` всегда. Он повторяет уже
    существующий рядом guard чтения ИНН/адреса/аккредитации и стережёт только
    заведомо неполный синтетический вход. Названо, потому что по слою 7 такая
    ветка — кандидат в мёртвые.
11. **Ставка не проверяется против режима налогообложения** (спека §5 п.10):
    `ОСН` в метке читается, но не используется.
12. **Прогон правила сделан по трём офертам одного источника и по fixture,
    сделанному из одной из них** (спека §5 п.4). О четвёртой оферте он не
    говорит ничего; вариативность шапки остаётся главным непокрытым риском
    парсера.
13. **Ветка `rate < VAT_RATE_MIN` в `read_label_rate` продакшеном не
    достижима:** `\d+` отрицательных чисел не берёт, то есть отрицательная
    ставка до проверки диапазона не доходит. Ветка оставлена как страховка на
    случай правки регулярного выражения и потому названа, а не удалена и не
    объявлена проверенной. Названо по находке финального ревью ветки.
14. **Требование §2.8 «номер строки шапки не ищется второй раз» непроверяемо по
    построению.** `test_vat_rate_survives_a_header_row_shift` доказывает только,
    что константа 9 не зашита; реализация, которая ищет шапку второй раз и
    находит ту же самую, поведенчески неотличима и этот тест прошла бы. «Одна
    правда о том, где шапка» держится сигнатурой — обязательным `header_row` без
    умолчания, — а не тестом. Названо по находке финального ревью ветки.
15. **Правило привязано к написанию «с учетом», через «е».** Метка «с учётом НДС
    20%» через «ё» ставки не даст — получится штатный `NULL` с предупреждением.
    Форма в корпусе не встречается, и решение спеки §2.2 требует сначала замерить
    новый вариант метки, а уже потом расширять правило. Названо, чтобы при первом
    же таком файле причина была известна заранее.

## 7. Найденные грабли

1. **Новый обязательный аргумент судит тестовые фикстуры раньше, чем
   продакшен-входы.** Фикстуры `test_get_proposals` пережили три фазы со
   значением `colspan`, которое продакшен отвергает на входе в парсер, — и
   заметить это можно было только прогоном нового правила по уже существующим
   входам **до** написания кода. Замер занял одну команду; не сделай его,
   исполнитель получил бы 21 красный тест «по чужой причине» посреди задачи и
   почти наверняка обернул бы раскладку в `try/except`, заведя мёртвый путь.
2. **PowerShell 5.1 не понимает heredoc, а `$(...)` спотыкается о скобки внутри
   regex.** `git commit -F - <<'EOF'` даёт ParserError, а
   `"...=$((rg -n 'x\(' … | Measure-Object).Count)"` — «missing closing ')'».
   Рабочие формы: here-string `@'…'@` с закрывающей меткой в нулевой колонке и
   присваивание результата в переменную до подстановки в строку.
3. **`pg_ctl start` под `Start-Process -Wait` не возвращает управление**, хотя
   сервер поднимается. Статус надо спрашивать отдельной командой `pg_ctl status`,
   а не ждать завершения запускающего процесса.

## 7a. Что ушло в инсайты

Новых файлов в `docs/insights/` не заведено — оба случая легли в существующие,
и это осознанный выбор: §12 `AGENTS.md` держится на том, что каждая строка
оплачена, а документ читается целиком в начале каждой сессии.

1. **Маскировка падения в PowerShell `try/finally`** — в
   [silent-test-runs.md](../insights/silent-test-runs.md). Класс тот же, что у
   всего файла: успех читается по напечатанным числам, а не по коду возврата;
   здесь маскировка живёт не в раннере, а в конструкции оболочки. Туда же — вторая
   сторона той же монеты: `rg` без совпадений возвращает `1` и портит вердикт
   блока, где всё верно. Выстрадано кругом гейта 3 этой фичи.
2. **Уточнение к слою 2 [replaying-new-rules.md](../insights/replaying-new-rules.md)**
   — «только добавляет поле» надо проверять по тому, что поле **тянет за собой**.
   Ф4б выглядела безобидным добавлением ключа, но вычисление за ним имело область
   определения, и уже существующие фикстуры в неё не попадали (§5 п.1, §7 п.1).
   Правило при этом сработало: прогон был сделан до кода. Новый инсайт поэтому не
   заводится — уточняется триггер существующего.

## 8. Хвосты

- **Принудительный репарсинг неизменённого файла** — по-прежнему не чинится и не
  планируется (решение пользователя, спека §2.11). Хвост devlog Ф4a §8 остаётся
  открытым сознательно.
- **Дефект `_money` с `NaN`/`Infinity`** на прочих денежных входах — хвост Ф4, в
  силе.
