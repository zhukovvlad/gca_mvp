# Проект: База расценок генподряда (MVP) — v6.14

> **v6.14 (2026-08-28), по итогам просмотра фичи 3 на стенде. ИНВАРИАНТОВ НЕ
> ПРАВИТ — только §11.** Добавлены шесть граблей. Первая: выравнивание строки
> диаграммы двигает стек колонки целиком, а не столбик, и замер обязан сверять
> величину, которая КОДИРУЕТ ЧИСЛО, а не только тесноту подписей. Вторая: новый
> корень страницы обязан нести общий ограничитель ширины, и правка ширины
> обесценивает прежние замеры прокрутки — их повторяют целиком. Третья, по
> второму кругу замечаний: «прокрутка внутри контейнера» не значит «таблицу
> видно» — мерить долю ширины у колонки подписи и достижимость правой колонки, а
> зажим ширины ставить только вместе с переносом. Шестая: согласованный макет
> сверяется с реализацией одним проходом и машиной, а не по замечанию за круг.
> Четвёртая и пятая, по третьему кругу: утверждение о макете проверяется командой по файлу макета, а не по
> памяти; выравнивание не сходится, пока у ячеек строки разная коробка. Порядок строк
> свода при этом сменён с сортировки по вкладу на порядок классификатора — это
> решение живёт в [спеке](docs/superpowers/specs/2026-08-27-stage-summary-design.md)
> §2.13 и инвариантом проекта не является: §7 п.8 и §10 порядка строк не
> называют. Замеры — [devlog](docs/devlog/2026-08-27-stage-summary.md) §9.
>
> **v6.13 (2026-08-28), по итогам реализации фичи 3 «Свод по этапам».
> ИНВАРИАНТОВ НЕ ПРАВИТ — только §11 и §12.** Свод реализован ровно в том виде,
> в каком его объявил инвариант v6.12, и пересматривать в §10 нечего.
>
> В §11 добавлена грабля замера тесноты в браузере: мерить надо ВСЕ элементы
> ряда, а не тот, о котором сообщили, и в обе стороны — «не налезает на соседа»
> и «не выходит за свою колонку». В §12 заведён инсайт о том, что инвариант
> формулируется ЭКВИВАЛЕНТНОСТЬЮ, а не половиной, и к правилу о снятии защиты
> добавлен тринадцатый слой — про ложный зелёный у самого снятия. Обоснование и
> замеры — [devlog](docs/devlog/2026-08-27-stage-summary.md).
>
> **v6.12 (2026-08-27), гейт 1 фичи 3 «Свод по этапам, по статьям». В §10 ДОБАВЛЕН
> ИНВАРИАНТ — это не уточнение буквы.** У денежных поверхностей появляется третий
> класс рядом с одно- и много-договорными: **одно-участниковый свод по этапам
> тендера**. Ось выбирается по множеству ИЗВЕСТНЫХ ставок среди ВЫБРАННЫХ колонок:
> одна известная — известные колонки валовые; несколько — известные колонки целиком
> нетто с подписью причины; ни одной — сопоставимых итогов нет. Колонка с
> неизвестной базой в любом случае показывает состояния ячеек, но не суммы.
> Ценовой уровень — номинальный и объявлен. **Существующие оси одно- и много-договорных
> поверхностей не пересматриваются** — новый класс добавлен рядом, ни одно из
> прежних правил §10 не изменено.
>
> Вторая правка — исправление ПЕРЕСКАЗА, а не решения: §7 п.8 называл фичей 3
> «сравнение участников между собой», тогда как рамка тендерного контура §4
> определяет фичу 3 как свод по этапам, а §5.5 — сравнение участников как
> неспроектированное. Склейка возникла в §4 спеки фичи 2 и при ревизии v6.11
> уехала сюда; обе строки приведены к рамке. Обоснование, отвергнутые
> альтернативы и границы — [спека](docs/superpowers/specs/2026-08-27-stage-summary-design.md).
>
> **v6.11 (2026-08-21), по итогам фичи диаграммы стоимости договоров и фильтра по
> классу объекта. ОСОЗНАННОЙ РЕВИЗИИ ИНВАРИАНТА ЗДЕСЬ НЕТ**, и это надо сказать
> прямо, потому что ревизия в фиче есть — но уровнем ниже: она правит §2.6 спеки
> сравнения, а не этот документ. Класс объекта перестаёт быть ФОРМОЙ выборки и
> становится её СУЖЕНИЕМ: принимает список id, применяется к обеим формам и
> законно сочетается с `ids`. Пересматривать в §10 нечего — ось сравнения
> осталась нетто, отклонения по-прежнему от неё не зависят.
>
> Рядом с ревизией идёт **отдельное изменение фактического поведения**, и оно
> тоже не инвариант: `ids` вместе с `q`, `object_id` либо `contractor_id`
> отвечает теперь **400**, а не 200. Прежде эти параметры при `ids` МОЛЧА
> игнорировались — ссылка выглядела отфильтрованной, а ответ приходил по полному
> перечислению. Названо отдельно намеренно: это единственное место фичи, где
> меняется код ответа, и откатываться оно должно независимо.
>
> Одно следствие стоит знать, хотя решать его инвариантам не пришлось: **медиана
> выборки впервые показана ЧИСЛОМ на поверхности.** До сих пор она участвовала
> счётчиком «меньше трёх сопоставимых» и серверной окраской отклонений. Линия
> медианы рисуется только там, где ось столбцов однозначна — в «Без НДС» и
> «Единой»; в «Своей ставке» и на оси сумм её нет, и подпись называет причину. В
> таблицу колонка «Медиана» НЕ добавлена, и это решение, а не пропуск.
>
> Правки буквы при неизменных решениях: §7 п. 7 пополняется диаграммой итога и
> фильтром по классу (оба выражены в URL); §11 — граблями `Decimal.//`,
> подменой локальной установки инструментов случайным `npx`, постоянным
> требованием мерить совпадение подписей со столбцами в браузере, правилом о
> том, что имена переменных из макета не совпадают с токенами приложения,
> специфичностью `dark:`-утилит shadcn поверх токен-классов, поведением
> `activeBar` у recharts, требованием кончать прогон снимком ВСЕГО экрана в
> обеих темах И в состояниях взаимодействия, и двумя правилами о том, что
> поверхность не вправе утверждать больше, чем знает: пустое поле — отсутствие
> факта с несколькими причинами, а направление считается по величинам, не по
> знаку множителя; двумя по ВТОРОМУ кругу того же ревью — минус, приклеенный к
> строке, не является вычитанием, а тест величины, чей знак участвует в решении,
> обязан покрыть оба знака; и двумя по ТРЕТЬЕМУ — дефект рядом с правкой не
> помечать «дофичевым» по памяти, и булево на направление есть два состояния там,
> где достижимо три; и одной перед мержем — «не входило в список задачи» не есть
> ответ на расхождение с согласованным макетом (devlog §4.18–§4.21). Четырнадцать
> грабель сверх списка задачи:
> все из семейства уже стоящих там, и §11 читается в начале каждой сессии — это
> её место. Четыре последние заведены по находкам пользователя ПО СНИМКАМ, уже
> после объявленной приёмки: панель управления расходилась с тем же
> согласованным макетом на восьми пунктах оформления и порядка блоков
> (devlog §4.14, §4.15), у диаграммы не было подсказки и даты подписания, а
> столбец был вдвое уже макетного (§4.16), и `activeBar`, которым я повторил
> подсветку наведения из макета, необратимо съедал штриховку промежутка
> ([§4.17](docs/devlog/2026-08-19-comparison-cost-chart.md)).
> Обоснование, отвергнутые альтернативы и границы —
> [спека](docs/superpowers/specs/2026-08-19-comparison-cost-chart-design.md),
> замеры — [devlog](docs/devlog/2026-08-19-comparison-cost-chart.md).
>
> **v6.10 (2026-08-19), по итогам фичи поправки на инфляцию. В §10 ДОБАВЛЕН
> ИНВАРИАНТ — это не уточнение буквы.** У денежных величин появляется вторая ось:
> рядом с налоговым составом теперь стоит **ценовой уровень**, и вместе с ним —
> новая обязанность подписи. Много-договорные СРАВНИТЕЛЬНЫЕ поверхности вправе
> дополнительно приводиться к ценовому уровню выбранного месяца по именованному
> ряду индексов, и **ряд с месяцем объявляются на самой поверхности** — тем же
> правилом, каким объявляется налоговый состав.
>
> Действующие инварианты при этом НЕ пересматриваются, и осознанной ревизии здесь
> нет: ось сравнения остаётся нетто всегда, медиана и отклонения считаются по
> нетто в любом режиме показа и при любом приведении, отклонения от ставки показа
> не зависят. Порядок операций — **нетто → инфляция → ставка показа**, и он
> производен: ₽/м², медиана строки, отклонения и итоговая строка следуют из него, а
> площадь не индексируется.
>
> **По умолчанию приведение выключено, и без него приложение отвечает как до фичи,
> посимвольно** — тем же основанием, каким §10 требует этого от ручной ставки НДС.
> Приведение считает СЕРВЕР и **до всякой агрегации**: у клиента нет разбивки по
> сметам, медиана считается раньше показа, в JS нет `Decimal`, а вторая копия
> формулы разошлась бы с первой.
>
> Плюс две правки состава экранов при неизменных решениях: §7 п. 3 пополняется
> рядами индексов инфляции, §7 п. 7 — двумя параметрами в URL страницы сравнения.
> Обоснование, отвергнутые альтернативы и границы —
> [спека](docs/superpowers/specs/2026-08-18-inflation-adjustment-design.md),
> замеры — [devlog](docs/devlog/2026-08-18-inflation-adjustment.md).
>
> **v6.9 (2026-08-18), по итогам разбора «куда класть отложенные решения».**
> Решения по домену не менялись — правка только процессная: в таксономию §9.2
> добавлены два класса документов. Один из них, `docs/TECH_DEBT.md`, существовал
> с полутора десятками записей и не был описан здесь **вовсе**; второй,
> `docs/proposals/`, заводится этой ревизией под решения, проработанные до
> готовности и отложенные. Причина завести отдельный класс, а не пользоваться
> `specs/`: файл в `specs/` означает пройденный гейт 2, то есть «реализация
> следующая», и отложенное решение, положенное туда, однажды будет взято в работу
> без решения пользователя. Первое предложение —
> [следствия дельта-модели допсоглашений](docs/proposals/2026-08-18-amendments-delta-model.md).
>
> **v6.8 (2026-08-17), по итогам фичи сравнения договоров.** Одна осознанная
> ревизия инварианта: §10 требует, чтобы много-договорные РАСЧЁТНЫЕ
> поверхности показывались в нетто; страница сравнения показывает каждый
> договор в его собственной ставке по умолчанию. Ревизия допустима, потому что
> инвариант запрещает не сложение сумм разного налогового состава, а МОЛЧАЛИВОЕ
> сложение, — а главная (`/`) уже складывает валовые суммы разных ставок с
> обязательной подписью на самой поверхности. Ось сравнения остаётся нетто
> всегда, поэтому отклонения не зависят от режима показа. Рядом с ревизией —
> две правки буквы при неизменных решениях: §7 пополняется экраном сравнения
> (пункт 7), §7.6 переводит «два отчёта — два файла» в три. Обоснование и
> решения —
> [спека](docs/superpowers/specs/2026-08-17-contract-comparison-design.md),
> замеры — [devlog](docs/devlog/2026-08-17-contract-comparison.md).
>
> **v6.7 (2026-08-16), по итогам фичи каскадного удаления договора.** Одна
> осознанная ревизия: прежнее «задания импорта — аудит и не удаляются никогда»
> (§5 правило 3, §8) сужено до своей области. Аудит защищает от подмены сметы в
> живом договоре, а не от удаления самого договора владельцем данных: удаление
> снимает предмет вопроса «что лежало здесь раньше», и запись о загрузке к
> несуществующему договору — не аудит, а мусор с висящим внешним ключом. В §3
> право `admin` дополнено удалением договора вместе со сметами и аудитом.
> Обоснование, границы и отвергнутые альтернативы —
> [спека](docs/superpowers/specs/2026-08-16-contract-cascade-delete-design.md),
> замеры — [devlog](docs/devlog/2026-08-16-contract-cascade-delete.md).
>
> **v6.6 (2026-08-12), по итогам фичи ручного разноса разделов по статьям.**
> Решения по домену не менялись — добавлено право `member` (§3), состав паспорта
> дополнен признаком ручного разноса и печатной сноской (§7.4), в §10 добавлены
> два пункта DoD: разнос НЕ обещает нуля «Нераспределённого» глобально, три
> границы §5.5 остаются диагностируемыми, а на смете стенда ноль проверяется
> буквально. Отдельно уточнён §7.4 о печати: фраза «состав полный» описывала
> намерение фазы 7, но замером в браузере установлено, что печатный состав
> следует состоянию раскрытия дерева на экране — по умолчанию свёрнутого. Сама
> формулировка фазы 7 не переписана, к ней добавлено уточнение. Обоснование и
> замеры — `docs/devlog/2026-08-11-unallocated-override.md`.
>
> **v6.5 (2026-08-05), по итогам разбора процесса.** Решения по домену не
> менялись — переписан §9: единица работы теперь **фича** (спека → план →
> реализация, три гейта), фазы остаются вехами приёмки; добавлены таксономия
> документов, правила ветвления и PR, обязательный `just ci` перед пушем.
> Уточнена преамбула: этот документ — источник истины по инвариантам и процессу,
> дизайн конкретных фич живёт в спеках. Обоснование и замеры —
> `docs/superpowers/specs/2026-08-05-development-process-v2-design.md`.
>
> **v6.4 (2026-08-04), по итогам фазы 6.** Зафиксирован **согласованный с
> пользователем макет отчёта «для банка»** — §7.6 требовал именно согласования, и
> теперь оно получено, так что документ несёт его, а не отсылку к разговору. Плюс
> три уточнения буквы при неизменных решениях: §10 ссылался на несуществующий
> подраздел «§1.2» (поля перечислены в §1 пункт 2 и §7.4), в §7.4 добавлена верхняя
> граница N с причиной, и в §11 добавлены трое грабель, найденных прогоном стенда:
> печатная раскладка А4, зажим многокилобайтового наименования в ячейке и предел
> точности чисел в xlsx. Обоснование и замеры — `docs/phase6-analytics.md`.
>
> **v6.3 (2026-08-04), по итогам первой загрузки реальной сметы.** Одно уточнение
> §4 и одни новые грабли §11, согласованные с пользователем: уникальность
> каталожной строки объявлена по `sha256` нормализованного названия, а мёртвый
> обычный индекс по `standard_job_title` удалён (миграция 0003). Причина — предел
> btree в 2704 байта: в наименовании реальной сметы оказалась спецификация на 5077
> символов, и импорт падал целиком. Идентичность работы не менялась — это
> по-прежнему полная пара (нормализованное название, единица), нормализация и
> `norm_version` не тронуты.
>
> **v6.2 (2026-08-03), по итогам фазы 4 и её ревью.** Два уточнения §5,
> согласованные с пользователем: (1) предупреждения разделены между сессиями —
> доменные пишет сессия B вместе с данными, которые они описывают; (2) правило
> идемпотентности повторной загрузки привязано к **текущей** смете пары, а не к
> любому историческому `done`-job. Оба — уточнения буквы при неизменных решениях;
> обоснование и замеры — `docs/phase4-import.md` §4.1 и §6.1.
>
> **v6.1 (2026-08-02), по итогам фазы 0.** Решения не менялись — зафиксированы
> результаты проверок, которые сам документ и предписывал провести:
> §4 и §6 — закрыта развилка quantity vs suggested_quantity и исправлено название
> колонки («Предлагаемое», не «Предполагаемое»); §9 фаза 0 — отмечена выполненной
> условно (один образец вместо 2–3). Обоснование замеров — `docs/phase0-input-data.md`.

Инструкция для кодового агента. Прочитай целиком до начала работы. Архитектурные решения приняты — не пересматривай их молча. **Останавливайся и задавай вопрос**, если обнаружишь: фактическое противоречие с кодом исходных репозиториев, бизнес-неоднозначность, не покрытую этим документом, риск потери данных или необратимой миграции.

**Целевой репозиторий:** `github.com/zhukovvlad/gca_mvp` (создан пустым). Вся работа ведётся в нём. Этот документ — источник истины по **архитектурным инвариантам и процессу разработки**; дизайн конкретных фич живёт в спеках (§9.2). При противоречии спеки инварианту побеждает инвариант; сам инвариант меняется только осознанной ревизией этого документа. Положить в корень как `AGENTS.md` первым коммитом (его читает Codex), рядом создать `CLAUDE.md` из одной строки-указателя «Прочитай и выполняй AGENTS.md — это основная инструкция проекта» (его читает Claude Code). Полный текст держать в одном файле, не в двух, — дублирование разъедется. Поддерживать актуальным при согласованных изменениях решений.

---

## 1. Бизнес-задача

Свести расценки всех договоров генподряда (ГП) 2024–2027 в единую базу:

1. **Единая база расценок** — по объектам и крупным статьям, со сквозным сравнением одной и той же работы между объектами и годами.
2. **Паспорт объекта на одной странице** — договор, подписант, дата, ключевые расценки, отклонения. Печатная форма.
3. **Нормативы расценок по классам объектов** — превышения подсвечиваются автоматически; переутверждение нормативов с учётом инфляции, с историей по периодам действия.
4. **Применение** — предметный торг с подрядчиками, защита цифр перед банком и партнёрами (выгрузки в Excel).

Анализ текстов самих договоров — **вне скоупа MVP** (отдельный модуль позже).

## 2. Исходные материалы (ревизии зафиксированы)

| Репозиторий | Commit SHA | Что берём | Что НЕ берём |
|---|---|---|---|
| `github.com/zhukovvlad/udp-tenders` | `98fb67667c4483afd90816d303c263614fe01ed3` | **Основа (форк):** структура backend/frontend, auth (JWT, ротация refresh, CSRF), `UnitOfMeasure`+`UnitAlias`, паттерны CRUD/роутеров/тестов, страницы Review/ReferencePrices/Reports/Settings, `just`, Alembic | `llm*.py`, `pdf_parser.py`, `pdf_orientation.py`, OpenRouter; MinIO/`s3.py` (см. §8); **мультиарендность: `Organization`, `ProjectOrganization`, все `org_id`** (см. §3); доменные модели УПД (`Invoice`, `InvoiceItem`, `Document`, `Supplier`, `MaterialClass`, `CompensationCorridor` — как код удалить, как паттерн подсмотреть) |
| `github.com/zhukovvlad/tenders-go` | `121718bf45df8ea7fdc5e6346471b137d84ded88` | **Схема БД** из `cmd/internal/db/migration/` (000001 consolidated, 000002 FTS, 000007–000008 grouping): эквивалентный перенос полей, типов, CHECK-констрейнтов и индексов. Логика транзакции `ImportFullTender` — как референс для порта на Python | Go-код как таковой, RAG-эндпоинты, Google File Search, `suggested_merges`-воркфлоу, `users`/`user_sessions` (auth из udp-tenders) |
| `github.com/zhukovvlad/parser_tender_xlsx` | `0e178c097d8097bc0ea981e7b4f517518461f61e` | `app/excel_parser/` целиком, `app/constants.py` | `celery_app`, `go_module`, `gemini_module`, `rag_google_module`, `json_to_server`, `markdown_utils`, `markdown_to_chunks`, `prompts`, `main.py` |

«Эквивалентный перенос» схемы допускает: переименование `tenders` → `estimates`, слияние units с `UnitAlias`, добавление новых таблиц и колонок (в т.ч. `normalized_job_title`, §4). Не допускает: изменение типов денежных полей, ослабление CHECK-ограничений, потерю частичных индексов.

## 3. Ключевые решения (зафиксированы, не пересматривать)

- **Single-tenant.** Внутренний инструмент одной компании. `organizations` и все `org_id` выпиливаются. Роли: `admin` — управление пользователями, классами, нормативами, замена/удаление смет, удаление договора целиком — вместе со сметами, заданиями импорта и файлами (v6.7); тендеры, раунды, удаление участника тендера, замена раунда (тендерный контур, спека 2026-08-26); `member` — чтение, загрузка смет, ручной матчинг (Review), ручной разнос разделов сметы по статьям классификатора («Нераспределённое» в паспорте проекта; [спека разноса](docs/superpowers/specs/2026-08-11-unallocated-category-override-design.md) §2.7); загрузка файлов раундов тендера — и она **заводит подрядчиков** по ИНН из файла (спека контура §2.8).
- **Стек:** Python 3.12, FastAPI, SQLAlchemy 2.x sync ORM, Alembic, psycopg3; PostgreSQL 16 + pgvector (extension включить первой миграцией, векторный матчинг — фаза 2); React + TS, Vite, shadcn/ui, Tailwind, TanStack Query, TanStack Table, Recharts.
- **Без брокеров:** никаких Celery/Redis. Длинные операции — FastAPI `BackgroundTasks` + таблица `import_jobs`. **MVP запускается строго с одним worker-процессом uvicorn** — это условие корректности startup-recovery (§6); зафиксировать в конфиге запуска и README. Таймаут парсинга — **мягкий**: проверка прошедшего времени между этапами пайплайна, не жёсткое прерывание.
- **Деньги:** `numeric` в БД ↔ `Decimal` в Python ↔ строки в JSON. Никаких float. Пустая стоимость в смете → `NULL`, не `0`.
- **Источник истины при импорте — карточка договора.** `contract_id` передаётся в запросе; объект, подрядчик и реквизиты договора НЕ апсертятся из XLSX. Реквизиты из шапки файла сохраняются в raw_data; при расхождении с карточкой — записи в `import_jobs.warnings`.
- **Каталог работ общий.** Единица «идентичности» работы в каталоге — **нормализованное название + единица измерения** (§4).

## 4. Доменная модель

Ключевой факт: **смета к договору ГП структурно = тендерная таблица**, но с одним подрядчиком, без колонки «Расчётная стоимость» (baseline) и с заполненным `suggested_quantity`.

**Уточнено по итогам фазы 0** (замер — `docs/phase0-input-data.md`): в реальном файле колонка называется **«Предлагаемое количество»** (не «Предполагаемое», как в ранних редакциях этого документа) — это количество, предложенное подрядчиком. Рядом идёт «Общее кол-во» — объём заказчика, он ложится в `quantity`. Константы детекции шапки в парсере (фаза 3) писать по реальному заголовку «Предлагаемое количество», не по пересказу.

```
rate_classes                 # классы объектов (справочник)

objects                      # из tenders-go + rate_class_id bigint NULL → rate_classes
                             #   (значение по умолчанию для новых договоров)
contractors                  # из tenders-go

contracts                    # договор ГП
  id, object_id NOT NULL → objects
  contractor_id NOT NULL → contractors
  rate_class_id NOT NULL → rate_classes    # СНИМОК класса на момент создания договора;
                                           # переклассификация объекта не меняет прошлое
  contract_number, title, signer, signed_date date NOT NULL, total_amount numeric, notes
  # signed_date обязателен: это фолбэк даты сравнения с нормативом (§4), обе даты
  # не могут быть NULL одновременно
  UNIQUE (contract_number)

estimates                    # смета (бывш. tenders); 1 договор : N смет
  id, contract_id NULL → contracts   # с 0015 (тендерный контур) — один из трёх
                                     #   владельцев, см. блок ниже: contract_id | offer_id | round_id
  amendment_no int NULL      # NULL = исходная смета, иначе номер доп. соглашения; только у contract_id
  title, data_prepared_on_date date
  import_job_id → import_jobs
  UNIQUE NULLS NOT DISTINCT (contract_id, amendment_no)   # PG16

tenders / tender_rounds / offer_packages / offers    # тендерный контур (спека 2026-08-26)
  # offers — ячейка решётки «раунд × участник»; составные FK с продублированным
  # tender_id держат раунд и участника в одном тендере структурно.
estimates: contract_id NULL | offer_id NULL | round_id NULL — РОВНО ОДИН (CHECK);
  # round_id на смете = baseline раунда. uq_estimates_contract_amendment — частичный.
proposals.contractor_id NULL у baseline (CHECK против is_baseline).
position_items.deviation_from_baseline_cost: у смет договора NULL; у offer-смет — из файла (§2.10).
estimate_raw_data — ПРОЕКЦИЯ разобранного JSON под смету; точный результат разбора файла —
  import_jobs.parsed_data + parser_version (три факта об одном файле, спека §2.3).
contractors.inn — канон, только ASCII-цифры (CHECK); одна canonicalize_inn().

estimate_raw_data
  estimate_id PK → estimates ON DELETE CASCADE
  raw_data jsonb NOT NULL    # проекция parsed_data под смету; точный результат разбора —
                             #   import_jobs.parsed_data + parser_version (см. выше)
  parser_version text NOT NULL
  created_at

lots / proposals / position_items
  # перенос из tenders-go. proposals — РОВНО ОДНО на лот (единственный подрядчик);
  # слой сохраняем: JSON парсера ложится 1:1, на proposal висят summary_lines и
  # additional_info. is_baseline — с 0015 опорное поле (не задел): у baseline-
  # proposal true и contractor_id NULL, у offer-proposal — наоборот
  # (CHECK ck_proposals_baseline_contractor), и тот же флаг ветвит импорт раунда.
  # position_items: quantity, suggested_quantity,
  #   unit_cost_{materials,works,indirect_costs,total}, total_cost_{...},
  #   is_chapter, chapter_ref_in_proposal, catalog_position_id NULL, unit_id,
  #   комментарии. deviation_from_baseline_cost — NULL у смет договора; у offer-смет — из файла.
```

Каталожный контур:

```
catalog_positions
  standard_job_title text NOT NULL        # отображаемое название (человекочитаемое)
  normalized_job_title text NOT NULL      # НОВАЯ колонка: normalize(standard_job_title),
                                          # вычисляется в Python при insert/update
  unit_id NULL → units_of_measurement
  kind ('POSITION|HEADER|LOT_HEADER|TRASH|TO_REVIEW'), status,
  embedding vector(768) NULL, FTS по standard_job_title (из миграции 000002)
  # УНИКАЛЬНОСТЬ — по нормализованной паре (это и есть идентичность работы),
  # но в индексе от названия лежит sha256 (миграция 0003, уточнение v6.3):
  #   UNIQUE (sha256(replace(normalized_job_title,'\','\\')::bytea),
  #           COALESCE(unit_id, -1))                                -- raw SQL
  # Причина: btree не индексирует значения длиннее 2704 байт, а в наименование
  # сметы попадают спецификации на килобайты (реальный файл — 5077 символов).
  # Идентичность НЕ изменилась: сравнение работ идёт по полному тексту, каждая
  # выборка по хэшу дополнена проверкой самой пары, коллизия — явный отказ.
  # Обрезать название нельзя: две спецификации с общим началом — разные работы.
  # Обычного индекса по standard_job_title НЕТ: поиск в UI — ILIKE '%…%', его
  # btree не обслуживает (нужен был бы GIN pg_trgm, вне MVP).
  # Частичные индексы (WHERE kind = 'POSITION' и т.п.) — raw SQL через op.execute().
  # Матчинг и get-or-create TO_REVIEW работают по ТОЙ ЖЕ нормализованной паре,
  # что и matcher, — никаких вторых представлений строки.

matching_cache
  cache_key text PK           # sha256(norm_version || '|' || normalized_title || '|' || unit_norm)
                              # unit_norm = каноническое имя единицы после разрешения алиасов,
                              # '' если единицы нет. ЕДИНИЦА ОБЯЗАТЕЛЬНА В КЛЮЧЕ.
  norm_version smallint NOT NULL
  job_title_text text NOT NULL           # исходное название — нужно для перевыпуска ключей
  unit_text text                          # исходная единица — то же
  catalog_position_id NOT NULL → catalog_positions
  source text NOT NULL CHECK (source IN ('auto','manual'))
  expires_at timestamptz NULL
  # ПРАВИЛО TTL: source='auto' → expires_at = now()+30 дней, продлевается при hit;
  #             source='manual' (решение из Review) → expires_at = NULL, НЕ истекает.
  # Инкремент norm_version: миграция обязана перевыпустить ключи всех source='manual'
  # записей из (job_title_text, unit_text) новой нормализацией; 'auto' можно отбросить.

units_of_measurement          # слить с UnitOfMeasure + UnitAlias из udp-tenders;
                              # алиасы «м2»/«кв.м»/«м²» обязательны
```

Нормативы:

```
rate_standards
  id, catalog_position_id NOT NULL, rate_class_id NOT NULL
  standard_unit_rate numeric NOT NULL CHECK (standard_unit_rate > 0)
  valid_from date NOT NULL, valid_to date NULL      # период = [valid_from, valid_to)
  CHECK (valid_to IS NULL OR valid_to > valid_from)
  inflation_index numeric NULL, approved_by, approved_at, note
  # Запрет пересечений (btree_gist):
  #   EXCLUDE USING gist (catalog_position_id WITH =, rate_class_id WITH =,
  #                       daterange(valid_from, valid_to, '[)') WITH &&)
  #   NULL в valid_to даёт бесконечную верхнюю границу — COALESCE не нужен.
  # Переутверждение = UPDATE valid_to старой строки + INSERT новой. Историю не мутировать.
```

Семантика отклонений (VIEW `v_position_deviation_inputs`):

> **Ревизия 2026-08-14, фича пересчёта по ставке НДС.** Раньше VIEW назывался
> `v_position_deviations` и сам нёс колонку `deviation_pct`, посчитанную на
> ВАЛОВОЙ цене. Норматив при этом объявлен ценой **без НДС** — значит стороны
> сравнения были измерены по-разному, и оставленная колонка стала бы молча
> неверной. Колонка убрана, VIEW переименован вместе со смыслом; отклонение
> считает Python от нетто. Причина и границы —
> [спека фичи](docs/superpowers/specs/2026-08-13-vat-rate-recalculation-design.md),
> замеры и отступления — [devlog](docs/devlog/2026-08-13-vat-rate-recalculation.md).

- Дата сравнения: `estimates.data_prepared_on_date`, фолбэк `contracts.signed_date`.
- Норматив по `(catalog_position_id, contracts.rate_class_id, дата ∈ [valid_from, valid_to))` — благодаря EXCLUDE максимум одна строка.
- **`standard_unit_rate` — цена БЕЗ НДС.** Собственной ставки у норматива нет и не заводится: файл на другом режиме налогообложения сравнивается с ним без возражений.
- **Отклонение считается от НЕТТО, а не от валовой цены:**
  `deviation_pct = (gross_to_net(unit_cost_total, база НДС) / standard_unit_rate - 1) * 100`.
  Точный Decimal, округление до 0.1 п.п. только на слое представления.
  База НДС — `COALESCE(estimates.vat_rate_base_override, proposals.vat_rate)`.
  Ось сравнения — нетто, поэтому отклонение **не зависит** от того, в какой
  ставке показана поверхность.
- **Пустое отклонение имеет ДВЕ разные причины, и смешивать их нельзя**
  (`deviation_reason`):
  - `no_standard` — норматива на дату нет; нетто выведено, сравнивать не с чем;
  - `unknown_vat_base` — база НДС не заявлена файлом и не назначена вручную,
    нетто вывести не из чего.
  Обе дают `deviation_pct = NULL`, но это РАЗНЫЕ факты, и UI обязан их
  различать: «нет норматива» против «неизвестна база НДС». При неизвестной базе
  норматив всё равно **показывается** (он и есть нетто) — не вычисляется только
  отклонение.
- Исключены: `is_chapter = true`, строки с `unit_cost_total IS NULL`, позиции, чья каталожная строка имеет `kind != 'POSITION'` (HEADER/TRASH/TO_REVIEW не сравниваются с нормативами).

Служебное:

```
import_jobs
  id, contract_id NULL, round_id NULL → tender_rounds   # с 0015 — два владельца
                                                         #   (CHECK: ровно один из двух)
  amendment_no int NULL      # только у contract_id; у round_id — NULL (CHECK)
  filename, file_key, file_sha256 text NOT NULL,
  status ('pending|parsing|importing|matching|done|error'), error_text,
  warnings jsonb NOT NULL DEFAULT '[]',
  counters (positions_total, matched_cache, matched_exact, matched_nonposition, to_review),
  parsed_data jsonb NULL, parser_version text NULL   # пара — вместе NULL либо вместе заданы (CHECK)
  estimates_created int NULL   # только у round_id; NULL либо > 0 (CHECK); число offer-смет
                                # этого job (§5, «Для раунда тендера»)
  created_at, started_at, finished_at
  # Локи — два частичных уникальных индекса:
  #   uq_import_jobs_active_pair: UNIQUE (contract_id, COALESCE(amendment_no,-1))
  #     WHERE contract_id IS NOT NULL AND status NOT IN ('done','error')
  #   uq_import_jobs_active_round: UNIQUE (round_id)
  #     WHERE round_id IS NOT NULL AND status NOT IN ('done','error')
  # Блокируется одна ПАРА (contract_id, amendment_no) либо один round_id;
  # параллельный импорт РАЗНЫХ допсоглашений одного договора — разрешён.
```

## 5. Пайплайн импорта

`POST /api/v1/estimates/upload` (multipart: файл + `contract_id` + `amendment_no?` + `replace?=false`). Лимиты: размер ≤ 25 МБ (настройка), мягкий таймаут 10 мин (§3).

**Правила повторной загрузки для той же пары (contract_id, amendment_no):**

1. **Текущая** смета этой пары загружена **тем же** файлом (`estimates.import_job_id` ведёт на `done`-job с тем же `file_sha256`) → идемпотентно вернуть этот job, не импортировать. Идемпотентность привязана к текущей смете, а не к любому историческому `done`-job: иначе после замены (правило 3) повторная загрузка **вытесненного** файла отвечала бы «уже загружено», хотя в БД лежит другая смета. Такой случай идёт по правилу 2 и требует `replace=true`.
2. Уже существует estimate, файл **другой**, `replace` не передан → **`409 Conflict`** с понятным сообщением («смета уже загружена; для замены повторите с replace=true»).
3. `replace=true` (право: `admin`) → в доменной транзакции: удалить существующий estimate (CASCADE на lots/proposals/positions/raw_data), импортировать новый. Старые `import_jobs` и их файлы **не удаляются при ЗАМЕНЕ сметы** — это аудит. Удаление самого договора уносит их вместе с ним (v6.7); удаление отвергается, пока импорт этой сметы выполняется. В новом job зафиксировать warning «заменена смета estimate_id=N от <дата>».

**Для раунда тендера** (`POST /api/v1/tenders/{id}/rounds/{rid}/upload`): владелец один — раунд; правило 1 работает через **текущий job** — `done`, число смет раунда с этим `import_job_id` равно `estimates_created`, других смет у раунда нет; замена — раунда целиком, до цикла, под `FOR KEY SHARE` тендера и `FOR UPDATE` раунда; один файл даёт N offer-смет плюс baseline в ОДНОЙ транзакции сессии B и ОДИН матчинг; `parsed_data` пишет сессия A сразу после парсинга у обоих владельцев.

Транзакционная модель — **две разные сессии**:

- **Сессия A (промежуточные статусы и ошибки):** переходы `pending→parsing→importing→matching` и запись предупреждений **парсера** — отдельные короткие транзакции с немедленным commit (фронт видит прогресс). Статус `error` пишется сессией A **после** rollback домена и потому его переживает.
- **Сессия B (домен + финал):** шаги 3–4, предупреждения, **описывающие доменное состояние**, и **финальный переход в `done` вместе с итоговыми счётчиками** — одна атомарная транзакция. Смета и её `done`-статус коммитятся неразделимо: не существует состояния «смета в БД, а job не done» (crash window закрыт). При исключении — полный rollback (домена и финального статуса), затем сессия A пишет `error` + текст.

**Предупреждения делятся по тому, о чём они говорят, а не по этапу.** Предупреждение парсера относится к файлу — оно обязано пережить откат домена и быть видно во время прогресса, поэтому его пишет сессия A. Предупреждение о замене сметы, расхождении шапки с карточкой, неизвестной единице или сработавшей эвристике **описывает состояние домена** — оно не должно существовать, если домен откатился, поэтому его пишет сессия B в той же транзакции. Это тот же аргумент, которым выше требуется атомарность `done` со счётчиками. Дописывание в оба конца делается SQL-конкатенацией (`warnings || :items`), а не «прочитать-изменить-записать»: иначе одна сессия теряла бы то, что дописала другая.

Шаги:

1. Валидация, сохранение файла (§8), sha256, создание `import_job` (сессия A), ответ `job_id`. **Сохранённый файл удаляется во всех путях, где новый job не создан:** идемпотентный возврат существующего done-job (правило 1), ответ `409` (правило 2), любая ошибка создания job. Файл остаётся только у реально созданного job. Тяжёлая часть — в `BackgroundTasks`.
2. **Парсинг** (`backend/parser/` = перенесённый `excel_parser`). Адаптация под смету ГП: один подрядчик → один proposal; отсутствие baseline-блока не должно ронять `read_contractors.py` / детекцию шапки (главный технический риск — покрыть тестом на реальном файле ДО написания адаптации); `suggested_quantity` парсится штатно.
3. **Импорт** (сессия B, порт логики `ImportFullTender`): [replace: удаление старого estimate] → estimate → raw_data (+parser_version) → lots → proposal → position_items. Реквизиты из шапки сравнить с карточкой договора → расхождения в `warnings` (не блокируют).
4. **Матчинг** (та же сессия B), каскад для каждой позиции кроме `is_chapter`; везде используется одна и та же нормализованная пара `(normalize(job_title), unit_norm)`:
   1. `cache_key` в `matching_cache` → hit: проставить `catalog_position_id`; продлить TTL только для `source='auto'`; счётчик — по `kind` целевой каталожной строки: `POSITION` → `matched_cache`, `HEADER`/`TRASH` → `matched_nonposition`;
   2. SQL-поиск точного совпадения `(normalized_job_title, unit_id)` среди `kind='POSITION'` → проставить + записать кэш (`source='auto'`);
   3. miss: **атомарный get-or-create** `catalog_positions` — `INSERT ... ON CONFLICT (normalized_job_title, COALESCE(unit_id,-1)) DO NOTHING` + повторный SELECT. По `kind` найденной строки: `TO_REVIEW` → привязаться; `POSITION` → это hit ветки 2 (дубль не создавать, записать кэш); `HEADER`/`TRASH` → привязаться (строка уже вручную размечена как не-работа), позиция не попадает ни в очередь Review, ни в VIEW отклонений; учитывать отдельным счётчиком `matched_nonposition`.
   Нормализация — `sanitize_text.normalize_job_title_with_lemmatization` из парсера; `norm_version` — константа, входит в cache_key.
5. Финальный статус `done` + итоговые счётчики — **последняя операция сессии B**, в той же транзакции, что и домен (см. транзакционную модель выше).

**Recovery при рестарте** (корректно при одном worker, §3): на startup все jobs в статусах `pending|parsing|importing|matching` переводятся в `error` («прерван перезапуском, повторите загрузку»). Повторная загрузка идемпотентна/разрешена по правилам выше.

Recovery — **условие запуска, а не удобство**: незавершённый job держит частичный уникальный индекс `uq_import_jobs_active_pair` (§4), поэтому без recovery повторная загрузка той же пары вечно отвечает `409` до следующего перезапуска. Не выполнившийся recovery обязан ронять старт приложения. Ретенция файлов (§8) — наоборот, best-effort: она выполняется **отдельной транзакцией**, и её ошибка не откатывает recovery и не мешает работе (файл, не удалённый сегодня, удалится при следующем запуске).

**Ручной матчинг (экран Review):** очередь позиций, привязанных к `kind='TO_REVIEW'`. Действия: «слить с существующей POSITION» (поиск FTS/ILIKE), «утвердить как новую POSITION», «пометить HEADER/TRASH». Решение применяется **ко всем** position_items, ссылающимся на данную TO_REVIEW-строку. При слиянии, в одной транзакции: `UPDATE position_items SET catalog_position_id = <target>` → запись в `matching_cache` c **`source='manual'`, `expires_at = NULL`** (ручные решения не истекают) → **`DELETE` TO_REVIEW-строки** (после переноса ссылок она пуста; удаление освобождает её уникальную нормализованную пару, иначе она перехватывала бы будущие get-or-create). «Утвердить как POSITION» / «пометить HEADER/TRASH» — смена `kind` той же строки, без удаления. Инвариант: **записи `matching_cache` никогда не указывают на строки `kind='TO_REVIEW'`** (кэш пишется только в ветках 2/HEADER/TRASH и при ручных решениях) — это условие безопасности DELETE, закрепить тестом. Векторные подсказки по embedding — фаза 2, в MVP не делать.

## 6. Семантика сквозной матрицы (зафиксировано)

- По каждому договору участвует **только последняя смета** (максимальный `amendment_no`, NULL — минимальный). Предыдущие — история в карточке договора, в матрицу не попадают.
- Вес позиции: `w = COALESCE(suggested_quantity, quantity)`. **Подтверждено на реальном файле в фазе 0** (`docs/phase0-input-data.md`), развилка закрыта: авторитетно `suggested_quantity` — колонка «Предлагаемое количество». На 1828 из 1828 расценённых строк `Стоимость всего = цена за единицу × suggested_quantity`, тогда как `quantity` («Общее кол-во») точно так же на 1828 из 1828 строк даёт справочную колонку «Стоимость всего за объемы заказчика». Строк, где `quantity` и осмысленно (≠1), и отличается от `suggested_quantity`, не найдено ни одной — то есть `quantity` в сметах ГП либо заглушка `1`, либо равен `suggested_quantity`, и `COALESCE` даёт верный вес в обоих случаях. Ограничение: проверено на одном образце (§9, фаза 0).
- Работа встречается в смете несколько раз → ячейка = средневзвешенная ставка:
  `SUM(unit_cost_total * w) / SUM(w)` **только по строкам, где `unit_cost_total IS NOT NULL` и `w > 0`**; если таких строк нет — ячейка пустая. Drill-down в позиции по клику.
- Строки = `catalog_positions (kind='POSITION')`, колонки = договоры (группировка по объекту), в ячейке ставка + `deviation_pct` цветом. Фильтры: класс, период, текстовый поиск по названию работы. Фильтр «статья/раздел» — не в MVP (у него нет формального источника: разделы специфичны для каждой сметы, а строки матрицы — каталожные). Закрепление первой колонки. Min/max/разброс — не в MVP.

> **Ревизия 2026-08-14, фича пересчёта по ставке НДС.** Формула ячейки была
> записана ВАЛОВОЙ (`SUM(unit_cost_total * w) / SUM(w)`), а условие пустоты —
> только «нет строк с ценой и весом». Матрица — много-договорная поверхность:
> у выборки нет общей ставки показа договора (у разных договоров разные
> `vat_rate_target`), поэтому единственная общая ось — нетто (спека §2.5), и
> строка выше это ещё не отражала.
>
> Ячейка теперь:
> `SUM(gross_to_net(unit_cost_total, vat_rate_base) * w) / SUM(w)` — по
> строкам, где `unit_cost_total IS NOT NULL`, `w > 0` **и `vat_rate_base`
> известна**; норматив `standard_unit_rate`, с которым ячейка сравнивается
> цветом, — тоже нетто (ревизия §4), и он один на обе оси. Условие пустоты
> расширено: ячейка, где база НДС неизвестна хотя бы у одной вошедшей строки,
> пуста ЦЕЛИКОМ, а не частично — «единица неполноты — ячейка, а не строка
> сметы» (спека §2.6): показать сумму по известным строкам, замолчав про
> неизвестные, значило бы выдать неполное значение за полное.
>
> Строка (одна работа, много договоров) сверх ячеек несёт **нетто-вес**
> `row_amount` — сумму по вычислимым ячейкам выборки — и булев признак
> **`row_amount_incomplete`**: строка, где хотя бы одна её ячейка скрыта
> неизвестной базой, обязана называть неполноту явно, а не молчать (`SUM` в
> SQL игнорирует `NULL`, и без признака частичный вес выглядел бы полным).
> Признаку неполноты соответствует нетто-вес, а не валовой: включать в
> `row_amount` валовую часть скрытой ячейки значило бы смешать единицы внутри
> одной суммы. Правка 4 финального ревью ветки: врезка о том же, что уже
> прожили §4 и §7.6 этой веткой, — экран (`frontend/src/pages/matrix/
> MatrixPage.tsx`) отдельно объявляет нетто-ось подписью на странице.
>
> Спека и замеры — [спека фичи](docs/superpowers/specs/2026-08-13-vat-rate-recalculation-design.md)
> §2.5–§2.6, [devlog](docs/devlog/2026-08-13-vat-rate-recalculation.md).

## 7. Экраны фронтенда

1. **Договоры/Объекты** (на базе Projects) — список, карточка: реквизиты, класс, текущие сметы, **история загрузок и замен** (список import_jobs с датами, статусами и скачиванием исходного XLSX; заменённые сметы доступны только как файл — структурированная история за скоупом MVP), drag-and-drop загрузка, статус job с поллингом, показ `warnings`.
2. **Ручной матчинг** (на базе Review) — очередь TO_REVIEW, действия из §5.
3. **Нормативы** (на базе ReferencePrices/handbook, право `admin`) — классы, ставки, периоды, форма переутверждения с коэффициентом инфляции (предзаполнение: новая ставка = старая × индекс, редактируемо); ошибки EXCLUDE (пересечение периодов) — человекочитаемый 400. **Плюс ряды индексов инфляции** (v6.10): именованные показатели с годовыми цепными коэффициентами, каждый год — коэффициент, источник и признак прогноза. Справочник создаётся ПУСТЫМ, первый ряд заводит человек; удаления нет ни у рядов, ни у значений — ненужный ряд архивируется и остаётся читаемым по старой ссылке. Правка ряда — ОДНА транзакция на всё окно: ряд общий и без версий, поэтому «исправлен наполовину» означает неверные числа у всех, кто в этот момент смотрит сравнение ([спека](docs/superpowers/specs/2026-08-18-inflation-adjustment-design.md) §2.6, §2.10, §2.12).
4. **Паспорт проекта** — страница `/contracts/:id/passport`, **свод по статьям фиксированного классификатора компании** с рублями на квадратный метр: шапка с ТЭП и коммерческими условиями, кольцо структуры стоимости, таблица по статьям, признак ручного разноса на строке статьи классификатора и печатная сноска о числе действующих ручных решений (фича разноса после фазы 7 — [спека](docs/superpowers/specs/2026-08-11-unallocated-category-override-design.md) §2.6, §2.10). Считается по **исходной** смете договора (`amendment_no IS NULL`). Печать — `@media print`, PDF в MVP = Ctrl+P, **раскладка многостраничная**.

   **Таблица статей несёт ещё две величины со самой сметы — объём статьи с её единицей и ставку ₽ за единицу** ([спека](docs/superpowers/specs/2026-08-24-passport-volumes-design.md) §2.1, §2.2, §2.5). Носитель обеих — строка-раздел сметы, несущая код статьи; сумма нижних позиций объёмом не является никогда (§3.1, §3.4 той же спеки — фолбэк разобран и отвергнут замером). У каждого узла свода РОВНО ОДНО из десяти состояний ставки, и причина пустой клетки всегда приходит СОСТОЯНИЕМ — клиент его не вычисляет. Подпись при этом несут не все состояния: **`no_carrier` остаётся визуально пустым, без пилюли** (спека §2.5, DoD 6), потому что смета эту статью как статью не называет вовсе, и «объяснять» тут нечего — пометка утверждала бы находку там, где её нет. Не «исправлять» это на подпись: восемь состояний из десяти несут текст, `rate` несёт число, `no_carrier` — ничего. Немасштабируемы ровно две единицы справочника — «Комплект» и «Месяц»; «Штука» масштабируема. **Под таблицей — строка охвата:** сколько статей неперекрывающегося набора несут ставку и какую долю цены договора эти статьи покрывают. Набор — антицепь дерева классификатора (статья с носителем, у которой нет потомка-статьи с носителем), и читатель может сложить видимую колонку «Доля» и получить то же число с точностью до округления показа. **Антицепь доказывает «доля ≤ 100 %» только при НЕотрицательных суммах**, а `CHECK`-а на знак `position_items.total_cost_total` в схеме нет — поэтому доля, вышедшая из диапазона `0..100`, отдаётся как `NULL` со состоянием `out_of_range` (спека §2.8, DoD 32), а не срезается до ста и не печатается. Не считать эту проверку избыточной при доказанной антицепи: доказательство держится на знаке сумм, которого схема не гарантирует. Строка печатается всегда: без неё лист читается как полный свод расценок, а он не полный.

   **Ставка выражена в той же ставке показа НДС, что и суммы паспорта** (§2.2, §2.7 той же спеки): приведение построчным `restate_gross` делается ДО деления на объём, а объём от ставки НДС не зависит. Отсюда запрет: **переносить ставку с этого экрана в норматив нельзя без деления на базу НДС** — норматив объявлен ценой без НДС (§4), а эта ставка валовая. Нормативов фича не заводит и `v_category_totals`, схему и миграции не касается.

   **Обещание «одна страница А4» снято** (фаза 7, Ф6; обоснование — спека `docs/superpowers/specs/2026-08-10-project-passport-design.md` §2.11 и замер §1.3). Причина: на лист входит порядка 45–50 плотных строк, а непустых ветвей дерева статей замерено 103–178 на смету. Свести их к одному листу можно было либо потеряв второй и третий уровни классификатора — а именно в них живёт разбивка, которую аналитик расписывает руками, — либо показав на бумаге меньше, чем на экране, то есть той самой обрезкой при печати, которую прежняя редакция этого пункта запрещала. Выбран третий вариант: состав полный, печать многостраничная. «Одна страница» в §1 пункт 2 читается после этого как **деловая цель «один свод»**, а не как ограничение печати.

   **Обязательство печатной раскладки осталось, изменилась его формулировка:** замером в браузере проверяется, что **нет обрезки по правому краю и нет разрывов внутри строк**, шапка таблицы повторяется на каждом листе (§11 — `@media print` в jsdom не наблюдаем).

   **«Состав полный» абзацем выше описывает НАМЕРЕНИЕ фазы 7, а не поведение экрана: замер показал, что по умолчанию оно не выполняется.** Формулировка оставлена как есть — это запись принятого тогда решения, и переписывать её задним числом нельзя; здесь она уточняется. Печатный состав следует состоянию раскрытия дерева статей на экране. Дерево раскрывается шевронами и по умолчанию свёрнуто; печатный слой — блок-лист (скрывает `data-print="hide"`) и ничего не разворачивает сам. Поэтому по умолчанию на бумагу попадают корни дерева, строка «Нераспределённое» и итог — без второго и третьего уровня классификатора; они печатаются только если аналитик раскрыл соответствующую ветку на экране перед печатью. Замерено фичей разноса, хотя верно и без неё — [спека](docs/superpowers/specs/2026-08-11-unallocated-category-override-design.md) §2.10, [devlog](docs/devlog/2026-08-11-unallocated-override.md). Печатное обещание самой этой фичи от раскрытия зависит лишь отчасти, и разница названа точно. Сноска под таблицей о числе действующих решений печатается **всегда**: она лежит под таблицей и раскрытию не подчинена. Пометка «вручную» стоит на строке **статьи классификатора** (не на строке-разделе сметы — это разные оси, §1.5 спеки Ф6) и печатается всегда для статей **верхнего** уровня, потому что корни дерева рисуются безусловно; на вложенной статье она попадает на бумагу только если раскрыт хотя бы один её предок. Носителем печатного обещания поэтому является сноска, а пометка — его уточнением там, где строка видна.

   **`passport_top_n` — история, а не действующий инвариант.** Настройка (таблица настроек, экран Settings) и её `CHECK` 1–20 в миграции 0004 остаются в БД и управляют **экраном фазы 6**, который убран из навигации и вернётся drill-down'ом из статьи. Верхняя граница подобрана замером печатной раскладки **того** экрана (`docs/phase6-analytics.md` §1.5, §3.2) и на паспорт проекта не влияет.
5. **Сквозная матрица** — новая страница на TanStack Table, семантика §6.
6. **Отчёты** (на базе Reports) — Excel через openpyxl: (а) свод расценок по договору с отклонениями; (б) сравнение с нормативами «для банка»; (в) сравнение договоров по статьям классификатора (§7 пункт 7). **Три отчёта — три файла** (v6.8; до сравнения договоров их было два): у них разный охват, разные параметры и разный разрез — свод берёт один договор, «для банка» — выборку в разрезе «класс → работа», сравнение — выборку в разрезе «статьи × договоры».

   **Макет «для банка» согласован с пользователем 2026-08-04** (вопрос задан первым сообщением фазы 6, как и требовал прежний текст этого пункта):

   | Что | Решение |
   |---|---|
   | Разрез | **класс → работа**: секции по `rate_class`, внутри — каталожные `POSITION` |
   | Колонки | работа, единица, объём, ставка, норматив, стоимость, отклонение %, **отклонение в деньгах** |
   | Итоги | **по каждому классу и общий** |
   | Шапка | название, период, состав выборки, дата формирования; снизу — блок подписей «Составил / Утвердил» |

   Отклонение в деньгах = `(ставка − норматив) × объём`. Итоги: деньги суммой; отклонение — **средневзвешенное по объёму**, то есть `(факт − норматив) / норматив` по суммам, а не среднее арифметическое процентов (последнее завышает вклад мелких работ и в разговоре с банком даёт неверную цифру). Позиции без норматива в расчёт отклонения **не входят** и показываются отдельным счётчиком в каждом итоге; когда сравнимых работ нет вовсе, отклонение пусто и в процентах, и в деньгах — ноль там читался бы как «сошлось с нормативом». Суммы сходятся **по файлу**: строка отчёта агрегирует работу (несколько позиций → одна строка), поэтому по числу видимых строк позиции не сосчитать — в итогах печатается счётчик «Сравнимых позиций», а в сноске — независимо посчитанное «Всего расценённых позиций» (`count(*)` по VIEW, не сумма счётчиков). Секции строятся от **классов выборки** (договоров с последней сметой), а не от строк с положительным объёмом: класс, все позиции которого без объёма, остаётся секцией со счётчиками, иначе шапка говорила бы «Классов: N», а секций было бы меньше.

   > **Ревизия 2026-08-14, фича пересчёта по ставке НДС.**
   >
   > **Лист измерен в НЕТТО, и это объявлено на нём самом.** Отчёт «б» — выборка
   > многих договоров с разными ставками показа, поэтому общей оси, кроме нетто,
   > у него не существует. И факт, и норматив, и отклонение в деньгах —
   > без НДС; в шапке листа стоит подпись «Все суммы и нормативы — без НДС».
   > Читатель обязан видеть, в чём измерены числа, которые он складывает.
   > Свод по договору (отчёт «а») — поверхность ОДНОГО договора, он идёт в
   > **целевой** ставке этого договора и подписан своей строкой.
   >
   > **Счётчиков исключённого стало ЧЕТЫРЕ**, и они по-прежнему образуют
   > разбиение (сравнимые + четыре = всего). Приоритет строгий, сверху вниз, и
   > он не декоративный — позиция, попадающая под несколько причин, считается
   > РОВНО ОДИН раз, в первой подходящей:
   >
   > 1. **с ценой, но без объёма** — блокирующая причина, как и раньше: норматив
   >    без объёма не помог бы;
   > 2. **с объёмом, но без базы НДС** — нетто не выведено, сравнивать нечем;
   > 3. **с объёмом и базой, но без норматива**;
   > 4. сравнимые.

7. **Сравнение договоров** — новая страница на маршруте `/compare`, строки — дерево статей классификатора, колонки — договоры выборки; вход только из списка договоров галочками и двумя кнопками, ПУНКТА В ГЛАВНОМ МЕНЮ НЕТ (сравнение без выборки бессмысленно). Режимы показа НДС и выбранная ставка живут в URL; **там же живут выбранный ряд индексов инфляции и целевой месяц** (v6.10) — ссылка воспроизводит параметры расчёта, но не гарантирует исторического результата, потому что версий у ряда нет. Целевой месяц разрешает СЕРВЕР в названной бизнес-таймзоне и возвращает его клиенту: `Date.now()` в браузере — часы читателя, и два человека получили бы два ответа. Печати нет. **Плюс диаграмма стоимости договоров над таблицей и фильтр по классу объекта** (v6.11), и оба выражены в URL: класс — многозначным `rate_class_id`, который СУЖАЕТ выборку обеих форм и законно сочетается с `ids`; адрес канонический — id по возрастанию, при полном наборе классов параметра нет вовсе. Диаграмма строится по ИТОГУ ДОГОВОРА, а не по выбранной статье классификатора (переключатель корзины ДГП/ДС/Итого она при этом слушает), столбцы идут ОТ СТАРЫХ К НОВЫМ (у таблицы порядок обратный, `signed_date DESC`, и это осознанная цена: у диаграммы горизонтальная ось — время). Единица диаграммы («₽/м²» либо сумма договора) — состояние страницы, в URL её нет. В режиме «Единая» действующая ставка показа ПИШЕТСЯ В АДРЕС: сужение по классу меняет состав выборки, а значит и серверный предвыбор ставки, и без записи числа поехали бы от нажатия на чип. Семантика и DoD — [спека](docs/superpowers/specs/2026-08-17-contract-comparison-design.md), приведение к ценовому уровню — [спека](docs/superpowers/specs/2026-08-18-inflation-adjustment-design.md), диаграмма и фильтр — [спека](docs/superpowers/specs/2026-08-19-comparison-cost-chart-design.md).
8. **Тендеры** — `/tenders` и `/tenders/:id`: список, карточка с решёткой «участники × раунды», панель раунда (`?round=`) с четырьмя состояниями расчётной стоимости, загрузка сводной таблицы, удаление участника с подтверждением состава (token). **Плюс свод по этапам одного участника** (v6.12) — `/tenders/:id/summary?offers=…`: строки — дерево статей классификатора, колонки — ВЫБРАННЫЕ раунды одного участника; вход только выбором предложений на решётке карточки (плитки-переключатели, клик по имени участника выбирает все его этапы), ПУНКТА В МЕНЮ НЕТ, выбор живёт в URL; путь считается по выбранным колонкам, четыре состояния ячейки данными, «снято» никогда не «−100 %». Сравнение участников между собой НЕ спроектировано (рамка §5.5) — прежняя редакция этой строки называла его фичей 3 по ошибке пересказа; смешанные трассы — задел контракта. Семантика и DoD — [спека](docs/superpowers/specs/2026-08-27-stage-summary-design.md), решётка — [спека контура](docs/superpowers/specs/2026-08-26-tenders-contour-design.md) §2.14.

## 8. Хранилище файлов

Локальная директория за абстракцией `Storage` (`save/get/delete`, реализация `LocalStorage`, путь из настроек). Требования: имя на диске = непрозрачный ключ (uuid), оригинальное имя — только в БД; никаких путей от клиента; выдача только через авторизованный `GET /api/v1/import-jobs/{id}/file`, не через статику. Ретенция: файлы jobs со статусом `error` старше 30 дней (настройка) удаляются проверкой при старте приложения — т.е. при первом запуске после истечения срока, не ровно в срок; файлы `done`-jobs хранятся бессрочно (аудит), пока жив владелец — договор либо раунд тендера; удаление договора, раунда или тендера удаляет их после коммита доменной транзакции, best-effort; удаление участника тендера файлы НЕ трогает — job и результат разбора остаются аудитом (спека контура §2.11). S3-реализация — потом, без правки вызывающих мест.

## 9. Процесс разработки

### 9.1. Цикл фичи (единица работы)

Единица работы — **фича**: связное изменение, которое можно спроектировать,
реализовать и принять целиком. Пять шагов, три гейта:

1. **Брейншторм** — разведка контекста, вопросы по одному, 2–3 варианта с
   trade-off. **Гейт 1: дизайн одобрен в диалоге.**
2. **Спека** → `docs/superpowers/specs/ДАТА-тема-design.md`. **Гейт 2.**
3. **План** → `docs/superpowers/plans/ДАТА-тема.md`. **Гейт 3.**
4. **Реализация** — TDD, коммиты по задачам плана.
5. **Финал** — `docs/devlog/ДАТА-тема.md`, затем PR. Если фича выстрадала правило
   работы — завести инсайт и строку в указателе (§12).

**Гейт — остановка, а не уведомление:** следующий шаг не начинается, пока
предыдущий не одобрен. Гейты проходят в диалоге; отдельных PR под спеку и план
нет — спека, план и реализация живут в одной ветке фичи.

**Код тестов в плане — ЭСКИЗ, пока каждое названное им имя не проверено
`grep`-ом.** На гейте 3 пройти по символам, которые план упоминает (фикстуры,
хелперы, ключи ответа, функции форматирования, маршруты): существующие оставить,
несуществующие пометить «заводится этой задачей» либо свести код к утверждениям.
Синтаксически законченный тест читается исполнителем как готовый, и поиск
несуществующего стоит круга на каждую задачу — на фиче пересчёта НДС так ушло
шесть кругов, включая набор с пятью независимыми причинами непрогоняемости
разом. При этом **утверждения с числами** в планах не подводили ни разу: ломается
обвязка, а не смысл (`docs/insights/replaying-new-rules.md`, слой 1).

Сокращения: дизайн уже зафиксирован и проверен фактами → спека тонкая (ссылка
на источник + дельта); правка только документации → без плана; тривиальная
механика (опечатка, версия зависимости) → без цикла, но если правка тянет за
собой поведение — это фича. **Гейт 1 не пропускается никогда.**

### 9.2. Таксономия документов

| Документ | Где | Что в нём |
|---|---|---|
| Инварианты и процесс | `AGENTS.md` | архитектурные решения навсегда, процесс, грабли (§11) |
| Рамка фазы | `docs/phaseN-frame.md` | скоуп / вне скоупа / DoD фазы / состав фич |
| Спека фичи | `docs/superpowers/specs/` | проблема с замерами, решение, отвергнутые альтернативы |
| План фичи | `docs/superpowers/plans/` | задачи с чекбоксами, файлы, интерфейсы, Global Constraints, ветка |
| Devlog фичи | `docs/devlog/` | что сделано, замеры, отступления от плана, найденные грабли |
| Инсайт | `docs/insights/` | одно выстраданное правило работы: чем выстрадано, как теперь, чего не ловит (§12) |
| Технический долг | `docs/TECH_DEBT.md` | известная проблема ДЕЙСТВУЮЩЕГО кода: что не так, чем закрыто сейчас, что сделать, чем выстрадано |
| Предложение | `docs/proposals/` | решение, проработанное до готовности и осознанно ОТЛОЖЕННОЕ: проблема с замерами, выбранный вариант, отвергнутые, список правок |

Последние два класса существовали и раньше — `TECH_DEBT.md` с полутора десятками
записей не был описан здесь вовсе. Граница между ними и спекой проходит по тому,
о чём документ говорит, а не по объёму: **долг описывает то, что есть, предложение
— то, чего ещё нет, спека — то, что делается сейчас.** Отложенное решение нельзя
класть в `specs/`: файл там означает пройденный гейт 2, то есть «реализация
следующая», и однажды будет взят в работу без решения пользователя. Предложение
живёт временно и обязано кончиться — стать спекой при планировании либо быть
удалённым вместе с умершей предпосылкой (`docs/proposals/README.md`).

**Факт живёт в одном месте.** Спека ссылается на рамку, план — на спеку,
devlog — на план; дублировать текст нельзя. При противоречии побеждает документ,
который ближе к инварианту: `AGENTS.md` → спека → план.

Фазового отчёта в новом процессе нет: при приёмке фазы в её рамку добавляется
врезка «принята, дата» со ссылками на devlog'и фич. Отчёты фаз 0–6
(`docs/phaseN-*.md`) остаются как исторические.

### 9.3. Ветвление, проверки, PR

- Ветка на фичу; префиксы `feat/`, `fix/`, `chore/`, `docs/`, `build/`, `ci/`,
  `perf/`. Имя ветки прописывается в плане.
- Спека, план и реализация — отдельными коммитами **в одной ветке**; PR один,
  со ссылками на спеку и план в описании.
- В `main` напрямую не коммитить. `main` держим зелёным.
- **Перед пушем — `just ci`** (ruff + pytest + eslint + tsc + vitest, шаги по
  отдельности). Правки только в `docs/` от `just ci` освобождены — ни одна
  проверка их не касается.
- Ветки после мержа удаляем.

### 9.4. Фазы (вехи приёмки)

Фаза — граница приёмки и крупная арка, а не единица работы: у неё рамка (§9.2),
состав фич и свой DoD. Приёмка = DoD фазы + прогон на живом стенде + ревью
пользователя. Внешнее ревью пользователь гоняет сам; замечания проверяются по
фактам, тестом до правки.

0. **Входные данные:** получить у пользователя 2–3 реальных XLSX-сметы ГП **до начала работ**. На них же: сверить семантику quantity vs suggested_quantity (§6). **Реальные файлы не коммитить**: каталог `samples/` в `.gitignore`; для тестов подготовить обезличенные fixtures (заменить наименования контрагентов/объектов, сохранить структуру и типы данных).
   **Статус: выполнено условно** (`docs/phase0-input-data.md`) — получен один образец вместо 2–3, сверка §6 проведена и вывод внесён в §4/§6. Дополнительные образцы (другие объекты и годы) нужны **до пилотной приёмки парсера**: вариативность шапки — главный риск фазы 3.
1. **Инициализация и чистка:** в `gca_mvp` — этот документ как `AGENTS.md`, рядом однострочный `CLAUDE.md` с указателем на `AGENTS.md`, `.gitignore` (включая `samples/`), затем перенос кода `udp-tenders@98fb676` **одним коммитом без чужой git-истории**, с текстом коммита вида `import udp-tenders @ 98fb67667c44 as boilerplate` и упоминанием источника в README. На зафиксированной ревизии udp-tenders файла LICENSE нет, а все три исходных репозитория принадлежат автору проекта; если LICENSE/copyright notices появятся в источниках — перенести их файлами, ссылки в README недостаточно. После переноса: удалить LLM/PDF/MinIO/organizations/УПД-домен, привести названия (пакеты, `application_name` в database.py, заголовки фронта) к проекту, зелёные оставшиеся тесты, запуск пустого приложения одним worker.
2. **Схема:** Alembic — pgvector + btree_gist extensions; перенос таблиц tenders-go (raw SQL через `op.execute()` для COALESCE-уникальных, частичных, EXCLUDE-индексов; проверить корректный downgrade); contracts, rate_classes, rate_standards, import_jobs, estimate_raw_data, `normalized_job_title`; VIEW отклонений; слияние units. SQLAlchemy-модели поверх.
3. **Парсер:** перенос `excel_parser` → `backend/parser/`, чистка импортов, адаптация один-подрядчик-без-baseline, юнит-тесты на fixtures из фазы 0.
4. **Импорт+матчинг:** две сессии, транзакция, каскад, recovery, идемпотентность, replace-флоу, эндпоинты. Интеграционные тесты, включая: повторный импорт того же файла (идемпотентность); другой файл без replace (409); replace=true (замена + аудит); два miss с одинаковой нормализованной парой (один TO_REVIEW, не конфликт); ручное решение переживает истечение auto-TTL; рестарт с зависшим job; смета и `done`-статус коммитятся атомарно (симуляция падения между матчингом и финалом не оставляет сметы без done-job); слияние TO_REVIEW удаляет строку и не оставляет кэш-записей на TO_REVIEW.
5. **CRUD и Review:** договоры, нормативы, ручной матчинг, роли.
6. **Аналитика:** паспорт, матрица, Excel (после согласования макета).
   **Статус: выполнено** (`docs/phase6-analytics.md`). Макет «для банка» согласован до начала работ по §7.6 и внесён в §7.6 этого документа; шесть решений §6 брифинга приняты и обоснованы. Метрика §10 перепроверена после наполнения каталога: 4091/4092 на трёх контрольных договорах с новыми `contract_id`.
7. **Паспорт проекта по статьям классификатора** — рамка и состав фич:
   `docs/phase7-frame.md`.

## 10. Definition of Done

DoD ниже — **проектный**. У каждой фичи свой DoD в её спеке, у фазы — в её рамке (§9.2).

- Реальная смета проходит конвейер целиком. Метрика матчинга: после первичного наполнения каталога и ручной обработки очереди Review те же файлы фазы 0 загружаются для **специально созданных контрольных договоров с новыми `contract_id`** (иначе сработает идемпотентность и вернутся старые jobs); по счётчикам этих новых jobs `(matched_cache + matched_exact) / positions_total ≥ 90%`, где `positions_total` = число позиций, допущенных к каскаду матчинга (`is_chapter = false`; `matched_nonposition` в знаменатель входит, в числитель — нет).
- Паспорт проекта показывает реквизиты договора (номер, подписант, дата, класс, подрядчик), ТЭП объекта, коммерческие условия и свод по статьям классификатора с рублями на квадратный метр; состав тот же в §7.4. Печатается **без обрезки по правому краю и без разрывов внутри строк**, с повторением шапки таблицы на каждом листе; печать многостраничная — обещание «одна страница А4» снято фазой 7 с обоснованием (§7.4). Проверяется **только замером в браузере**: `@media print` в jsdom не наблюдаем (§11). Прежняя редакция ссылалась на «§1.2» — подраздела с таким номером в документе нет.
- **Отклонений от нормативов в паспорте проекта НЕТ, и это смена состава, а не пропуск.** §1 пункт 2 называет их среди полей паспорта; фаза 7 сделала осью паспорта статью классификатора, а не каталожную позицию, и норматив к статье не применим — он задан на пару «каталожная позиция × класс» (§4). Отклонения остаются в матрице (§7.5) и в отчётах (§7.6), а в паспорт вернутся drill-down'ом из статьи вместе с экранами фазы 6. До тех пор строка «Превышение норматива видно в матрице и паспорте» ниже верна **только про матрицу**.
- Превышение норматива видно в матрице без ручных действий; «нет норматива» отличим от «0%» **и от «неизвестна база НДС»** — это ТРИ разных факта, а не два (ревизия §4, фича пересчёта по ставке НДС 2026-08-14). Пустое отклонение несёт код причины, и UI обязан подписывать причины по-разному. В паспорте проекта отклонений нет — см. выше.
- **Деньги показываются в объявленном налоговом составе, и состав объявлен на самой поверхности.** Речь именно о СОСТАВЕ суммы — входит в неё НДС или нет, — а не о «разных единицах»: рубли остаются рублями, и складывать их арифметически можно всегда. Нельзя другое — молча сравнивать и подытоживать суммы разного состава.
  - **Одно-договорные поверхности** (паспорт проекта, паспорт Ф6, свод по договору) — в целевой ставке договора.
  - **Много-договорные РАСЧЁТНЫЕ поверхности** (сквозная матрица §7.5, отчёт «для банка» §7.6) — в нетто. Их назначение — сравнивать расценки между договорами, а общей ставки у выборки нет: валовые суммы разных ставок сравнению не подлежат.
  - **Много-договорные СРАВНИТЕЛЬНЫЕ поверхности** (страница сравнения договоров, §7 пункт 7, и её выгрузка, §7.6): ось сравнения — всегда нетто (медиана и отклонения считаются по нетто в любом режиме); ПОКАЗ может иметь объявленные режимы (своя ставка договора / единая выбранная ставка / нетто), и состав объявляется на самой поверхности — на экране и на листе; в режиме «своя ставка» итог по договору вправе складывать суммы разного налогового состава при обязательной подписи — тем же основанием, каким это разрешено обзорной главной.
  - **Обзорная главная** — стартовый дашборд на маршруте `/` ([спека](docs/superpowers/specs/2026-08-16-home-dashboard-design.md)) — **валовая, каждый договор в СВОЕЙ действующей ставке**, с обязательной подписью на поверхности. Это осознанная ревизия прежней редакции, а не исключение из неё: главная ничего не сравнивает и ничего не рассчитывает, она отвечает на вопрос «сколько всего денег в базе». Ответ на него человек ждёт в тех же деньгах, что стоят в договорах, — то есть с НДС. Ставка при этом у договоров разная, и итог складывает суммы разного налогового состава; именно поэтому подпись здесь не пожелание, а условие корректности. Действующая ставка — `money.vat.effective_display_rate`, второй формулы не заводить.
  - **Свод по этапам тендера** (§7 пункт 8, v6.12) — **одно-участниковая** поверхность: колонки — выбранные раунды ОДНОГО участника. Ось выбирается по множеству ИЗВЕСТНЫХ ставок среди выбранных колонок — три случая: одна известная ставка → известные колонки валовые (единый множитель сокращается в проценте и одинаково масштабирует вклад — таблица сходится тождественно); несколько известных → известные колонки ЦЕЛИКОМ нетто с подписью причины и перечислением ставок по этапам — торг, начавшийся в одной ставке и кончившийся в другой, законен, и отказывать в дельтах нельзя; ни одной известной → сопоставимых итогов нет, трасса недоступна. Колонка с неизвестной базой НДС в любом из трёх случаев показывает состояния ячеек, но не суммы: под подписью «с НДС 20 %» валовая величина неизвестного состава была бы ложью, а на нетто-ось валовая сумма не попадает никогда. Ценовой уровень — номинальный, объявлен на поверхности; приведения нет.
  - Подпись обязательна на экране и на листе: читатель обязан видеть, в чём измерены числа, которые он складывает. Тултип подписи не заменяет — он объясняет расчёт.
  - Ось сравнения при этом всегда нетто, поэтому отклонения от ставки показа не зависят.
- **Ценовой уровень — ВТОРАЯ ось денежных величин, и она объявляется на поверхности так же, как налоговый состав** (добавлено v6.10). Много-договорные СРАВНИТЕЛЬНЫЕ поверхности (§7 пункт 7 и её выгрузка) вправе дополнительно приводиться к ценовому уровню выбранного месяца по ИМЕНОВАННОМУ ряду индексов; ряд и месяц печатаются и на экране, и на листе. Правило то же и по той же причине: читатель обязан видеть, в чём измерены числа, которые он сравнивает, — а рубли 2024 года и рубли 2026-го измерены по-разному, даже когда налоговый состав у них один.
  - **Приведение по умолчанию ВЫКЛЮЧЕНО**, и без него ответ не меняется ни одним ключом — то же требование, что у ручной ставки НДС выше.
  - **Единица расчёта — смета, а не договор:** дельта допсоглашения подписана позже базы, иногда на годы, и это разные рубли внутри одного договора. Корзина «Итого» становится суммой раздельно приведённых ДГП и каждого ДС.
  - **Порядок операций — нетто → инфляция → ставка показа.** Ось сравнения остаётся нетто, площадь не индексируется, медиана и отклонения считаются от ПРИВЕДЁННЫХ значений — иначе подсветка отвечала бы на другой вопрос, чем показанные числа.
  - **Считает сервер, до агрегации.** У клиента нет разбивки по сметам, медиана считается раньше показа, в JS нет `Decimal`, а вторая копия формулы разошлась бы с первой. Ряд индексов — общий и без версий: воспроизводимость исторического расчёта лежит на выгруженном файле, и лист печатает по каждому использованному году коэффициент, источник, признак прогноза и дату правки ряда.
  - **Дыра в покрытии — доменный отказ, а не подстановка.** Номинальные числа не выдаются как результат приведения; экстраполяции нет — число, выдуманное кодом, некому подписать.
- **Без ручной поправки приложение отвечает как до фичи, посимвольно.** Суммы паспорта, дерево статей, кольцо, ₽/м², файловые итоги и `delta_to_file_total` при незаданной цели совпадают с дофичевыми до последнего знака; `delta_to_file_total` — диагностика ИМПОРТА и от ручных ставок не зависит **никогда**.
- Переутверждение норматива не меняет отклонения старых смет (сравнение по дате сметы).
- Прерванный рестартом импорт не оставляет ни зависшего job, ни половины сметы в БД.
- Ручное решение из Review продолжает матчить ту же позицию спустя >30 дней (тест с подменой времени).
- Загрузка исправленного файла даёт 409 без replace и корректную замену с replace.
- **Ручной разнос статей не обещает нуля «Нераспределённого» глобально.** Средствами
  приложения до нуля доводятся нераспределённые деньги, принадлежащие разделам с
  определённой структурой; позиции вне структуры файла, `structure_disabled` и
  допработы с неразрешимой ссылкой остаются диагностируемыми границами — [спека
  разноса](docs/superpowers/specs/2026-08-11-unallocated-category-override-design.md) §5.5.
- **На смете стенда `gca_dev`** ни одной из трёх границ нет, и там
  «Нераспределённое = 0» проверяется буквально.
- `pytest` и `vitest` зелёные; критический путь покрыт интеграционными тестами.

## 11. Известные грабли

- COALESCE-уникальные, частичные и EXCLUDE-индексы — только raw SQL в миграциях; `alembic downgrade` должен их корректно снимать.
- Парсер опирается на объединённые ячейки (`build_merged_shape_map`); не ломать досрочный выход по merged-ячейке в первой колонке (признак конца позиций / начала summary).
- `normalize_job_title_with_lemmatization` тянет NLP-зависимости (pymorphy3/natasha) — оставить только реально используемые; spaCy-модель не тащить, если не используется. Нормализация должна быть **детерминированной** (одна строка → всегда один результат) — иначе разъедутся кэш, каталог и матчер.
- Инкремент `norm_version` — это не «поменял константу»: обязательная миграция перевыпуска ключей `source='manual'` (§4) и пересчёт `normalized_job_title` в каталоге.
- `UNIQUE NULLS NOT DISTINCT` — PG16-синтаксис; в тестах та же мажорная версия PG, что и в проде.
- **Откат приложения ниже миграции 0003 не поддерживается после первой сметы с многокилобайтовым наименованием.** Индексы версии 0002 такие значения не проиндексируют, поэтому `alembic downgrade` упрётся в те же `ProgramLimitExceeded`; downgrade сам это проверяет и отказывается с объяснением, а не сырой ошибкой PostgreSQL. Вернуться на 0002 можно только удалив или сократив эти строки — это потеря данных и решение человека, не миграции. Схема откатывается, данные нет.
- **btree не индексирует значения длиннее 2704 байт**, а наименование работы приходит из ячейки XLSX и бывает многокилобайтовым (реальный файл: 5077 символов). Любой новый индекс по тексту из сметы — только по `sha256`/GIN, иначе импорт падает `ProgramLimitExceeded` и смета не сохраняется целиком. Две тонкости, обе проверены замером: индексный кортеж перед проверкой предела **сжимается**, поэтому фикстура из повторяющейся фразы дефект не воспроизводит (нужен несжимаемый текст); и `text::bytea` разбирает вход как escape-формат — «C:\temp» приводится с ошибкой, а «\x41» молча даёт тот же байт, что «A», поэтому обратные слэши удваиваются перед приведением (`replace(x,'\','\\')`), а `convert_to` не годится вовсе — она `stable`.
- Деньги: Decimal end-to-end; в JSON — строки; в тестах сравнивать Decimal с Decimal.
- **Pydantic ключует `field_validator`-ы по имени метода.** Два миксина с
  одноимёнными валидаторами схлопываются в **один** слот: побеждает первый по
  MRO, второй исчезает молча — защита читается как живая, будучи мёртвой.
  Замерено в Ф5 фазы 7: схема, наследующая `_MoneyMixin` и `_PercentMixin`, где
  оба метода назывались `_reject_float`, отвергала `float` в сумме договора и
  **принимала** его в проценте условия. Имена методов-валидаторов обязаны быть
  уникальными по всему дереву наследования схемы; проверяется прогоном по
  конкретному входу, а не чтением — по коду оба валидатора выглядят живыми.
- Один uvicorn worker — не «дефолт, который можно поменять», а условие корректности recovery; при переходе на несколько workers понадобится лизинг jobs (за скоупом MVP).
- **Печатную раскладку А4 проверяет только замер в браузере.** `@media print` в jsdom не наблюдаем, и расчёт «на бумаге» ошибается: паспорт с экранными KPI-карточками занимал 284 мм при доступных 277, а таблица с ширинами колонок в rem выходила за правый край листа больше чем на 1100 px, то есть на бумаге правые колонки просто обрезались бы. Ни один тест этого не поймал. Любая правка печатной формы требует замера высоты листа и выхода за край при **максимальном** N (`docs/phase6-analytics.md` §3.2).
- **Наименование работы надо зажимать по высоте всюду, где оно попадает в таблицу.** В каталоге есть работа с наименованием на 5077 символов, и без зажима её ячейка вырастает до 1323 px — одна строка выше листа А4, остальные уезжают за экран. Тонкость: класс `block` рядом с `line-clamp` отменяет `display: -webkit-box`, без которого зажим не работает вовсе; первая правка из-за этого сделала хуже (2201 px), и увидеть это можно только замером.
- **`npx tsc --noEmit` во фронтенде проверяет НОЛЬ файлов.** `frontend/tsconfig.json` —
  references-only (`"files": []`), поэтому голому вызову компилировать нечего: замерено
  `--listFiles` — 0 строк против 1346 у `-p tsconfig.app.json`. Шаг проверки типов в такой
  форме пуст и на фиче разноса спрятал настоящую ошибку компиляции. Гонять `npx tsc -b
  --force`; `just ci` использует `tsc -b --noEmit` и слепым не был — слепыми были ручные
  прогоны.
- **`npx vitest` из КОРНЯ репозитория поднимает другой vitest.** В корне лежит своя
  установка (4.1.10 против 4.1.6 во `frontend/`), она не разрешает алиас `@/`, и весь
  набор падает на сборке с «Cannot find package '@/components/ui/button'», печатая
  «Tests no tests». Выглядит в точности как сломанный импорт в проверяемом коде. Гонять
  только из `frontend/`.
- **Имена CSS-переменных из макета НЕ совпадают с токенами приложения, и
  неразрешимая `var()` не падает — она молча меняет цвет.** Макеты в `docs/`
  автономны и объявляют свою палитру (`--accent`, `--accent-soft`,
  `--accent-text`, `--fg4`); в приложении токены зовутся иначе
  (`--accent-primary`, `--accent-primary-soft`, `--text-tertiary`). Перенос имён
  из макета в компонент даёт объявление, недействительное на этапе вычисления
  значения, а тогда свойство берёт НАЧАЛЬНОЕ: у `fill` это ЧЁРНЫЙ, у `stroke` —
  `none`. Замерено на диаграмме стоимости: все пять имён отсутствовали, столбцы
  стали чёрными, а штриховка промежутка, риска номинала и обе линии медианы
  исчезли совсем — при верной геометрии и сошедшихся до пикселя замерах
  координат. Ни `tsc`, ни `eslint`, ни vitest этого не видят: в jsdom каскада
  нет. Проверять существование токена текстом (тест
  `src/pages/compare/chartTokens.test.ts` — образец), а в браузере смотреть на
  РАЗРЕШЁННЫЕ значения через `getComputedStyle`, не на атрибуты: атрибут со
  строкой `var(…)` выглядит правильным на всех слоях, кроме глаза.
- **Совпадение подписей со столбцами у диаграммы проверяет только замер в
  браузере, и любая правка ширин требует его заново.** jsdom раскладку не считает
  вовсе, поэтому тест может утверждать лишь то, ЧЕМ раскладка запрошена: `w-full`
  у полосы подписей, `flex-1 basis-0` у подписи, `minWidth` как НИЖНЮЮ границу и
  отсутствие инлайновых `width`. Совпадение объявленных ширин друг с другом
  доказательством НЕ является: именно такой тест был зелёным, пока на стенде
  подписи стояли на 645 px левее своих столбцов — полотно тянется на всю ширину
  контейнера, а подписи стояли фиксированными. Правило то же, что для печатной
  раскладки ниже: **правка ширин полотна, полосы подписей или `ChartContainer`
  требует замера в браузере** — центры прямоугольников против центров подписей,
  на узком и на широком окне, — плюс проверки, что `scrollWidth` действительно
  превышает `clientWidth`. Сшивать центры надо со сдвигом на договоры БЕЗ суммы:
  у них есть подпись и нет столбца.
- **Меряя тесноту в браузере, меряй ВСЕ элементы ряда, а не тот, о котором
  сообщили.** Коллизия под столбиками трассы свода стоила ТРЁХ кругов замера:
  первый нашёл налезающие подписи этапов и починил подписи; второй показал ровно
  тот же дефект строкой ниже — у СУММ, другого элемента той же строки, и на
  снимке это читалось как слипшиеся цифры, то есть деньги, отрисованные
  нечитаемо; и только третий круг, измерявший все узлы под столбиком, показал,
  что их три и что третий держится сам. Замер тесноты — это таблица «вид узла ×
  сочетание», а не одно число: наименьший зазор печатается ЧИСЛОМ по каждому
  виду, иначе близкий промах не отличить от запаса. И проверять надо в обе
  стороны — что узел не налезает на соседа И что не выходит за свою колонку:
  перенос лечит первое, создавая второе.
- **Выравнивание СТРОКИ диаграммы двигает СТЕК колонки целиком, а не столбик, —
  и замер обязан сверять то, что КОДИРУЕТ ЧИСЛО, а не только тесноту подписей.**
  Строка трассы свода стояла `flex items-end`, а колонка — вертикальный стек
  «контейнер столбика фиксированной высоты → сумма → подпись этапа → значок
  изменения». У первой колонки изменения нет по построению, значок рисовал
  пустоту (замер: высота узла 0 px против 22,39 px у соседей), и весь её стек
  уезжал вниз ровно на эту разницу: столбик стоял ниже соседних, подписи —
  строкой ниже. Пользователь увидел это на первом же снимке готовой страницы.
  Выравнивать такую строку надо ПО ВЕРХУ: при постоянной высоте контейнера
  совпадение верхов даёт совпадение НИЗОВ — оснований, от которых читается
  высота, — а разница высот стеков уходит вниз, под подписи, где ничего не
  кодирует. Вторая половина правила важнее первой: ТРИ круга замеров этой же
  трассы мерили зазоры между подписями и ни разу не проверили, что столбики
  вообще кодируют числа. У диаграммы, чья геометрия несёт величину, замер
  обязан сверять измеренную величину с полем ответа (здесь — доля высоты
  столбика против `bar_height_pct`: 100 / 99 / 95,59 / 80,2 против
  100 / 99 / 95,6 / 80,2), иначе зелёным остаётся замер «подписи не слипаются»
  у диаграммы, рисующей не те числа.
- **Новый корень страницы обязан нести `container-page`, а правка ширины
  ОБЕСЦЕНИВАЕТ прежние замеры прокрутки.** Утилита объявлена в `index.css`
  (`margin-inline: auto`, `max-width: 1400px`, `padding-inline: 24px`), и её
  несут корни всех страниц и шапка. Свод по этапам уехал в ветку без неё и был
  единственным корнем страницы в проекте, тянущимся во всё окно; замер: корень
  0…1425 px против контейнера шапки 12,5…1412,5 при окне 1440. Ставить её надо
  на КАЖДУЮ ветку возврата — загрузку и пустые состояния тоже, иначе отказ
  выглядит другой поверхностью, чем готовая страница. Вторая половина правила
  дороже первой: у поверхности с горизонтальной прокруткой внутри (таблица со
  многими колонками) ограничитель ширины меняет ровно то, что мерил прошлый
  замер, — поэтому замер повторяется ЦЕЛИКОМ, а не досматривается глазом.
- **«Прокрутка живёт внутри контейнера» — НЕ критерий читаемости таблицы, и
  зажим ширины без переноса хуже, чем его отсутствие.** Три проверки, которыми
  до сих пор кончался замер раскладки (контейнер прокручивается, страница не
  едет, первая колонка закреплена), были зелёными на поверхности, где РАСКРЫТИЕ
  СТАТЬИ выталкивало две правые колонки за край: таблица с авторазметкой отдаёт
  колонке подписи столько, сколько просит самое длинное наименование, и та
  выросла с 459,8 до 752,81 px — 55,8 % контейнера. Мерить надо ещё две
  величины, обе числом: ДОЛЮ ширины у колонки подписи (у зажатой она не растёт
  при раскрытии) и достижимость правой колонки без прокрутки. Вторая половина
  правила оплачена отдельно: примитив `TableCell`/`TableHead` из shadcn несёт
  `whitespace-nowrap`, поэтому один `max-width` не переносит текст, а выпускает
  его ЗА ячейку — замер показал 25 наименований, легших поверх первой денежной
  колонки, то есть первая же правка сделала хуже. Зажим ставится вместе с
  `whitespace-normal` и на ВСЕ ячейки колонки (в авторазметке ширину диктует
  самая широкая), а проверяется числом `scrollWidth > clientWidth` по каждой
  ячейке подписи.
- **Утверждение о том, что делает МАКЕТ, проверяется командой по файлу макета —
  особенно когда на нём стоит запись об осознанном отклонении.** Я написал в
  спеку, в devlog, во врезку самого макета и в вопрос пользователю, что «у
  таблицы макета нет `vertical-align`, действует умолчание `middle`». В файле
  стоит `vertical-align: baseline` явной строкой; `middle` там есть, но у
  ДРУГОЙ таблицы. Цена: решение пользователя записано как отклонение от макета
  той половиной, которая с макетом СОВПАДАЕТ, довод «иначе сумма уехала бы
  вниз» оказался про несуществующее поведение, а альтернатива, которую стоило
  отвергнуть письменно, вовсе не была названа. Ловится одной командой `grep` по
  макету — как и «дефект дофичевый?» ловится `git log`. Правило шире вёрстки:
  ссылка на артефакт, которым обосновано решение, обязана быть прочитана в
  момент написания обоснования, а не восстановлена по памяти.
- **Выравнивание не сходится, пока у ячеек строки РАЗНАЯ КОРОБКА.** Ячейки
  этапов свода были сырыми `<td>` без отступов, соседние колонки — примитивом
  `TableCell` с `p-2`; пока всё нечисловое центрировалось, разница пряталась, а
  на правиле «число по верхней строке» вылезла числом: первая строка ячейки
  этапа на 8 px выше первой строки соседней колонки, ровно на чужой
  `padding-top`. Никакое `vertical-align` этого не лечит. И сам замер тут
  ошибается дважды: рамки inline-`span` и block-`div` считаются по-разному
  (ложные 9 px), а у многострочной ячейки центр блока не совпадает с центром
  первого элемента (ложные 10,75 px). Мерить текстовые прямоугольники через
  `Range` и сравнивать сравнимое.
- **Согласованный макет сверяется с реализацией ОДНИМ ПРОХОДОМ И МАШИНОЙ, а не
  по одному замечанию за круг.** Свод по этапам прошёл четыре круга просмотра
  пользователем, и каждый круг находил ровно одно потерянное ограничение того же
  файла макета. Сверка, сделанная потом за один заход — макет открыт в браузере,
  вычисленные стили сопоставимых узлов сняты с него и со страницы и сопоставлены
  свойство за свойством, — дала девять расхождений разом. То есть цена ловли
  глазами: четыре круга пользователя против одного захода машины. Делать такую
  сверку надо ДО показа, а не после. Две ловушки самого приёма: список
  сравниваемых свойств обязан включать фон, цвет, тени и `z-index` — три находки
  из девяти пришли именно оттуда и первым проходом были пропущены; и сверка
  видит только то, что есть в макете, — расхождение с СИСТЕМОЙ КОМПОНЕНТОВ
  проекта (свой примитив вместо чужого) она не покажет по построению.
- **`Decimal.//` усекает частное К НУЛЮ, а не вниз, поэтому «потолок» через
  `-(-a // b)` ЗАНИЖАЕТСЯ.** Приём верен для `int`, где `//` округляет вниз, и
  неверен для `Decimal`: на отрицательном делимом усечение к нулю и округление
  вниз расходятся. Замер: `205 879` при шаге `50 000` дало 4 интервала вместо 5,
  верх оси диаграммы оказался НИЖЕ самого высокого столбца, и столбец рисовался
  на 10 % короче собственной подписи — во всех состояниях сразу. Ошибку нашло
  измерение высот, а не проверка глазами: картинка выглядела правдоподобной.
  Потолок писать явным сравнением (`n = floor(a / b)`, затем `+1`, если
  `b * n < a`), а у любой «вмещающей» величины держать проверку вида
  `assert max(pool) <= top` — она ловит целый класс ошибок округления, а не эту
  одну.
- **Случайный `npx <инструмент>` из каталога БЕЗ этого инструмента уносит
  `node_modules/.bin` соседнего проекта.** Один вызов `npx tsc` из корня
  репозитория (TypeScript стоит только во `frontend/`) заставил npm поставить из
  реестра пакет-обманку `tsc@2.0.4` — заглушку с сообщением «This is not the tsc
  command you are looking for», — и установка снесла `frontend/node_modules/.bin`
  целиком. После этого `npx` В ПРАВИЛЬНОМ КАТАЛОГЕ начал тянуть инструменты из
  реестра вместо локальных: `npx vitest --version` дал `4.1.11` вместо проектных
  `4.1.6`. Это опаснее двух граблей выше: там неверный каталог виден в самой
  команде, здесь каталог верный, и подмена не видна вовсе — прогон просто идёт
  другой установкой. Признак: строка `npm warn exec The following package was not
  found and will be installed`. Увидел её у `npx tsc`/`npx vitest` — локальной
  установки не нашли, и прогону нельзя верить ни в какую сторону. Лечится `npm
  install` в каталоге проекта (`npm ci` на этой машине упирается в занятый файл).
- **Точность денег в xlsx ограничена форматом, а не кодом.** Формат хранит числа как IEEE-754 double: до ~15 значащих цифр значение доезжает точно, дальше теряет младшие разряды (замер: `12345678901234567.89` → `1.234567890123457e+16`). Писать деньги текстом нельзя — банк складывает колонку, а `СУММ` по тексту даёт ноль. То есть §3 выполняется до файла включительно по величинам договоров ГП, но «полная точность» в xlsx — неправда, и обещать её не надо.
- **Утилиты `dark:` у примитивов shadcn перебивают токен-классы СПЕЦИФИЧНОСТЬЮ,
  и `cn` от этого не спасает.** В `index.css` вариант объявлен как
  `@custom-variant dark (&:is([data-theme="dark"] *))` — селектор составной и
  побеждает одноклассовый вне зависимости от порядка. Замер: `bg-accent-soft`,
  написанный рядом с `variant="outline"`, в тёмной теме проигрывал встроенному
  `dark:bg-input/30`, и выбранный чип фильтра выглядел как невыбранный
  (`oklab(… / 0.3)` вместо `#2A352D`); то же у `Input`/`SelectTrigger` на
  акцентной подложке. `tailwind-merge` конфликта не видит и не должен —
  `bg-*` и `dark:bg-*` разные группы. Ставишь свою заливку поверх примитива с
  `dark:`-правилом — пиши `dark:`-двойника явно. Ловится только браузером:
  вычисленных стилей в jsdom нет.
- **Прогон на стенде заканчивается снимком ЭКРАНА, а не изменённого компонента,
  и обе темы открываются обе.** Дважды за одну фичу пользователь находил по
  снимку то, чего не видели ни `tsc`, ни `eslint`, ни 563 теста, ни мой же
  замер: сначала пять несуществующих CSS-переменных в диаграмме (чёрные столбцы
  при верной геометрии), потом восемь расхождений панели управления с тем же
  согласованным макетом — и вместе с ними два дефекта тёмной темы, которая в
  прогоне не открывалась вовсе. Замеры координат и атрибутов ни одного из них не
  ловят. Смотреть надо на весь кадр, включая то, что этой задачей не трогали:
  макет согласован целиком, а не по компонентам.
- **`activeBar` у recharts не возвращает прямоугольник в неактивный слой, и
  наведение портит столбец НЕОБРАТИМО.** Проп разносит серию на
  `recharts-active-bar` / `recharts-inactive-bar`; наведённый прямоугольник
  переезжает в активный слой, а при уходе курсора обратно НЕ возвращается. Замер
  на диаграмме стоимости: до наведения под `.recharts-bar` восемь прямоугольников
  (две совмещённые серии), при наведении семь плюс активный слой, после ухода
  курсора — снова семь, а восьмой висит снаружи и закрашивает штриховку
  промежутка сплошным. Столбец, на который навели, терял шапку «номинал ↔
  приведённое» до следующего перерендера. У диаграммы из двух совмещённых серий
  `activeBar` не применять; подсветку наведения, если она нужна, делать не
  пропом.
- **Снимок, которым кончается прогон, нужен и для СОСТОЯНИЙ ВЗАИМОДЕЙСТВИЯ,
  которые правка вводит.** Правило выше про «снимок всего экрана в обеих темах»
  оказалось недостаточным: подсветку наведения я снял на снимке ДО наведения, а
  после наведения не снимал — и дефект `activeBar` уехал пользователю. Ввёл
  наведение, фокус, раскрытие, перетаскивание — сними ПОСЛЕ них, а не только до.
- **Пустое поле — не факт, а ОТСУТСТВИЕ факта, и причин у него бывает
  несколько.** Подпись на поверхности не имеет права выбирать одну из них по
  умолчанию. Замер: диаграмма сравнения писала «нет суммы» над договором с полной
  стоимостью, потому что на оси ₽/м² читала `shown_per_sqm`, а сервер оставляет
  его пустым и при незаведённых ТЭП — и причины в `incomplete_reasons` про этот
  случай МОЛЧАТ, они описывают саму сумму. Ту же неправду получал скринридер.
  Лечится различителем в данных (отдельным полем), а не веткой в тексте: иначе
  два факта продолжают жить в одном значении
  (`docs/insights/one-value-two-states.md`). Проверять такой различитель надо в
  ОБЕ стороны — прибив его к `true`, а не только сняв.
- **Подпись, объявляющая НАПРАВЛЕНИЕ, обязана считать его по величинам, а не по
  знаку множителя.** Замер: подсказка столбца брала знак `k − 1`, а при разных
  множителях смет (`inflation_coefficient: null` плюс `inflation_factors`) такого
  знака нет вовсе — и подсказка писала «Приведено, рост» безусловно, хотя агрегат
  мог снизиться. У равенства при этом своё, третье состояние: множитель ровно 1
  достижим, и называть его ростом значит сообщать движение, которого не было.
- **Минус, приклеенный к строке, — не вычитание.** `addDecimalStrings(a, `-${b}`)`
  на отрицательном `b` даёт `--100`, разбор отказывает, функция возвращает `null`,
  и вызывающий получает «не смог посчитать» ровно там, где считать важнее всего.
  Замер: подсказка диаграммы теряла направление приведения и для роста
  (−100 → −80), и для снижения (−100 → −120). Для сравнения есть
  `compareDecimalStrings` — знак там внутри целого `BigInt`, ломаться нечему.
  Отрицательные суммы в проекте хранимы: `position_items.total_cost_total` —
  `Numeric NULL` без `CheckConstraint` снизу.
- **У величины, чей ЗНАК участвует в решении, тест обязан покрыть ОБА знака.** Не
  тот, что стоит на стенде, а оба. Выстрадано дважды в одной строке: правка,
  которая ввела разбор знака, сама же не проверила отрицательный вход, и внешнее
  ревью нашло это вторым кругом по уже исправленному коду. Проверять такое надо и
  снятием «наоборот» — сравнением по модулю: на неотрицательных фикстурах оно
  проходит как верное.
- **Дефект рядом с правкой не помечать «дофичевым» по памяти — это проверяется
  одной командой.** `git log origin/main -- <путь>` говорит, был ли файл в базе
  вовсе. Замер: тон чипа поправки в диаграмме сравнения я объявил дофичевым и
  отложил «до согласования палитры», а компонент заводится тем же PR, в
  base-коммите его нет — то есть дефект был мой, и ярлык снял с меня мою же
  работу. Ревью нашло это третьим кругом. Заодно выяснилось, что и согласовывать
  было нечего: нейтральный тон в компоненте уже существовал.
- **Булево на направление — это два состояния там, где достижимо три.** Множитель
  ровно 1, дельта ровно 0, разность ровно 0 — законные состояния, и в
  `down: boolean` им места нет: всё, что «не down», красится и подписывается как
  рост. Замер: чип печатал нейтральное «поправка 0,0%» жёлтым тоном роста, тогда
  как подсказка на тех же данных уже говорила нейтральное «Приведено». Различитель
  — строка (`"up" | "down" | "flat"`), а не второй флаг: два булева умеют
  оказаться в невозможной паре. И проверять надо в обе стороны: третье состояние
  не должно съесть два первых.
- **«Не входило в список задачи» — не ответ на расхождение с согласованным
  макетом.** Ответ — либо правка, либо запись в долг с причиной; третьего варианта
  нет, потому что уехавшее в `main` расхождение перестаёт быть расхождением и
  становится поведением, за которым не стоит ничьё решение. Замер: порядок корзин
  на экране сравнения расходился с макетом («ДГП ДС Итого» против «Итого ДГП ДС»,
  причём нажатая по умолчанию «Итого» оказывалась третьей кнопкой) — я называл это
  дважды и дважды оставлял с формулировкой «это порядок данных, а не оформление».
  Правка стоила трёх строк, и цена была измерима заранее.
- **`Worksheet.cell(row=, column=, value=None)` в openpyxl НИЧЕГО НЕ ПИШЕТ.**
  Тело метода — `if value is not None: cell.value = value`, поэтому очистить
  ячейку так нельзя; нужно `ws.cell(row=, column=).value = None`. Замерено на
  фиче «колонки по заголовкам»: два теста «очищали» ярус шапки первым способом,
  ярус оставался заполненным, и обе проверки были бы зелёными по совершенно
  другой причине, чем написано в их докстроках. Тест, который что-то «стирает»
  этим вызовом, не проверяет того, о чём говорит.
- **Чтение НЕ-якорной ячейки объединённого диапазона даёт `None`.** Значение
  живёт только в верхней левой ячейке; `ws.cell(...)` по любой другой отдаёт
  пустоту, и код молча теряет заголовок вместо того, чтобы упасть. Замерено там
  же: смещение групповой шапки считалось как позиция колонки «Материалы», что
  совпадает с якорем лишь пока «Материалы» физически первая в группе. При
  перестановке подколонок внутри группы деньги раскладывались верно, а ставка
  НДС становилась `None` — дефект, которого не видел ни один тест, потому что
  все фикстуры переставляли группы целиком. Любое смещение, по которому потом
  ЧИТАЮТ объединённую шапку, обязано быть смещением якоря (`min_col`
  диапазона), а не позицией «удобной» колонки внутри него.
- **Перезапись файла целиком из Python переводит его в LF, и `git status`
  считает файл изменённым при пустом `git diff`.** Рабочее дерево здесь CRLF.
  Само расхождение безобидно, опасна его непрозрачность: `snapshot_parse_samples.py`
  пишет в манифест флаг `dirty`, читая `git status --porcelain -- backend/parser`,
  а `compare_parse_snapshots.py` отказывается сравнивать снимок с `dirty=true`.
  Проверка происхождения при этом срабатывает ПРАВИЛЬНО — она честно отказывает,
  — но причина отказа выглядит необъяснимой: файл «изменён», хотя diff пуст.
  Правки в существующих файлах вносить точечным редактированием, а не
  перезаписью; увидел отказ по `dirty` при пустом `git diff` — смотри на
  окончания строк.

- **Дата внутри ZIP имеет гранулярность 2 секунды, поэтому «тот же файл» из
  тестового помощника два раза подряд — это РАЗНЫЕ байты и разный sha256.**
  `zipfile.writestr(str, ...)` штампует запись текущим временем, а формат DOS
  внутри ZIP хранит его с точностью до двух секунд. Помощник, собирающий
  «тот же» xlsx по одному и тому же маркеру, даёт совпадающие байты, только
  пока оба вызова укладываются в одно двухсекундное окно. Замер: тест
  идемпотентности загрузки раунда («тот же файл — тот же sha, ответ 200»)
  падал ТОЛЬКО на полном прогоне и проходил на своём файле в одиночку — под
  нагрузкой разрыв между двумя вызовами регулярно превышал порог. Ни одно
  подмножество набора его не воспроизводило, потому что течи между тестами не
  было вовсе: подводил сам помощник. Две правдоподобные гипотезы (незаметённые
  таблицы в списке TRUNCATE и незакрытый патч модуля) стоили полутора часов и
  обе оказались неверны. Лечится `ZipInfo` с фиксированной `date_time`.
  Правило шире ZIP: **если тест зависит от ТОЖДЕСТВА порождённого файла, у
  порождающей функции не должно быть источника недетерминизма** — ни часов, ни
  случайности, ни порядка словаря. И проверять такую правку надо в обе стороны:
  одинаковый вход обязан дать одинаковые байты, а РАЗНЫЙ — разные, иначе
  детерминизм куплен ценой того, что помощник перестал различать входы.
- **Два прогона pytest против одной тестовой БД одновременно портят оба.**
  Схема пересоздаётся на сессию, и второй процесс сносит её из-под первого:
  падения расходятся по несвязанным файлам, а время растёт в разы. Замер:
  полный интеграционный набор, запущенный параллельно с агентом, который тоже
  трогал базу, дал 14 падений и 34 минуты вместо 6,5. Признак — падения в
  файлах, которых правка не касалась. Прогон против `gca_test` — один за раз.
- **`max-width` у `td` в АВТОРАЗМЕТКЕ (`table-layout: auto`) НЕ ОБЯЗЫВАЕТ, и
  зелёный замер на коротком содержимом потолок не проверяет вовсе.** Фича
  попозиционного раскрытия статьи наступила на грабли, о которых выше уже есть
  запись про зажим и перенос, — новым механизмом и новым числом. Заголовочная
  ячейка блока разложения не унаследовала общий класс первой колонки свода и
  получила `whitespace-nowrap` от голого примитива `TableCell`; таблица свода —
  ОДНА на весь экран, и `table-layout: auto` делит ширину колонки на все её
  строки сразу. Одна строка длиной около 1500 символов растянула эту общую
  колонку до **5738 px** при объявленных `min-width: 250px; max-width: 420px` —
  и вместе с ней разъехались все ячейки первой колонки, включая короткие
  названия корневых статей вроде «Котлован». Причина та же, что и раньше:
  `max-width` ограничивает ПРЕДПОЧТИТЕЛЬНУЮ ширину колонки, а не итоговую, —
  свободное место авторазметка отдаёт содержимому, а не защите. Замер обязан
  идти по ДОЛЕ ширины колонки на САМОМ ДЛИННОМ содержимом, какое вообще
  встречается в таблице, а не на удобном: собственные числа этой же фичи
  показывают, что зелёный замер на коротком содержимом потолок не проверяет
  вовсе — на длинных настоящих наименованиях колонка встала в 519 px при
  контейнере 1238 (41,9 %), тогда как в макете свода, где подписи короче, та же
  колонка ни разу не подошла к потолку — 358,7 px при контейнере 1200 (29,9 %).
  Ни один из 918 фронтовых тестов, зелёных до браузерной приёмки, дефекта не
  увидел: jsdom раскладку не считает, и авторазметка ей ненаблюдаема в принципе
  (`docs/insights/unobservable-in-the-runner.md`). Настоящая регрессионная сеть
  здесь — className-проверки В ОБЕ СТОРОНЫ (свой класс на ячейке стоит, чужой
  снят), заведённые этой же фичей по образцу `StageSummaryTable.test.tsx`.

## 12. Выстраданные правила работы (инсайты)

§11 — про то, где укусит стек и домен. Здесь — про то, **как работать и как
проверять**. Каждое правило заведено потому, что его отсутствие уже стоило
проекту дефекта или ложно-зелёного прогона; в файле указано, чем именно
выстрадано и **чего правило не ловит**.

- **[Тест, доказывающий защиту, обязан быть проверен снятием защиты](docs/insights/verifying-guards.md)** — четырнадцать слоёв, каждый добавлен после того, как предыдущего не хватило: снять и увидеть красный; убедиться, что снятие применилось; контрольный прогон до снятия; форма CI и по отдельности; эталон не должен ехать вместе с проверяемым; снятие доказывает только исполняемый путь; не валит ничего → защиты нет; защита бывает не одна — снятие одной дефект не воспроизводит, а соседняя защита маскирует снятую, и вход негативного теста обязан нарушать ровно одно ограничение, иначе маскировку создаёт сам выбор входа; и наконец — **снятие обязано быть тем, которое может случиться**: на сконструированном варианте подтверждается свойство, которого у теста нет, а утверждение об отсутствии требует сигнала, общего для обеих веток. Десятый слой уточняет седьмой и смотрит на защиту, которую **только что завели**: верное рассуждение не доказывает, что условие делает работу — её мог уже делать кто-то другой (`fetchState()` библиотеки гасил ошибку сам, снятие моего условия не уронило ничего), и мёртвая защита утаскивает за собой комментарий, объясняющий несуществующий механизм. Одиннадцатый и двенадцатый добавлены фичей диаграммы стоимости: маскировку создаёт и ПОЗДНЕЙШАЯ правка соседнего слоя — тест был честным в момент написания и стал пустым от моей же правки, не покраснев ни разу; и ложную зелень создаёт ВЫБОР ЧИСЕЛ во всех фикстурах разом — три утверждения об отсутствии выполнялись по совершенно другой причине, чем та, о которой были. Тринадцатый стоит особняком и добавлен сводом по этапам: ложный зелёный бывает у САМОГО СНЯТИЯ, а не у теста — четыре случая за одну фичу: якорь встретился одиннадцать раз и правка ушла в чужой хендлер; скрипт умер до восстановления и оставил файл пропатченным; «несуществующая» проба оказалась годной; удаление строки сломало СБОРКУ, а не поведение, и разбор вывода прочёл это как зелёное. Четырнадцатый заведён внешним ревью PR #34 и уточняет шестой и седьмой: у ветвления, которое ВЫБИРАЕТ, что показать, снятие обязано проверять и то, что выбранная ветка не ВЫТЕСНИЛА соседнюю. Замер: ячейка колонки без базы НДС показывала только подпись «нет базы НДС», съедая состояние — снятие подписи краснело честно, но «состояние на месте РЯДОМ с подписью» не утверждал никто, и порядок двух веток тернарника и был всем дефектом.
- **[Предпосылка теста бывает ложной, и видно это только замером](docs/insights/false-test-premises.md)** — лемма, которой модель не даёт; выборочный промах float; вакуозный тест метки времени в транзакции; замер печати, мерявший состояние по умолчанию вместо того, о котором дано обещание (§7.4), — дефект пережил две фичи. Предпосылку, способную тихо измениться, проверять внутри самого теста; «состояние по умолчанию» — тоже предпосылка.
- **[Прогон, который молчит вместо того чтобы упасть](docs/insights/silent-test-runs.md)** — успех читается по числу прошедших, а не по коду возврата: файл без `pytestmark` (молча пропало 65 тестов), integration без `TEST_DATABASE_URL`, `&&` с `grep` вместо отдельных шагов, ПРОГОН ПО КАТАЛОГУ вместо формы гейта (семь запрещённых `as any` дожили до полного `npm run lint`, а за ними пряталась сверка с чужим множеством кодов), невзлетевший раннер, снесённый `caplog`, `try/finally` в PowerShell (после блока `$?` равен `True` при ненулевом `$LASTEXITCODE`), конвейер (`just ci | tail` возвращает код `tail` — падение приходит как `exit 0`).
- **[Дефект, который просыпается только на партии](docs/insights/batch-larger-than-five.md)** — `prepare_threshold = 5` в psycopg3: партия из 5 проходит, из 10 падает. Raw-SQL объекты схемы проверять партией >5, а не одной строкой.
- **[Порядок и связь вызовов стережёт только утверждение о потоке данных](docs/insights/data-flow-assertions-for-order.md)** — пять случаев на одной фиче (четыре круга правок плана и один на реализации): снятие, ломающее порядок или связь звеньев, назначалось тесту результата отдельной функции, который его не ловит по построению. Шпион на каждом звене по коду, сравнение записанного между звеньями; стаб не ставится на звено, чьё снятие тест стережёт.
- **[Одно значение на два состояния](docs/insights/one-value-two-states.md)** — если значение несёт СМЫСЛ, отсутствие данных обязано быть отдельным состоянием: `?? null`, `?? []`, `?? 0` превращают «не знаем» в утверждение. Один класс дал **три формы** за три круга ревью фичи инфляции, и соответствия «форма ↔ круг» нет: первая потребовала двух кругов, потому что правка после первого убрала один источник промаха, а не сам класс. Промах поиска стал режимом создания; упавший запрос списка стал пустым справочником, и экран **лгал**, зовя завести ряд, который уже есть; «ждём» и «не дождёмся» одним значением дали бесконечную загрузку. Правило про домен в §10 уже есть («нет норматива», «0 %» и «неизвестна база НДС» — три факта); это оно же на слое состояний загрузки. Носитель различия — тип (обязательное поле), а не дисциплина; ветку ошибки, которую видит только вызывающий, изнутри прочитать нельзя. Признак, что лечится симптом: правка перечисляет случаи вместо смены контракта.
- **[Утверждение, которого прогон не может опровергнуть](docs/insights/unobservable-in-the-runner.md)** — снятие защиты бессильно там, где сам прогон не наблюдает проверяемого свойства, и зелёное означает «смотреть было нечем», а не «код верен». Три случая одной формы: jsdom не считает раскладку, и тест о совпадении ОБЪЯВЛЕННЫХ ширин был зелёным, пока на стенде подписи разъехались со своими столбцами на 645 px; граница «параметр не вернулся» стояла таймером, то есть мерила машину, а не механизм, и снятие защиты её не роняло; `@media print` в jsdom не наблюдаем вовсе — там это стоило обрезки листа, которую не поймал ни один тест. Как теперь: границу наблюдаемости называть в докстроке теста вместе с тем, ЧТО проверяет само обещание, и у каждого такого теста называть напарника — прогон на стенде либо пункт приёмки; отложенное обещание, которого не называет ни один чек-лист, испаряется.
- **[Правило, записанное половиной, ловит половину дефектов](docs/insights/state-the-rule-as-an-equivalence.md)** — инвариант формулируется как ЭКВИВАЛЕНТНОСТЬ, которую реализует код, а не как та импликация, что ловит сегодняшний дефект: ошибка, вытесненная из первой половины, селится во второй. Выстрадано сводом по этапам — девять кругов правок фикстур при тридцати двух зелёных инвариантах; «шаг между двумя доступными концами не бывает погашен» ловилось, обратное «настоящий вид между двумя недоступными» жило в фикстуре четыре круга. Второй случай той же фичи: правило читало ПОКАЗАННУЮ сумму там, где код решает по ВАЛОВОЙ — верно для всех обычных ячеек и неверно ровно для ячеек без базы НДС; а когда фикстура разошлась с таким правилом, правка пошла в обратную сторону — фикстуру подогнали под тест. Признак: очередная правка чинит названное, а следующее чтение находит тот же по смыслу дефект на шаг в сторону.
- **[Новое правило надо прогнать, а не описать](docs/insights/replaying-new-rules.md)** — три слоя, все оплачены в Ф2: свойство проверки утверждалось, не будучи прогнанным по конкретному входу (неисполнимый тест колонки A, ложно-зелёный тест денег); новое правило не прогонялось по **уже существующим** входам (строка лота в общем хелпере, моки в чужом файле, лист без A/B); и — отдельно — покрытие спеки сверялось по памяти, поэтому три её поведенческих требования остались без исполняющего теста при верной реализации, и нашли это ревьюеры. На финале строить соответствие «требование спеки → тест» списком; требование без теста либо получает тест, либо объявляется границей в devlog.
- **[Правку решения надо пройти по списку зависимостей, а не по памяти](docs/insights/walk-dependents-with-a-tool.md)** — фича попозиционного раскрытия статьи прошла двенадцать кругов внешнего ревью (восемь по спеке, четыре по плану), и **девять из двенадцати оказались одним классом**: решение менялось верно в одном месте и не доезжало до всех мест, которые от него зависят — словарь состояний объявлен переиспользованным и переписан заново, ключ допработ по наименованию склеивал разные работы, кнопка была нарисована и не подключена, ревизия «раскрывать поддерево статьи» не прошла по четырём местам сразу, три пересказа того же решения разошлись в комментариях генератора, довод «иначе не сойдётся» в пользу расширения ключа оказался попросту неверным. Лечится не памятью, а списком: после правки решения выписать явным перечнем всё, что от него зависит (текст, контракт, ключи, тесты, поведение элементов), и пройти список ИНСТРУМЕНТОМ — `grep` по старым словам, счётчик в браузере, замер числом, — а не чтением своей же правки.
- **[Замер обязан строить данные той же моделью, что и экран](docs/insights/same-model-for-measurement-and-screen.md)** — та же фича: генератор замеров сперва считал группы работ отдельной функцией, читавшей только `position_items`, тогда как экран строит их `load_groups` (позиции плюс допработы плюс непривязанные строки). Числа получались правдоподобные и были неверны — допработы на КОНЦАХ трассы меняли и движение статьи, и отбор объяснителей (369/363 объяснителей, 1017/1025 групп, 0/1 групп с дырой в середине — модель «только позиции» против модели экрана), и самый дорогой случай («дыра в середине») был всё это время в данных, а не в будущем, как гласила граница. Второй пример той же фичи: смена ключа группировки со «статья + позиция» на «каталожную позицию без статьи» пересчитала число групп с нулевой суммой хотя бы на одном этапе с 507 и 294 на 480 и 156 — прежние числа перестали быть замером и стали производной от новой модели. Замер строит вход ТОЙ ЖЕ функцией, что и показ, а не параллельной, которая читает то же место в базе иначе; расхождение двух моделей одного факта решается чтением кода, а не усреднением.
- **[Помощник, которого никто не доказал вызванным](docs/insights/prove-the-component-calls-it.md)** — три раза за одну фичу (попозиционное раскрытие статьи) помощник был написан верно и оттестирован юнитом, а компонент не был доказан его вызывающим: пилюля допработы (поймано ещё на плане), React `key` строки разложения (задача 10 — компонент честно отрисовывал бы обе строки при коллизии ключей, тест этого не видел), латентно — тест пилюли одного лота, не отличавший условный вызов от захардкоженной строки. У помощника, который компонент вызывает УСЛОВНО или из которого собирает видимое значение, нужен тест не на сам помощник, а на то, что компонент его действительно зовёт, — и портить в проверке надо КОМПОНЕНТ (альтернативная сборка прямо в разметке), а не помощника; там, где фреймворк глотает дефект молча (React с дублирующимся `key` только пишет в stderr), одного атрибута мало — нужен ещё наблюдатель за побочным сообщением (`console.error`).
- **[Когда защита реальна, а эффект — нет, ищи наблюдаемого соседа](docs/insights/find-an-observable-neighbor.md)** — уточняет слой 7 [verifying-guards.md](docs/insights/verifying-guards.md) с противоположной стороны: там механизм не делает ничего, здесь механизм (ключ монтирования блока работ, задача 11) делает настоящую работу, а снятие всё равно не роняет тест — потому что `gcTime: Infinity` и неотменяемый запрос без `signal` гасят РАЗНИЦУ между «смонтирован после закрытия» и «нет», а не потому что раннер не умеет её увидеть (это уже отличает случай и от [unobservable-in-the-runner.md](docs/insights/unobservable-in-the-runner.md)). У того же ключа была вторая обязанность — ЛЕНИВОСТЬ, запрос не должен уходить до первого открытия, — и она наблюдаема тем же раннером, просто её никто не спросил явно (`grep` по `toHaveBeenCalled` дал ноль совпадений). Увидев, что guard-removal ничего не роняет, не делать вывод сразу: спросить, исполняется ли механизм и производит ли разное состояние, перечислить ВСЕ его обязанности и поискать среди них наблюдаемую — и только когда ни одной не нашлось, дело переходит в область unobservable-in-the-runner.md.
- **[Список свойств правила живёт в тесте СТРУКТУРОЙ, а не намерением сверяющего](docs/insights/enumerate-the-rules-own-properties.md)** — пять случаев одной формы на одной фиче, причём четвёртый произошёл ВНУТРИ фикса третьего, уже после того как правило было записано словами, а пятый — ВНУТРИ фикса четвёртого, уже после того как правило стало структурой (тип + счётчик длины): сверка накрывает подмножество членов проверяемого целого и объявляет результат полным. DoD 6 (задача 12) сравнила 72 пары `getComputedStyle`, все — свойства СТИЛЯ, и ни разу текст узла (пропала строка «2 стр. · 1 компл» допработы); тот же список сравнил у кнопки «Работы» радиус/отступ/цвет/фон и ни разу — `border` и `:hover` (полировка, круг 1); фикс ЭТОГО расхождения сам оказался списком из пяти признаков против одиннадцати деклараций правила плюс двух у `:hover`, и пропустил `cursor`/`white-space` (круг 2, ветка `feat/drilldown-polish`) — несмотря на то, что первый круг уже требовал перечислять свойства ИЗ ПРАВИЛА. Круг 3 переписал список СТРУКТУРОЙ (тип `WorksDeclaration` с обязательной диспозицией, счётчик `toHaveLength`) и всё равно потерял `font: inherit` при переносе строки правила — число «десять» подтвердило уже неполный список сам собой, потому что было пересчитано по нему, а не по правилу; без видимых последствий, поскольку Tailwind Preflight ставит это свойство кнопке безусловно (круг 4, тот же файл). Закрыто ТОЧЕЧНО: число и состав деклараций теперь считаны функцией, читающей файл мокета с диска и извлекающей блок `.works { … }` по литеральному селектору, а сверка со списком идёт множеством имён, а не литералом длины — для `.works:hover` и для любых других списков той же формы это по-прежнему требует отдельной реализации.

Новый инсайт заводится на финале фичи (§9.1 шаг 5) и **только** если отсутствие
правила уже стоило дефекта — со ссылкой на случай. Положительные приёмы и общие
соображения сюда не попадают: документ читается целиком в начале каждой сессии,
и его ценность держится на том, что каждая строка оплачена.
